# הכרעות סבב N — מחזור ראשון
**מאת: Tech Lead · 21 בספטמבר 2026 · קראתי את כל החומר שהשארתם**

---

## למפתח

### N-D1 · שבע המחרוזות — **מאושרות, עם שינוי אחד**
נזרעו. 312 שורות `ui`, אף קוד בלי שפה.

**`ui.bucket_weekend` הוחלף ב-`ui.bucket_thisweek` ("השבוע").**
צדקת ב-§0.2, והעלית את זה נכון: בגבול `d<=6`, ביום ראשון משמרת של יום
שלישי יושבת תחת כותרת שאומרת "סוף השבוע". **תווית שלא מתארת את מה
שמתחתיה גרועה מאי-קיבוץ.** "השבוע" נכון בכל יום בשבוע.

**מה שנשבר בגלל זה, ואתה מתקן:** בדלי "השבוע" ו"בהמשך" השורה מציגה רק
שעה, והעובד לא יודע אם זו שלישי או שישי. **הוסף את יום השבוע לשורות
בשני הדליים האלה בלבד** — לא בהיום ולא במחר, שם הוא רעש.

**זו לא מחרוזת חדשה:**
```js
d.toLocaleDateString('he-IL', { weekday: 'short' })
```
נכון לכל שפה בלי שורה אחת ב-`terms`. לזרוע שבעה ימים בשתי שפות כדי
לחזור על מה שהפלטפורמה כבר יודעת היא הטוהרנות הלא-נכונה.

**`ui.flag_until` עם `{n}` — מאושר כפי שהוא.** `{n}` הוא חריץ יחיד
גנרי, לא בהכרח מספר. **אבל אל תיתן לו `.one`/`.other`** — הוא לא מונה.

### N-D2 · חמש המחלקות הנוספות — **נכנסות לחוזה**
`.list-c` · `.vr` · `.num` · `.cur` · `.hot`/`.need`.
כולן מהמוקאפ, והמוקאפ הוא החוזה. **§2 בלוח הסבב מתעדכן — הן חלק ממנו
מעכשיו, לא חריגה.** יפה שעצרת לשאול במקום להוסיף בשקט.

### N-D3 · `.has-act` — **הכלל שלי משתנה, לטובת המדידה של המעצב**
ההוראה המקורית: `.has-act` רק בשורות עם כפתור. **המעצב מדד שזה מקפיץ
את טור הסכום ב-164px** בין שורה עם כפתור לשורה בלעדיו, באותו דלי.
הסכום הוא העוגן של D17; טור שקופץ הורס אותו.

**הכלל החדש:**
- **`.has-act` על כל שורה בדלי שיש בו פעולה** — גם על שורה שכבר הגישו
  אליה. תא רביעי ריק לא עולה כלום, והקפיצה יורדת לאפס.
- **`.c-act` (הכפתור עצמו) רק בשורות שאפשר להגיש אליהן.**

שני דברים שונים: הרצועה היא פריסה, הכפתור הוא יכולת.

---

## למעצב

### N-D4 · שתי ההכרעות שלך — **שתיהן מאושרות**
**`152px` ולא `auto`** ו-**`.has-act` על כל שורות הדלי**. שתיהן נתמכות
במדידה על הקובץ האמיתי (164px מול 0), ושתיהן מתקנות הוראה שלי שהייתה
שגויה. זה בדיוק מה שאני רוצה שיקרה כשאני טועה.

### מה שנשאר לך
המפתח מוסיף יום בשבוע לשורות ב"השבוע" ו"בהמשך" (N-D1).
**איפה הוא יושב בתוך `.c-time` — שלך.** מתחת לשעה? לצד `.c-len`?
שמור על 58px הרצועה ואל תיתן לגובה השורה לזוז.

---

## ל-QA

### N-F1 · **צדקת בממצא, וטעית ב"ה-revoke בטוח"**
הפגם אמיתי ורחב ממה שדיווחת: **כל** פונקציה ב-`private` נשאה execute
ל-PUBLIC, ולשני התפקידים היה USAGE על הסכימה. לא רק `is_adult`.

**אבל "אימתתי שאף INVOKER לא קורא ל-private" לא מכסה את המסלול שחשוב.**
בדקתי: **אחת-עשרה מדיניות RLS** על `applications`, `business_members`,
`profile_contacts`, `reviews` ו-`team_members` קוראות ל-
`private.is_business_member` / `is_business_owner` / `shift_business` /
`counterpart_submitted`. **מדיניות מוערכת בתור התפקיד השואל.** revoke
גורף מ-`authenticated` היה מחזיר permission denied על חמש טבלאות —
כלומר משבית את האפליקציה.

**מה שהוחל, לפי פונקציה ולא לפי סכימה:**
- `anon` — USAGE על `private` הוסרה כליל. אין לו שם עסק.
- ארבע הפונקציות שהמדיניות צריכה — נשארות ל-`authenticated`.
- חמש פונקציות טריגר INVOKER — נשארות, לכתיבות ישירות של הלקוח.
- **`is_adult` ו-`is_operator` נשללו גם מ-`authenticated`.** אלה השניים
  שבאמת חשובים: הגיל הוא בדיוק מה שהוצא מהמשטח, ומי-מאמת-את-מי אינו
  מידע ציבורי.

**אומת אחריו:** חמש הטבלאות עדיין נקראות בהתחזות (1/0/1/2/0 שורות,
`my_shifts` רץ), ו-`private.is_adult` מחזיר **`permission denied`**.

**דיוק אחד לחומרה:** PostgREST חושף רק `public`. הפונקציות ב-`private`
לא היו נגישות מרחוק דרך ה-API גם לפני התיקון. זו הייתה הרשאה שגויה
ושכבת הגנה חסרה — **לא דליפה פעילה מהאינטרנט.** לדייק בחומרה זה חלק
מהעבודה; ממצא מנופח שוחק את הבא אחריו.

### מסלול ההצלחה של `verify_phone` — **הרצתי, בדיוק כפי שניסחת**
ב-transaction עם `rollback`. מפעיל → `verified` עם חותמת זמן.
אחרי ה-rollback: **0 מאומתים, 0 מפעילים.** לא אומת אף חשבון אמיתי.

### N-Q1 · הלוח ל-`anon` — **כן, בכוונה. ולא אבד כלום**
הסרתי את ההרשאה כחלק מרצפת ההרשאות. **אבל היא מעולם לא הייתה מסלול
עובד:** `shifts_read` ו-`businesses_read` הן `to authenticated`, ולכן
קורא אנונימי קיבל אפס שורות גם כשה-EXECUTE היה לו. התיעוד שלך רשם
**מענק**, לא יכולת. עדכן את §4 בהתאם.

**ושאלת המוצר שמאחורי זה טובה ואני שומר אותה:** אם הלוח ייפתח לגלישה
בלי חשבון, ההרשמה תקרה רק ברגע הפעולה — **ואז תפיסת מספרים נעלמת מעצמה,
כי אין חשבונות לא מאומתים שתופסים מספרים.** זה קושר את N-Q1 לסבב הזהות.
לא מחליט עכשיו; נכנס כמועמד.

### N-F2 · `phone_conflict`
הענף נשאר — הוא מגן מפני מצב שאסור שיקרה, וזה בדיוק תפקידו.
ההערה אומרת שהייחודיות יושבת ב-Auth ושכפילות "אמורה להיות בלתי אפשרית".
**אם יש שם משפט שאינו מדויק — צטט אותו** ואתקן. סיווג "הנמקה שגויה"
בלי ציטוט הוא בדיוק מה שסגרנו בסבב הקודם.

---

## ל-DevOps

### ה-hash — **הטעות שלי**
מסרת אותו ב-20/09 ב-`DEVOPS-SPEC-A2-HANDOFF.md`, ואני רשמתי בלוח
"ממתין ל-hash" ואז ביקשתי אותו שוב. לא קראתי את הקובץ. הלוח מתוקן.

`dde8b829…fadb63` נרשם. **מה ש-N5 ממתין לו באמת הוא 36 השדות ממני**,
כפי שכתבת. קורא את §4 שלך ומחזיר אותם בהכרעה הבאה.

### N4 — הרנבוק
נכתב לפני שהחסמים נפתחו, וזו העבודה הנכונה: ברגע שהמייסד מאשר, הפתיחה
היא דקות ולא סבב.

---

## מה שנשאר אצל המייסד
ללא שינוי: דחיפה לגיטהאב · הפעלת R2 · סימון הסיכה · והוספתו כמפעיל
ראשון, שבלעדיה מנגנון האימות נשאר תיאורטי.
