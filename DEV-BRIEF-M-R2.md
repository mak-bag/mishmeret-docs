# DEV-BRIEF — סבב M, סיבוב 2: הכרעות והשתלה
**מאת: Tech Lead · 20 בספטמבר 2026 · נספח ל-`DEV-BRIEF.md`, לא מחליף אותו**

תיק החפיפה שלך התקבל. העבודה טובה, והבאג שמצאת ב-`whenSized` הוא ממצא אמיתי
שלא היה בבריף — מדידת מיכל לפני שהפריסה הסתיימה. תודה שתיעדת אותו עם המספר
(`beforeSize {x:0,y:318}`) ולא כתחושה.

להלן ההכרעות. אחת מהן מבטלת חלק מהעבודה של המעצב, ושתיים סוגרות חסימות שלך.

---

## 1. התנגשות ה-CSS — הכרעה: **הבלוק שלך נשאר, לא של המעצב**

כתבת CSS למפה ב-00:08. המעצב מסר חוזה מחלקות אחר ב-00:13. שני המימושים
שלמים ולא תואמים: אצלך `.pin-shift` הוא האלמנט עצמו עם `.many`, אצלו
`.pin-shift > .pin-body` עם `.multi`; אצלך `.pin-pick.on`, אצלו
`.unset/.set/.is-dragging`; אצלך `.pop-row`, אצלו `.map-pop`.

**הבלוק שלך הוא הבסיס.** לא כי הוא טוב יותר — אלא כי הוא כבר משולב, כבר
נטען בלי שגיאות, ומחליף אותו עכשיו פירושו להחליף גם JS שאיש לא יכול לבדוק
בדפדפן עד שהמייסד ייכנס. **להכניס שינוי לא בדיק לתוך קוד לא בדיק זה איך
מצטברים באגים שאין להם בעלים.**

הערה לזכותך: המעצב הזהיר ש-Leaflet כותבת `transform:translate3d()` על אלמנט
הסמן ולכן חייב אלמנט פנימי. **המבנה שלך כבר עומד בזה** — `className:''`
ו-`html:'<span class="pin-shift">'`. הגעתם לאותה מסקנה בשתי דרכים.

### מה כן נלקח מהמעצב — ארבעה פריטים, כולם CSS טהור

**זה החלק שרק הוא יכול היה לייצר**: פילטר האריחים כויל בשלוש איטרציות מול
נתניה, מפרץ חיפה ומרכז תל אביב. אל תשנה בו מספר.

**א. שני טוקנים, בשלושת בלוקי הטוקנים** (`:root`, `@media
(prefers-color-scheme:dark)`, `:root[data-theme="dark"]`). המעצב מדווח
שבסבב קודם `replace_all` פספס לו בלוק כהה אחד בגלל הזחה שונה — **ספור שלושה**.

```css
/* ב-:root */
--tiles-filter:none;
--pin-shadow:0 2px 6px rgba(13,27,51,.32);

/* בשתי חסימות הכהה */
--tiles-filter:invert(1) hue-rotate(182deg) brightness(.86) contrast(1) saturate(.78);
--pin-shadow:0 2px 10px rgba(0,0,0,.62);
```

**ב. פילטר האריחים ובידוד הערימה.**
```css
.leaflet-tile-pane{filter:var(--tiles-filter)}
```
ולכלל `.map` הקיים שלך הוסף `isolation:isolate` — **בנוסף** ל-`z-index:0`
שלך, לא במקומו. הערת "השורה הזו מבנית" שלך נכונה ונשארת.

**אל תעלה את הפילטר ל-`.leaflet-container`.** שם הוא יהפוך גם את הסמנים,
הפופאפים והעיגולים.

**ג. צל הסמן.** ב-`.pin-shift` החלף `box-shadow:var(--shadow)` ב-
`box-shadow:var(--pin-shadow)`. `--shadow` הוא צל של כרטיס; מעל אריחים הוא
מרוח מדי.

**ד. מצב "סמן פתוח".** חסר אצלך, ועל מפה צפופה מאבדים את הסימן שנפתח.
```css
.pin-shift{transition:transform .14s var(--ease-soft)}
.pin-shift.is-open{transform:translateY(-3px) scale(1.08);z-index:1000}
```
ובצד ה-JS, על כל סמן:
```js
mk.on('popupopen',  e => e.target.getElement()?.firstElementChild?.classList.add('is-open'));
mk.on('popupclose', e => e.target.getElement()?.firstElementChild?.classList.remove('is-open'));
```

### מה שנשאר שלך ולא מוחלף
`.pop-name/.pop-sub/.pop-count/.pop-list/.pop-row`, `.seg/.seg-b`,
`.pin-pick` העגול עם `.on`, `.pin-shift.many`, `.pin-shift.approx`,
`.radius-ring`, בקרות הזום וה-attribution. שקול-ערך למה שהמעצב הציע, וכבר משולב.

---

## 2. פגם שמצאתי בעצמי — התווית של סמן העיר

הרצתי רתמת אינטגרציה משלי: **ה-markup שלך** עם **פילטר האריחים שלו**, 40
סמנים מעל מרכז תל אביב, בשני המצבים. הצירוף הזה לא נבדק על ידי אף אחד
מכם — כל אחד בדק את הצד שלו.

**התוצאה, במספרים מדודים:**

| מה נמדד | ערך | פסק |
|---|---|---|
| `.leaflet-tile-pane` filter | `invert(1) hue-rotate(182deg)…` | חל |
| `.leaflet-marker-pane` filter | `none` | אין זליגה |
| `.leaflet-popup-pane` filter | `none` | אין זליגה |
| `.leaflet-overlay-pane` filter | `none` | אין זליגה |
| `.leaflet-tooltip-pane` filter | `none` | אין זליגה |
| `.pin-shift` רקע / טקסט (כהה) | `rgb(37,99,235)` / לבן | `--accent-solid`, 5.17:1 |
| `.radius-ring` stroke (כהה) | `rgb(92,155,255)` | CSS גובר על Leaflet |
| `.map` isolation | `isolate` | ההשתלה תופסת |
| סמנים שנוצרו | 40, מהם 8 `approx` | הצפיפות עובדת |

**ו-הפגם:** `.leaflet-tooltip` יוצא **לבן, `rgb(255,255,255)`, עם טקסט
`rgb(34,34,34)`** — ברירת המחדל של Leaflet. במצב כהה זו לוחית לבנה בוהקת על
מפה כהה. ראיתי אותה בעיניים ומדדתי אותה.

אתה השתמשת ב-`bindTooltip({permanent:true})` לתווית "העיר שלכם"; המעצב
עיצב `.pin-city .pin-label` — אלמנט פנימי משלו שלא קיים אצלך. אף אחד לא
עיצב את ה-tooltip של Leaflet, כי אף אחד לא ידע שהשני משתמש בו.

**ההכרעה: משאירים `bindTooltip` ומעצבים אותו.** כלל אחד מכסה גם כל שימוש
עתידי בתווית:
```css
.leaflet-tooltip{background:var(--surface);color:var(--text);
  border:1px solid var(--border);border-radius:8px;box-shadow:var(--pin-shadow);
  font-size:12px;font-weight:600;padding:4px 9px;white-space:nowrap}
.leaflet-tooltip-bottom::before{border-bottom-color:var(--border)}
```
המעצב יאשר את הערכים ויבדוק בעיניים אחרי ההדבקה. **אל תמתין לו** — הדבק
כמו שכתוב וסמן את המקום.

---

## 3. שתי החסימות שלך — שתיהן נפתרו

### 3.1 `my_shifts()` — תוקן במסד, ואתה טעית בחצי
**`business_id` כבר היה שם** לפני הסבב הזה, יחד עם `biz_verified_at`.
אבל **הנקודה שלך נכונה בעיקר**: ל-`located_at` לא הייתה דרך להגיע, כי
משמרת מאושרת לא מופיעה ב-`shifts_for_me` ולכן ה-join שלך ב-`loadBoard`
לא מכסה אותה. מסכים שזה כבר לא קוסמטי — זה קובע לאן העובד נוסע.

מיגרציה `my_shifts_exposes_biz_located_at` הוחלה. העמודה החדשה היא
**`biz_located_at`** (באותה מוסכמה כמו `biz_verified_at`, לא `located_at`).
ההרשאות שוחזרו במדויק: `authenticated` ו-`service_role` בלבד, בלי `public`.

אומת בהתחזות: המשמרת המאושרת היחידה במסד חוזרת עם `address` ריק
ו-`biz_located_at` מלא — כלומר בדיוק המקרה שבו הכלל שלך מחייב ניווט
לקואורדינטה, וקודם לא הייתה דרך לדעת.

**אצלך בקוד:** הסר את ההשלמה ב-`loadBoard` שמזריקה `located_at` לשורות
`my_shifts` מתוך `S.bizAddr` — היא מיותרת עכשיו ומכסה רק חלק מהמקרים.
קרא ישירות `m.biz_located_at`.

### 3.2 טוסט אחד, לא שניים — **מאושר**
הנימוק שלך נכון והתקדים קיים. הודעה מחוברת אחת. ה-`§3.4` המקורי מבוטל.

---

## 4. שתי שאלות של המעצב שהכרעתי

**טבעת ה-50 ק״מ לא נראית בזום 14** (הוא מדד 12,400px ברוחב מפה של 688px).
**אין שינוי.** זום 14 הוא הזום שבו מסמנים דלת של מסעדה, וזה מה שהמסך קיים
בשבילו. הטבעת היא מעקה שנעשה רלוונטי בדיוק כשמתרחקים — ואז היא מופיעה מעצמה.
לא לפתוח את זה שוב.

**attribution ב-10.5px וב-`--muted`** (5.4:1 בהיר, 6.87:1 כהה) — **מאושר,
וחתום עליי.** זה גודל ברירת המחדל של Leaflet בקירוב, הטקסט קריא והקישור
עובד. שניכם הגעתם לאותם ערכים בנפרד.

---

## 5. החסימה האמיתית: כניסה למערכת

צדקת שלא עקפת את הכלל, וצדקת שלא ביקשת סיסמה. **אל תבקש גם בהמשך.**

הפתרון: **המייסד מקליד בעצמו.** אתה מרים את השרת המקומי, פותח את מסך
הכניסה בחלונית הדפדפן ועוצר. הוא מקליד את הטלפון והסיסמה בעצמו בחלונית.
משם הסשן מחובר ואתה מריץ את כל 12 הסעיפים בלי שראית תו אחד מהסיסמה.

תתאם איתו מתי. אל תתחיל את §6 לפני זה, ואל תסמן שום סעיף כ"עבר" בלעדיו.

---

## 6. סדר העבודה מכאן

1. ההשתלה מסעיף 1 (ארבעה פריטים) ומסעיף 2 (כלל ה-tooltip).
2. `biz_located_at` מסעיף 3.1, כולל הסרת ההשלמה המיותרת.
3. טוסט מחובר מסעיף 3.2.
4. המייסד נכנס → 12 סעיפי §6 → דיווח.
5. אז QA.

**מה שלא נבדק ברתמה שלי ואסור לספור כבדוק:** שום דבר שדורש כניסה —
כתיבת `located_at`, `shifts_moved`, `location_far` מהטופס, זהות רשימה↔מפה,
יצירת `applications` מפופאפ, גרירת הרדיוס. הרתמה בדקה שכבת CSS בלבד, בדיוק
כמו שהרתמה שלך בדקה. **היא לא אימות של §6, וגם שלי לא.**
