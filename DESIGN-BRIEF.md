# DESIGN-BRIEF — סבב M: השכבה הוויזואלית של המפה
**מאת: Tech Lead · 19 בספטמבר 2026, לילה**

אתה המעצב. בסבב הזה נכנסים לאפליקציה שני מסכים עם מפה, ואתה מספק את
**כל** ה-CSS שלהם. המפתח לא ימציא צבעים ולא פילטרים — הוא לוקח מכאן.

**אתה מספק CSS בלבד, בתוך בלוק אחד מסומן, ואתה לא נוגע ב-JS.**
`app.html` נערך במקביל על ידי המפתח בסבב הזה. **תאם איתו מי מחזיק את הקובץ
מתי, או מסור לו את הבלוק והוא ידביק.** הקובץ כבר השתנה מתחת לידיים יותר
מפעם אחת.

---

## 1. מה נבנה

**A. מפת בחירת מיקום למעסיק** — בטופס פתיחת/עריכת עסק. סיכה שנגררת, עיגול
50 ק״מ מקווקו סביב מרכז העיר, שורת מצב מתחת.

**B. מפת משמרות לעובד** — תצוגה חלופית ללוח. סמנים לפי עסק, עיגול רדיוס
סביב מרכז העיר של העובד, סמן דהוי למרכז העיר עצמו, פופאפים עם כרטיס מוקטן.

הספרייה היא Leaflet 1.9.4 עם אריחי OpenStreetMap. **אנחנו דורסים את העיצוב
של Leaflet כמעט לגמרי** — ברירת המחדל שלה (הסיכה הכחולה-אפורה, הפופאפ הלבן
עם ה-X) נראית כמו 2011 ותשבור את המראה של האפליקציה.

---

## 2. המגבלות. כולן מתועדות, כולן כבר עלו לנו פעם

1. **אין אימוג׳י.** בשום מקום, כולל בסמנים. סט האייקונים הוא
   `.icon.icon-<name>` עם `mask-image` שצובע `currentColor`. צריך אייקון
   חדש (סיכה, שכבות) — הוסף באותה שיטה, לא פונט ולא תמונה.
   התו היחיד שמותר הוא `✓`.
2. **`--accent-solid`, לא `--accent`, בכל משטח מלא עם טקסט לבן.** זה כולל
   את מתג רשימה/מפה ואת הסמנים. הבאג הזה חזר כבר פעמיים אחרי שתוקן —
   בכפתורים ואז בגלולת הלשוניות התחתונה (2.77:1 במצב כהה).
3. **מצב כהה נבדק בעיניים, לא בהיגיון.** יש builder שמייצר וריאנט כהה כפוי
   (מחליף `@media (prefers-color-scheme:dark){` ב-`@media all{`) — בנה את
   שתי הווריאנטות לפני שאתה אומר גמור.
4. **טוקנים בלבד.** אל תכתוב hex חדש בתוך כלל CSS. אם חסר צבע, הוסף טוקן
   ב-`:root` **וגם** בשתי חסימות הכהה (`@media` ו-`[data-theme=dark]`) —
   שתיהן קיימות ושתיהן חייבות להתעדכן.
5. **`[hidden]{display:none !important}` קיים במכוון.** כל `display` חדש על
   מחלקה שיושבת על אלמנט שמוסתר ב-`hidden` תיפול באותה מלכודת.
6. **RTL.** הממשק עברי. Leaflet ממקמת בקרות ב-`left`; השתמש ב-`inset-inline`
   ובמאפיינים לוגיים, לא ב-`left/right`.
7. **הכל בתוך `app.html`.** אין build, אין npm, אין קובץ CSS נפרד.
   CSS של Leaflet מגיע מ-CDN; ההתאמות שלנו יושבות אחריו.

---

## 3. מה אני צריך ממך, פריט-פריט

### 3.1 מיכל המפה — `.map`
פינות `var(--r)`, `border:1px solid var(--border)`, `overflow:hidden`,
`background:var(--surface-2)` (מה שנראה בזמן שהאריחים נטענים).
גובה: `min(62vh, 560px)`, לא פחות מ-320px. ברוחב טלפון המפה לא גולשת
מהמרווח הצדי של התוכן.

`.map-pick` (מפת המעסיק) — נמוכה יותר, 280px, היא יושבת בתוך טופס.

### 3.2 אריחי OSM במצב כהה
זו הבעיה הוויזואלית האמיתית של הסבב. אריחי OSM בהירים תמיד, ומפה לבנה
בוהקת בתוך ממשק כהה נראית כמו באג.

תן טוקן, למשל `--tiles-filter`, `none` בבהיר ומשהו כמו
`invert(1) hue-rotate(180deg) brightness(.92) contrast(.9) saturate(.75)`
בכהה, מוחל על `.leaflet-tile-pane`.

**הערכים למעלה הם נקודת פתיחה, לא תשובה.** תכוון בעיניים על נתניה ועל חיפה:
הכבישים צריכים להישאר קריאים, הים לא צריך להיראות כמו יבשה, וטקסט השמות
חייב להישאר קריא. **הפילטר לא חל על השכבות שלנו** — סמנים, עיגולים ופופאפים
יושבים בשכבות אחרות וחייבים להישאר בצבעי המותג. ודא את זה.

### 3.3 סמן משמרת — `.pin-shift`
`L.divIcon` עם ה-HTML שלנו. שתי צורות:
- **משמרת אחת**: גלולה קטנה עם `₪<תעריף>`, רקע `--accent-solid`, טקסט
  `--on-accent`, זנב משולש קטן שמצביע על הנקודה.
- **כמה משמרות באותו עסק**: אותה גלולה עם המספר, ומשקל חזותי מעט גדול יותר.

דרישות: נקראת מעל אריח בהיר **ומעל אריח כהה** (צל או קו מתאר), לפחות
`44×32px` שטח נגיעה, מצב `:hover`/`:active` עדין, ומצב "נבחר" כשהפופאפ פתוח.
**`--accent-solid`.** גלולה על `--accent` במצב כהה נופלת מתחת ליחס הניגודיות.

תג משני לעסק שהמיקום שלו משוער (`ui.approx_location`) — הסמן צריך להיראות
פחות ודאי: קו מתאר מקווקו, או אטימות מופחתת. **זה לא קישוט.** סיכה שיושבת
במרכז העיר ונראית בדיוק כמו סיכה מדויקת היא הבטחה שקרית לעובד שנוסע.

### 3.4 סמן העיר של העובד — `.pin-city`
עיגול קטן דהוי, `--muted`, בלי מילוי מלא. הוא הפניה, לא יעד. לצדו התווית
"העיר שלכם".

### 3.5 עיגול הרדיוס — `.radius-ring`
קו מקווקו ב-`--accent`, מילוי ב-`--accent` ב-6–8% אטימות. חייב להיקרא בשני
המצבים בלי להסתיר את האריחים.
אותו סגנון, בגוון `--muted`, לעיגול 50 ק״מ במפת המעסיק — שם הוא אומר "הגבול",
לא "הטווח שלך".

### 3.6 הסיכה של המעסיק — `.pin-pick`
שני מצבים, וההבדל ביניהם חייב להיות ברור בהצצה:
- **לא נקבע** (יושבת במרכז העיר כברירת מחדל): עמומה, מקווקוות, מרחפת.
- **נקבע** (המעסיק גרר או לחץ): מלאה, `--accent-solid`, עם צל.

מצב גרירה: משוב קל (הגדלה קטנה, צל ארוך יותר). זו הפעולה שהמסך קיים בשבילה.

### 3.7 פופאפ — `.leaflet-popup`
דרוס את `.leaflet-popup-content-wrapper`, `.leaflet-popup-tip` ואת כפתור
הסגירה כך שייראו כמו `.card`: `--surface`, `--border`, `var(--r-s)`,
`var(--shadow-lift)`. רוחב 260–300px, גלילה פנימית אם יש יותר משלוש משמרות.
כפתור `.btn` בתוך הפופאפ שומר על המראה הקיים שלו ועל שטח נגיעה מלא.

### 3.8 מתג רשימה/מפה — `.seg`
שני מקטעים, הפעיל מלא ב-`--accent-solid` עם `--on-accent`, הלא-פעיל
`--surface-2` עם `--muted`. גובה זהה לשאר הבקרות בשורת הכלים, מעבר 
`var(--ease-soft)` קצר. **זה בדיוק המקום שבו הבאג של `--accent` חזר בפעם
השנייה.** בדוק אותו במצב כהה לפני שאתה מוסר.

### 3.9 מצבי טעינה וכשל
שלד למפה בזמן טעינת Leaflet (`ui.map_loading`) — משטח `--surface-2` באותו
גובה בדיוק, בלי קפיצת פריסה. מצב כשל (`ui.map_failed`) בסגנון `.panel.warn`
הקיים. **אל תמציא רכיב הודעה חדש** אם `.panel.warn` ו-`.empty` מכסים.

### 3.10 בקרות Leaflet
כפתורי הזום, ה-attribution והסקאלה — התאם לטיפוגרפיה ולגבולות שלנו.
**ה-attribution של OSM נשאר גלוי.** הוא תנאי רישוי, לא קרדיט שאפשר להחביא.
מותר להקטין ולהחליש, אסור להסתיר.

---

## 4. מה לא לעשות

- **לא להחליף ספק אריחים.** Mapbox/Google/Stadia דורשים מפתח וחיוב.
  התקציב הוא אפס, וזו החלטה ארכיטקטונית לא פתוחה.
- **לא לטעון פונט או ספריית אייקונים חדשה** בשביל סמן אחד.
- **לא `!important`** מלבד כשדריסת Leaflet לא מותירה ברירה — ואז עם הערה
  שאומרת למה.
- **לא לגעת ב-`assets/`.** הנכסים נחתכים פרוגרמטית מקובצי המקור; אל תערוך
  קובץ נגזר ביד.

---

## 5. גמור = 

1. שני המסכים צולמו במצב בהיר **ובמצב כהה**, על רוחב טלפון ועל דסקטופ.
2. הסמנים נקראים מעל שכונה צפופה (נסה מרכז תל אביב) ומעל שטח פתוח.
3. הפילטר הכהה לא נזל לסמנים, לעיגולים ולפופאפים.
4. כל צבע חדש הוא טוקן, והוגדר בשלושת המקומות.
5. `.seg` הפעיל נבדק לניגודיות במצב כהה. מספר, לא תחושה.
6. אין אימוג׳י, אין hex מקומי, אין `left/right` פיזיים.

סיים בבלוק החפיפה (פורמט ב-`HANDOVER-DEV.md` §9), ל-**Tech Lead**, ובו
במפורש: מה נבדק בעיניים בשני המצבים, ומה לא.
