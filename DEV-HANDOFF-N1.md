# DEV → Tech Lead · N1 — חלופה ג מומשה
**מאת: Developer · 21 בספטמבר 2026, 15:2x**
**`app.html` 137,493 בתים · 21/09 15:22 · קונסולה נקייה**

---

## 0. מה חוסם אותך — שלושה פריטים, כולם קצרים

### 0.1 שבע מחרוזות לאישור · **חוסם את כותרות הדליים**
אף אחת מהן לא נכתבה בקוד. הקוד קורא לקודים; בלי זריעה ב-`terms`
הכותרות יוצאות ריקות.

| קוד | עברית | אנגלית |
|---|---|---|
| `ui.bucket_today` | היום | Today |
| `ui.bucket_tomorrow` | מחר | Tomorrow |
| `ui.bucket_weekend` | סוף השבוע | This weekend |
| `ui.bucket_later` | בהמשך | Later |
| `ui.bucket_count.one` | משמרת אחת | 1 shift |
| `ui.bucket_count.other` | {n} משמרות | {n} shifts |
| `ui.flag_until` | עד {n} | until {n} |

`bucket_count` נושא מספר → מפוצל ל-`.one`/`.other` לפי הכלל.
`flag_until` נושא **שעה** ולא מספר, אבל השתמשתי ב-`{n}` כי `fill()`
הקיים יודע רק אותו. אם אתה מעדיף placeholder אחר — תגיד ואשנה.

**מה שלא ביקשתי, כי כבר קיים ב-`terms`:** `ui.adults_only`,
`ui.unverified_biz`, `ui.approx_location`, `ui.slots_left.one/.other`.
"דחוף", "הסעות", "ש׳" ו"לשעה" נשארו כפי שהם כבר היום בקוד — לא
הוספתי מחרוזות קשיחות חדשות.

### 0.2 גבול הדלי — שאלה על סמנטיקה, לא על שם
מימשתי לפי המוקאפ: `d<=6 → סוף השבוע`. המשמעות בפועל — **ביום ראשון,
משמרת ביום שלישי תופיע תחת "סוף השבוע".** התווית לא תתאר את מה
שמתחתיה.

לא נגעתי, כי המוקאפ הוא ההכרעה. שתי חלופות אם תרצה:
- לצמצם את הדלי לשישי–שבת הקרובים ולהוסיף דלי "השבוע" (מחרוזת נוספת), או
- להשאיר את הגבול ולשנות את התווית ל"השבוע".

### 0.3 חמש מחלקות שאינן ברשימת §2
כולן מהמוקאפ, אף אחת אינה המצאה שלי — אבל אתה מחזיק את החוזה:
`.list-c` (המיכל), `.vr` (ה-✓ של עסק מאומת), `.num`, `.cur`,
ו-`.hot`/`.need` בתוך `.c-flags`.

---

## 1. באג שנמצא, והוא קדם לסבב הזה

הטענה החדשה החזירה `.c-act` 0/0, והדליים יצאו "מחר | סוף השבוע"
במקום "היום". השורש:

```js
Math.round((start - new Date().setHours(0,0,0,0)) / 864e5)
```

מחסרים חותמת מלאה מחצות ומקבלים **שבר יום**; `round` על 0.75 נותן 1.
**כל משמרת שמתחילה אחרי 12:00 היום סווגה כמחר.**

אותו ביטוי בדיוק ישב ב-`whenLabel` וב-`workerCard` — כלומר הבאג קיים
בקוד **מלפני הסבב הזה**: משמרת של היום ב-18:00 הוצגה "מחר" גם בפופאפ
של המפה וגם בכרטיסי "המשמרות שלי".

**תיקון:** `dayOffset` אחד מנרמל את **שני** הצדדים לחצות מקומית לפני
החיסור. זה גם סופג את שעת המעבר לשעון קיץ. שלושת המקומות קוראים לו.

---

## 2. מה מומש

- קיבוץ לארבעה דליים, דלי ריק לא מוצג, הכותרת נושאת מונה.
- **אין מיון מחדש בלקוח** — הסדר הוא מה שהשרת החזיר.
- `.c-act` **רק** בשורות דלי "היום" (§4), על אותו `data-claim` הקיים.
  לא הוספתי מאזין; ההאצלה על `#view` עובדת.
- המפה ומתג רשימה/מפה לא שונו.
- `shiftCard` הישן **נמחק** — הוחלף במלואו, ולהשאיר פונקציה שמציירת
  את העיצוב הקודם זו מלכודת למי שיבוא אחריי.

---

## 3. הרתמה — 11/11, אפס שגיאות קונסולה

| טענה | תוצאה |
|---|---|
| **N1 — סכום שורות == `boardShifts()`** | ✓ 3 == 3, שני דליים מוצגים |
| **N1 — `.c-act` רק ב"היום"** | ✓ 2/2 בדלי היום, 0 מחוצה לו |
| §6.2 סיכה נשלחת | ✓ `p_lat 32.806100` / `p_lng 34.995200` |
| §6.3 סיכה **לא** נשלחת | ✓ `p_name,p_city_id,p_address,p_phone,p_kind` |
| §6.5 `location_far` | ✓ הנוסח העברי מ-`terms` |
| §6.6 רשימה↔מפה | ✓ 3 == 3 |
| §6.7 הגשה מפופאפ | ✓ קריאה אחת, `p_shift=s1` |
| §6.8 אשכול | ✓ סמן אחד, "2", שתי שורות |
| §6.9 מחוון רדיוס | ✓ 0 שגיאות, מפה חיה אחת |
| תג "מיקום משוער" | ✓ 1 מתוך 3 |
| `wazeUrl` | ✓ ll=1 · q=1 · בלי קישור=1 |

**עדכוני פיקסצ׳ר שנדרשו:** המשמרות מקובעות עכשיו לימים יחסיים
(`atDay`) כדי שהדליים יהיו דטרמיניסטיים בכל שעת ריצה, וטענת
רשימה↔מפה סופרת **שורות** ולא כפתורים — כי בחלופה ג רק דלי היום
נושא כפתור.

---

## 4. מה לא סגרתי, ולמה

**מבחן 20 בשני המצבים — לא רץ.** הוא דורש את ה-CSS של N2.
הכנסתי בלוק זמני מסומן ‼ עם **גיאומטריה בלבד מתוך §2** כדי שהרתמה
תוכל לרוץ: רוחבי העמודות כפי שכתבת, והרביעית ב-`.has-act`. אין בו
צבע חדש, אין עיצוב ל-`.c-act` מעבר למיקום, ואין החלטה על 375px.

**`DESIGN-CSS-N2.css` ו-`DESIGN-NOTE-N2.md` נחתו ב-15:29 — לא קראתי
ולא הדבקתי.** לפי ההערה בלוח, ארבע העמודות ב-375 נפסלו במדידה ויש
מבנה חלופי, ושתי הכרעות וממצא אחד ממתינים לך. **אני לא מדביק לפני
שתכריע**, כי המבנה החלופי עשוי לגעת במה שה-markup פולט.

**המחרוזות ברתמה זמניות.** הזרקתי את שבעת הקודים לנתוני הבדיקה של
הרתמה בלבד, מסומן בהערה. **לא ב-`app.html` ולא במסד.** כשתזרע אותן,
הרתמה תמשוך את האמיתיות והבלוק הזמני יהפוך ללא-פעולה.

---

## 5. בלוק חפיפה

```
--- HANDOFF TO: Tech Lead ---
DONE:      חלופה ג ב-app.html: קיבוץ לארבעה דליים, שורה בחוזה §2,
           .c-act רק בדלי היום. dayOffset תוקן ומאוחד לשלושה מקומות.
           shiftCard הישן נמחק. רתמה הורחבה בשתי טענות; 11/11 עוברות,
           אפס שגיאות קונסולה. אומת ברתמת לקוח, לא בדפדפן מחובר.
STATE:     app.html 137,493 בתים, 21/09 15:22. מסד ללא שינוי, אפס
           כתיבות. הרתמה ב-scratchpad. ריפו עדיין לא נדחף.
NEXT:      1) אישור שבע המחרוזות וזריעתן. 2) הכרעה על גבול הדלי.
           3) הכרעה על חמש המחלקות. 4) הכרעתך על N2 → אדביק CSS →
           מבחן 20 בשני המצבים. 5) QA.
FOR YOU:   §0.1–§0.3. שלושתם קצרים ושלושתם חוסמים אותי.
MUST KNOW: הבאג ב-dayOffset היה בקוד לפני הסבב והשפיע על whenLabel
           ועל workerCard, לא רק על הדליים. אם QA בדק "היום/מחר"
           בעבר — הבדיקה ההיא רצה מול קוד שגוי.
OPEN:      מבחן 20 חסום על N2. **אף סעיף לא מסומן "עבר מקצה לקצה" —
           הרתמה היא שכבת לקוח.** התפר, דפדפן מחובר מול PostgREST,
           נשאר לא מכוסה.
--- END HANDOFF ---
```
