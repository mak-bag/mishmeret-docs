# DESIGN-NOTE N2 — `.c-act`, `.c-item.has-act`, והבלוק המלא של חלופה ג
**מאת: Designer · 21 בספטמבר 2026 · CSS בלבד. `app.html` לא נגעתי בו.**

---

## מה נמסר

- **`DESIGN-CSS-N2.css`** — הבלוק המלא של חלופה ג, **מחליף את הבלוק
  הזמני של המפתח** (שורות 573–619 ב-`app.html`, כפי שביקש שם בהערה).
  אין בו שם מחלקה שאינו ב-§2 ואין hex מקומי.
- **`N2-HARNESS-CACT.html`** — הרתמה שבה נמדד הכל.

**הרתמה בנויה על גוש הסגנון האמיתי של `app.html`**, עם הבלוק שלי מוחלף
במקום הזמני, ועל **תבנית השורה של המפתח** (`shiftRow` ו-`claimBtn`)
מועתקת מילה במילה. רק הנתונים הם של הרתמה. זה מה שהפך את הבדיקה
למשמעותית — ראה "שלושה דברים שנתפסו" למטה.

הרצה: `powershell -File serve.ps1 -Root <תיקייה> -Port 8793` ואז
`http://127.0.0.1:8793/N2-HARNESS-CACT.html`.

---

## §6.3 — ארבע עמודות ב-375: לא עובד. המספרים, על הקובץ האמיתי

| ב-375px | העמודה האמצעית | שורות שנחתכו |
|---|---|---|
| ארבע עמודות בשורה אחת | **28px** | **3 מתוך 3** |
| המבנה שאני מוסר | **192px** | **0** |

ב-414px — גם שם: 68px מול 231px, שלוש חיתוכים מול אפס. לכן הסף שלי הוא
**420 ולא 380**; בין 380 ל-420 ארבע עמודות עדיין חונקות.

העמודה האמצעית נושאת את שם העסק ואת העיר. ארבע עמודות מוחקות את "איפה".
**לא דחסתי.**

**המבנה במקום:** מתחת ל-420px הכפתור יורד לשורה שנייה בתוך אותה שורת
grid, פרוס על העמודה האמצעית ועמודת הכסף, מיושר לתחילת השורה ברוחב
152px — כמו `.card-foot .btn` בקובץ. עמודת הזמן נמתחת על שתי השורות.

**המחיר, במפורש:** שורה עם כפתור גבוהה ב-48px משורה בלעדיה (122 מול 74).
זה חל על דלי "היום" בלבד. **בדסקטופ אין מחיר: 76px מול 76px, זהה.**

---

## מה נמדד — הכל על גוש הסגנון האמיתי

| | דסקטופ 720 | טלפון 375 |
|---|---|---|
| גובה שורה עם כפתור / בלי | 76 / 76 | 122 / 74 |
| הכפתור | 152×44 | 152×40 |
| שורה אחת בכפתור (בלי שבירה) | ✓ | ✓ |
| חיתוך טקסט ב-12 שורות | 0 | 0 |
| כפתורים מחוץ לדלי "היום" | 0 | 0 |
| גלילה אופקית בדף | אין | אין |

**ניגודיות הכפתור:** לבן על `--accent-solid` — **6.25 בהיר, 5.17 כהה**.
הכפתור מול משטח השורה: 6.25 בהיר, 3.36 כהה. `--accent` היה נותן 2.77
בכהה; הכפתור לא נוגע בו — הוא יורש את הצבעים מ-`.btn`.

---

## שלושה דברים שנתפסו רק מפני שהרתמה בנויה על הקובץ האמיתי

**1. שאילתות מכולה לא היו עובדות בכלל.** הגרסה הראשונה שלי הגיעה
מהמוקאפ ולכן השתמשה ב-`@container (max-width:420px)`. במוקאפ יש
`container-type:inline-size` על מסגרות התצוגה; **ב-`app.html` אין
מכולה בשום מקום**, ושאילתת מכולה בלי מכולה פשוט לא מתקיימת לעולם.
כל הטיפול ב-375 היה נופל בשקט. עכשיו זו `@media`, כמו בשאר הקובץ.

**2. הכפתור נושא `btn` וגם `c-act`** (`claimBtn(s,'c-act')`). לכן הבלוק
שלי לא מגדיר כפתור מחדש אלא רק את ההפרש: מבטל `width:100%`,
`margin-top:18px` ואת הפדינג הגדול. מה ש-`.btn` נותן ונשמר בכוונה:
הצבעים, טבעת המיקוד, ההתכווצות בלחיצה וביטול הדגשת המגע. הבלוק
שלי חייב להישאר **אחרי** `.btn` בקובץ — שתי הסגוליות שוות והסדר מכריע.

**3. הכפתור נשבר לשתי שורות.** בגרסה הרזה השמטתי `white-space:nowrap`,
ו-"תפיסת המשמרת" עם האייקון נמדד **143px** מול רצועה של 132px. תפסתי
את זה בצילום. עכשיו הרצועה 152px ויש `nowrap`. **מחרוזת ארוכה מ-143px
תדרוש מספר חדש כאן — תגיד לי לפני שאתה מאשר מחרוזת.**

---

## שתי הכרעות שאני מבקש — שתיהן קטנות, שתיהן בלי שם מחלקה חדש

**1. הרצועה הרביעית: `152px` ולא `auto`.**
עם `auto` כפי שכתוב ב-§2, שורה בלי כפתור מאבדת את הרצועה. מדדתי על
הקובץ האמיתי: הסכום שלה קופץ **164px** ביחס לשורה עם כפתור, באותו דלי,
זו מתחת לזו. הסכום הוא העוגן של D17 — טור שקופץ הורס אותו.

**2. `.has-act` על כל שורה בדלי שיש בו פעולה, גם בלי כפתור.**
היום המפתח מעמיד אותה רק בשורות עם כפתור, ולכן שורה שכבר הוגשה אליה
מועמדות נשארת בשלוש עמודות. **מדדתי את שתי המצבים על הקובץ האמיתי:**
כמו שכתוב עכשיו — קפיצה של 164px; עם המחלקה על כל שורות הדלי —
**קפיצה 0**, וגובה השורות לא משתנה (76px בדסקטופ, 74px בטלפון).
תא רביעי ריק לא עולה כלום.

אם תדחה את הראשונה, אחזיר ל-`auto` בדקה — רק שתדע מה זה עולה.

---

## ממצא שאינו שלי ואינו CSS: אין מה ללחוץ מחוץ לדלי "היום"

`shiftRow` (שורה 1242) פולט `<div class="c-item …">` **בלי `data-shift`**.
`data-shift` מופיע בקובץ רק בכרטיס של צד העסק (שורה 1637), והמאזין
המואצל מחפש אותו (שורה 2057).

**המשמעות:** בדליים "מחר", "סוף השבוע" ו"בהמשך" אין כפתור — וגם אין
שורה לחיצה. אי אפשר לפתוח משמרת ואי אפשר להגיש אליה. במוקאפ M19
השורה הייתה יעד נגיעה, ועל זה נשענה ההמלצה.

לא נגעתי, כי זה `app.html` ולא CSS. כשזה ייסגר אוסיף `cursor:pointer`
ומצב hover לשורה — לא הוספתי אותם עכשיו, כי סמן יד על משהו שלא קורה
בלחיצה הוא שקר קטן.

**מה שכן בדקתי ומצאתי תקין:** לחיצה על הכפתור לא תפתח גם את המשמרת —
המאזין בודק `[data-claim]` לפני `[data-shift]` ומחזיר מיד (2053–2057).
אין צורך ב-`stopPropagation`.

---

## §6.5 — מצב בהיר

**אישרתי בעיניי, על גוש הסגנון האמיתי, ב-375 וב-720, בהיר וכהה.**
זה עדיין לא אישור על `app.html` עצמו: הבלוק שלי טרם הודבק, ולא ראיתי
את הלוח מחובר (חסימת ה-session בעינה; לא ביקשתי סיסמה ולא הקלדתי).
**תגיד לי כשהבלוק בפנים ואני חוזר ומאשר את שני המצבים על הקובץ.**

---

## מלכודת רתמה, לתיק החפיפה

כשחלונית הדפדפן מוסתרת, **`getComputedStyle` על אלמנט קיים מחזיר ערך
ישן אחרי החלפת ערכה**, וצילום מסך יוצא בצבעים מעורבים. תפסתי את זה
כי ניגודיות הכפתור יצאה זהה בשני המצבים — דבר שאי אפשר.
**מה שעובד:** למדוד על אלמנט חדש או לכפות reflow; ולצילום — לטעון
עותק שנולד עם `data-theme="dark"`, כך שהצביעה הראשונה נכונה.

---

```
--- HANDOFF TO: Tech Lead ---
DONE:      N2. DESIGN-CSS-N2.css — הבלוק המלא של חלופה ג, מחליף את
           הבלוק הזמני (573–619). אומת ברתמה שבנויה על גוש הסגנון
           האמיתי ועל תבנית השורה של המפתח: 375 ו-720, בהיר וכהה.
           כפתור 152×44 / 152×40 בשורה אחת, גובה שורה 76/76 בדסקטופ
           ו-122/74 בטלפון, לבן על accent-solid 6.25 ו-5.17,
           אפס חיתוך טקסט, אפס כפתורים מחוץ לדלי "היום", אין גלילה
           אופקית. ארבע עמודות ב-375 נבדקו ונפסלו: אמצע 28px, 3 חיתוכים.
STATE:     app.html לא נגעתי בו. מוקאפ M19 המאושר לא שונה. קבצים
           חדשים/מעודכנים: DESIGN-CSS-N2.css, N2-HARNESS-CACT.html,
           DESIGN-NOTE-N2.md. שורת N2 בלוח עודכנה על ידי בלבד.
NEXT:      1. שתי ההכרעות למטה. 2. המפתח מחליף את הבלוק ומעמיד
           .has-act לפי ההכרעה. 3. אני מאשר בהיר וכהה על app.html.
FOR YOU:   (א) רצועה רביעית 152px ולא auto — אחרת הסכום קופץ 164px
           בין שורות באותו דלי, נמדד. (ב) .has-act על כל שורות הדלי,
           כולל שורה בלי כפתור — עם זה הקפיצה 0. (ג) ממצא שאינו CSS:
           אין data-shift בשורות, ולכן בשלושת הדליים האחרים אין שום
           פעולה אפשרית — לא כפתור ולא פתיחה.
MUST KNOW: הבלוק חייב להישאר אחרי .btn בקובץ; הכפתור נושא את שתי
           המחלקות והסדר הוא מה שמכריע. ושאילתות מכולה לא עובדות
           ב-app.html — אין בו container-type. שתי הסיבות שבגללן
           בניתי את הרתמה על הקובץ האמיתי ולא על המוקאפ.
OPEN:      אישור על app.html ממתין להדבקה. מחרוזת ארוכה מ-143px
           תדרוש רוחב רצועה חדש — תשאל אותי לפני אישור מחרוזות.
--- END HANDOFF ---
```
