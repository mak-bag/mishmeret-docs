# DESIGN-NOTE M19 — שלוש חלופות להיררכיית לוח המשמרות
**מאת: Designer · 20 בספטמבר 2026 · מוקאפ בלבד, `app.html` לא נגעתי בו**

---

## מה נמסר ואיך פותחים

`MOCKUP-M19-BOARD.html` — קובץ יחיד, שלוש החלופות בדף אחד, אותם נתונים
בשלושתן כדי שההשוואה תהיה על ההיררכיה בלבד.

**הקובץ הועתק לתיקיית הפרויקט ולא נשאר ב-scratchpad.** הבריף ביקש
scratchpad, אבל ה-scratchpad הוא פר-סשן ונמחק איתו — וקובץ שאתה צריך
לפתוח חייב לשרוד את הסשן שלי. העותק ב-scratchpad עדיין שם.

בדף יש שני מתגים: **מצב כהה** ו**מבחן הצפיפות · 20**, שמכפיל את הרשימה
לעשרים כרטיסים. זה המבחן שקבעת שקובע, ולכן הוא לחיצה אחת ולא הרכבה ידנית.

הרצה (אין node ואין python אמיתי במכונה):

```bash
powershell -File serve.ps1 -Root <תיקיית הקובץ> -Port 8793
```

ואז `http://127.0.0.1:8793/MOCKUP-M19-BOARD.html`. ב-`file://` הדף מרונדר
כתמונה סטטית בהירה, ה-JS לא רץ, ואין מה לראות.

---

## ההמלצה: ג · ציר זמן מקובץ

**הזמן הוא הכותרת שלך (D17), ובחלופה ג הוא לא תגית בתוך הכרטיס אלא מבנה
הדף.** ארבעה דליים — היום · מחר · סוף השבוע · בהמשך — ולכן השורה עצמה
משוחררת מהתאריך ועונה רק על מה · כמה · איפה. עשרים משמרות הופכות לארבע
החלטות, ועובד שפותח את האפליקציה ב-16:00 רואה בלי לקרוא שורה אחת אם יש
משהו להערב. זה בדיוק המדד של המוצר.

ג משיגה כמעט את כל הצפיפות של א (2.7 מסכים מול 2.4 לעשרים משמרות) ומשלמת
עליה פחות: השורה נשארת מיכל עם גבול ורדיוס, ולכן היא תסבול בעתיד כפתור
פעולה — דבר שבשורת רשימה טהורה אין לו מקום.

**הסיכון שאני לא מסתיר:** בג אי אפשר להגיש בלי לפתוח. למשמרת **שיבוץ
מיידי** זה תוסיף שנייה או שתיים בדיוק במקום שבו הן נמדדות. הדרך הזולה
לבדוק את זה בסבב הבא היא כפתור פעולה **רק בשורות של דלי "היום"**, ולא
בכל הרשימה. לא בניתי את זה כאן כי זו חלופה רביעית ולא ביקשת אחת.

**מתי הייתי חוזר לא:** אם תחליט שהמיון אינו תמיד לפי זמן. כל מיון אחר
שובר את ג — שורה מחוץ לקבוצה שלה אינה נושאת תאריך.

---

## שלוש החלופות, שתי שורות כל אחת

**א · שורת רשימה.** *מנצחת:* הזמן בעמודה קבועה, הסריקה היא ירידה בטור
אחד — שמונה שורות במסך טלפון מול שלושה כרטיסים. *מקריבה:* אין מקום
לכפתור, וכשיש שלושה מצבים באותה שורה הם נדחסים לטקסט זעיר שמאבד משקל.

**ב · כרטיס.** *מנצחת:* כל משמרת עומדת בפני עצמה — זמן ככותרת, סכום כולל
כמספר הגדול, כתובת וכפתור באותה יחידה; היחידה שבה מגישים בלי לפתוח.
*מקריבה:* צפיפות — שלושה כרטיסים למסך, ועשרים משמרות הן 5.4 מסכים שבהם
אי אפשר להשוות שתי משמרות זו לזו.

**ג · ציר זמן מקובץ.** *מנצחת:* הזמן יצא מהכרטיס אל מבנה הדף; "אין כלום
להערב" נראה בלי לקרוא. *מקריבה:* השורה מאבדת עצמאות — אין בה תאריך מחוץ
לקבוצה — והכותרות גובות גובה כשיש משמרת בודדת בדלי.

---

## מה נמדד — לא הוערך

### צפיפות · מסגרת 375×812, שורה חציונית

| | שורה חציונית | לעשרים משמרות | מסכים לגלילה |
|---|---|---|---|
| א | 95px | 1,930px | **2.4** |
| ב | 218px | 4,418px | **5.4** |
| ג | 94px (+4 כותרות) | 2,216px | **2.7** |

בדסקטופ (720px, רוחב `.wrap` האמיתי) השורה של א יורדת ל-74px.

### ניגודיות · WCAG, נמדד בדפדפן בשני המצבים

| צמד | בהיר | כהה |
|---|---|---|
| `--text` / `--surface` | 17.19 | 14.95 |
| `--muted` / `--surface` | 5.40 | 6.87 |
| `--navy` (הסכום) / `--surface` | 14.13 | 13.96 |
| **לבן / `--accent-solid`** (כפתור) | **6.25** | **5.17** |
| לבן / `--accent` — *הטעות שלא עשיתי* | 6.25 | **2.77** |
| `--amber` (דחוף) / `--surface` | 5.78 | 8.50 |
| `--accent-2` (תקנים) / `--surface` | 8.46 | 8.58 |
| `--green` (✓ מאומת) / `--surface` | 5.34 | 7.60 |
| `--muted` / `--surface-2` | 4.78 | 6.14 |
| `--accent-2` / `--accent-soft` | 7.34 | 7.03 |
| `--amber` / `--amber-soft` | 5.05 | 7.48 |
| `--green` / `--green-soft` | 4.69 | 6.61 |

**הנמוך ביותר: 4.69.** כל הצמדים עוברים AA לטקסט רגיל, בשני המצבים.
השורה החמישית היא המלכודת שחזרה שלוש פעמים בסבב הקודם, מדודה: אותו לבן,
אותו טקסט, 2.77 מול 5.17. כל משטח מלא במוקאפ משתמש ב-`--accent-solid`.

### התווית הארוכה

`איש פלור (אחמ״ש / מנהל אירועים)` — ב-375 היא נשברת לשתי שורות בשלוש
החלופות, **לא נחתכת ולא קוצרה**, והשורה גדלה ל-146px במקום 95. בדסקטופ
היא נכנסת בשורה אחת. מדדתי גם חיתוך טקסט בכל השורות: **אפס** ב-12
כרטיסים ואפס ב-20.

---

## עשרת המצבים — כולם, בשלוש החלופות

| מצב | איך הוא נראה |
|---|---|
| רגילה | ללא סימון |
| **דחוף** | פס מותג בקצה המוביל של השורה + המילה בענבר |
| **שיבוץ מיידי** | `.icon-bolt` ירוק לפני התפקיד (א/ג), תגית ירוקה (ב) |
| **18+** | ענבר. ראה הממצא למטה |
| **הסעות** | `.icon-van` + המילה |
| **כמה תקנים** | `3 תקנים · נותרו 2` בכחול המותג |
| **מאומת** | `✓` ירוק אחרי שם העסק |
| **לא מאומת** | הטקסט "עסק לא מאומת" — החריג מסומן, לא ברירת המחדל |
| **מיקום משוער** | הטקסט, **ובמקום תווית המרחק** ולא לצידה |
| **הוגשה מועמדות** | `✓ המועמדות נשלחה` ירוק |
| **בוטלה** | השורה מעומעמת, הפס מאבד את צבע המותג |

**אין אימוג׳י בשום מקום, והתו היחיד שנכנס הוא `✓`.** אין hex מקומי, אין
`left/right` פיזיים, ו-`tabular-nums` על כל מספר בטור — מאומת ב-DOM.

---

## שתי הכרעות שלך שאכפתי בקוד, ושווה שתדע איפה

1. **M20 — אין קישור ניווט בלי סיכה ובלי כתובת.** בגרסה הראשונה שלי
   כפתור הניווט הופיע בכל כרטיס, כולל עסק במצב "מיקום משוער" — כלומר
   ניווט למרכז העיר, הכשל שסגרתם. תוקן ונמדד: בעשרים כרטיסים יש 16
   קישורי ניווט, ארבעה כרטיסים במצב משוער, **ואפס מהם עם קישור**.
2. **D15–D16 — אין מפה מוטמעת, אין מסלול, אין זמן נסיעה.** מה שיש: תווית
   `≈7 ק״מ` במשקל קל וב-`--muted` עם ה-`≈` שנשאר, וכפתור ניווט שנשאר
   כפתור. ההסעות קיבלו נוכחות אמיתית ולא תגית בסוף התור.

---

## ממצא שאינו שלי ואינו במוקאפ: `18+` מתהפך ב-RTL

בתגית שהעתקתי מילה במילה מ-`app.html`, המחרוזת `18+` מתרנדרת **`+18`**.
הסיבה היא דו-כיווניות: הפלוס נייטרלי, ובהקשר RTL הוא נודד לצד השני.
ראיתי את זה בדפדפן לפני התיקון, במרקאפ זהה לשלכם. במוקאפ עטפתי ב-`<bdi
dir="ltr">`, וזה מתרנדר נכון בשני המצבים.

**מה שלא בדקתי:** מה בדיוק כתוב ב-`terms` תחת `ui.adults_only`. אם הערך
שם הוא `18+` — הפגם קיים היום במוצר בכל מקום שבו התגית מוצגת, ולא רק
בלוח. אם הערך הוא נוסח אחר, אין ממצא. זו בדיקה של שורה אחת אצלך.

---

## מחרוזות שידרשו קוד ב-`terms` אם תבחר חלופה

לא המצאתי טקסט בקוד. אלה המחרוזות שאין להן היום קוד, לפי מה שראיתי
ב-`app.html`:

- `עד` (כקידומת לשעת הסיום: "עד 23:00") — א ו-ג
- `סוף השבוע`, `בהמשך` — כותרות הדליים של ג
- `N משמרות` — מונה בכותרת הדלי של ג (צורת רבים, צריך `.one`/`.other`)
- `ניווט` — היום הכפתור נקרא `ui.waze`

`היום` ו-`מחר` קיימים, אבל **מקודדים קשיח ב-`whenLabel()` ולא ב-`terms`**.
אם ג נבחרת, הן עולות לכותרות ונעשות מבנה — שווה להעביר אותן ל-`terms`
באותה הזדמנות.

---

## מה לא בדקתי, ושלא ייכנס לדיווח כאילו כן

- **לא ראיתי את הלוח האמיתי מחובר.** חסימת ה-session עדיין שם, ואני לא
  מבקש סיסמה ולא מקליד אחת. כל מה שלמעלה נמדד על המוקאפ.
- **לא בדקתי מכשיר אמיתי**, רק אמולציה של 375 ושל 720 בדפדפן.
- **לא בדקתי מקלדת וקורא מסך.** השורות במוקאפ הן `div` לחיץ ולא `button`;
  בקוד האמיתי זה חייב להיות אלמנט מוקד עם תפקיד.
- **לא בדקתי מצב ריק ולא מצב טעינה.**

---

```
--- HANDOFF TO: Tech Lead ---
DONE:      M19. שלוש חלופות בקובץ MOCKUP-M19-BOARD.html (עותק גם ב-scratchpad),
           12 כרטיסים בכל אחת, בהיר וכהה, 375 ו-720, מתג צפיפות ל-20.
           אומת בדפדפן מול שרת מקומי: 12 צמדי ניגודיות בשני המצבים
           (מינימום 4.69), צפיפות (95/218/94px לשורה חציונית), אפס חיתוך
           טקסט ב-12 וב-20, התווית הארוכה שלמה בשתי שורות ב-375.
STATE:     app.html לא נגעתי בו — הוא בידי המפתח מאז M17. אין שינוי במסד,
           אין שינוי ב-assets. הקבצים החדשים בתיקייה: MOCKUP-M19-BOARD.html,
           DESIGN-NOTE-M19.md. שורת M19 בלוח הסבב עודכנה על ידי בלבד.
NEXT:      1. תבחר חלופה. 2. תקבע את חוזה שמות המחלקות בכתב, כפי שכתבת
           שתעשה מראש. 3. רק אז אני כותב CSS, וכבלוק BEGIN/END בקובץ נפרד.
FOR YOU:   שורה אחת ב-terms: מה הערך של ui.adults_only. אם הוא "18+",
           יש פגם RTL חי במוצר בכל מקום שהתגית מוצגת (פירוט בגוף המסמך).
MUST KNOW: המלצתי על ג ולא על שלוש. הסיכון היחיד שלה הוא שאין הגשה בלי
           פתיחה, וזה נוגע דווקא ל"שיבוץ מיידי". הצעת הבדיקה הזולה בגוף
           המסמך. אם המיון לא יהיה תמיד לפי זמן — ג נשברת ו-א היא הנכונה.
OPEN:      לא ראיתי את הלוח מחובר (חסימת session, לא עוקפים). לא נבדקו
           מקלדת, קורא מסך, מצב ריק ומצב טעינה. ארבע מחרוזות חדשות
           ממתינות לקודים ב-terms.
--- END HANDOFF ---
```
