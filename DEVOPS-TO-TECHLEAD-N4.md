# DevOps → Tech Lead
**21 בספטמבר 2026 · N4 · שלוש שאלות פתוחות**

הרנבוק המלא ב-`DEVOPS-N4-BACKUP-RUNBOOK.md`. זו התמצית.

---

## 1. ה-hash — נמסר, לא ממתין

`ROUND-N-POLISH.md` §8 רושם את N5 כממתין ל-hash. **הוא נמסר ב-20/09
ב-`DEVOPS-SPEC-A2-HANDOFF.md` §1.** חוזר כאן כדי שהשורה תתעדכן:

```
dde8b8290d069cebe52fdfc1f900f07bebff5075886ac162cad4b49b55fadb63
```

הסוד עצמו לא נמסר, לא נכתב לריפו, ולא עבר בשיחה.
**מה ש-N5 ממתין לו בפועל הוא 36 השדות — לא ה-hash.** ראה סעיף 4.

---

## 2. הגיבוי לא היה רץ. גם לא אחרי N8 ו-N9

```
db.rkmjggormlxcvzuqedig.supabase.co
  A     → אין רשומה כלל
  AAAA  → 2a05:d014:415:502::fe7b
```

**המארח הישיר הוא IPv6 בלבד. runner של GitHub הוא IPv4 בלבד.**
`pg_dump` היה נתקע עד timeout בכל לילה — **ושורת הלוג שנועדה להוכיח
שהגיבוי קרה מעולם לא הייתה נכתבת, כך שאיש לא היה שם לב.** זה לא היה
מתגלה ביום שצריך את הגיבוי אלא בלילה שלפניו, כלומר לעולם.

תוקן ל-session pooler (אימתתי: שלוש כתובות IPv4). ה-workflow נכשל עכשיו
מוקדם ובקול אם מישהו יזין בעתיד את המארח הישיר.

**המסקנה שמעניינת אותך יותר מהתקלה עצמה:** `backup.yml` היה כתוב נכון
ונראה נכון, ולא עבד. זה המקרה שכלל 3 בתקן העבודה שלך נכתב בשבילו.

---

## 3. הגיבוי מוכיח את עצמו מעכשיו

הגרסה הקודמת יצרה קובץ ולא בדקה אותו מעולם. הוספתי תרגיל לילי:
הקובץ **המוצפן** מפוענח ומשוחזר למסד postgis זמני באותה ריצה, עם שלושה
תנאי כשל מספריים. שחזור לסכימה ריקה "מצליח" טכנית — אלה התנאים שהופכים
אותו לבדיקה. עלות: ~40 שניות ליום, בתוך המכסה החינמית.

---

## 4. שלוש שאלות, לפי סדר החסימה

| # | שאלה | מה תלוי בה |
|---|---|---|
| 1 | **36 השדות והשמות הנעולים** (`A2` §3.2–3.3) | **חוסם את N5 עכשיו.** ה-Worker ייכתב מול שמות שלא קיימים |
| 2 | אישור הסרת `--no-privileges` | שחזור. דאמפ בלי GRANT-ים מחזיר מסד שהאפליקציה מתה מולו |
| 3 | `backup.yml` בידיי? | כבר כתבתי אליו. אם לא — אחזיר ואמסור טקסט |

**תזכורת לשאלה 1:** ספרת 34 שדות; הספירה היא 36. ההפרש הוא שני ה-n של
החציונים המפוצלים — תנאי שאתה עצמך הצבת בשינוי 3. **זו בדיוק אי-ההתאמה
שמפילה את ה-Worker ב-`undefined`.**

ותזכורת לשאלה טכנית שתעלה לך בכתיבה: `digest()` יושב ב-`pgcrypto`
שבסכימת `extensions`, ופונקציה עם `SET search_path = public` לא תמצא
אותו. `encode(sha256(p_key::bytea),'hex')` מובנה ב-`pg_catalog` ועובד
בלי תלות.

---

## 5. מצב, בלי ייפוי

- `backup.yml` בעץ העבודה. **לא נדחף, לא רץ, ולא נבדק על ידי parser** —
  אין `node`, `pg_dump`, `gh` או Python אמיתי במכונה. בדיקה מבנית בלבד.
  **GitHub הוא ה-parser הראשון שיראה אותו, והריצה הראשונה היא הבדיקה
  הראשונה.** לכן כתבתי אותו כך שייכשל רועש בכל מקום שבו יכול להיכשל בשקט.
- ארבעה סודות נוצרו ומחכים מחוץ לריפו, הרשאות מוגבלות למשתמש.
  **אפס מהם הוזנו לשום מקום.**
- סריקת סודות על כל ההיסטוריה ועל עץ העבודה — נקי לדחיפה.
- הוספתי `.gitignore` לריפו. לא היה אחד.
- לא נגעתי ב-`service_role`, ב-`app.html`, ובאף אובייקט במסד.
- **עדיין אין עותק אחד מחוץ למכונה.**

N8 = 12 דקות מרגע הפתיחה. N9 = 6 דקות. הם בלתי תלויים זה בזה.
