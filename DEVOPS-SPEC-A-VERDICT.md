# הכרעה על DEVOPS-SPEC-A
**מאת: Tech Lead · 20 בספטמבר 2026**

**מאושר לבנייה, עם ארבעה שינויים.** §2 (Worker), §3 (HMAC), §4 (עלות)
ו-§5 (`tables_known`) מאושרים כפי שהם. שדות 26–27 הם התוספת הטובה במפרט —
עמוד שמזהה את התיישנותו בלי מתחזק.

---

## שינוי 1 — חור אבטחה במפרט: ה-RPC פתוח לאינטרנט

ביקשת `GRANT EXECUTE ON status_snapshot() TO anon`. **זה מבטל את §3.**

מפתח ה-`anon` יושב בגלוי בתוך `app.html`, שמוגש לכל אדם. כל מי שמעתיק
אותו יכול לקרוא ל-`status_snapshot()` ישירות מול ה-REST ולקבל את כל נתוני
הנפח — בלי לעבור דרך ה-Worker ובלי לדעת את הסיסמה. **הסיסמה תגן על הדף
ולא על הנתונים**, וזו בדיוק הצגה שסגרתי כבר פעם אחת בסבב הזה.

זה לא פגם בחשיבה שלך — הנחת שהמפתח לא מגיע לדפדפן, וזה נכון **לעמוד שלך**.
הוא כבר שם, מצד המוצר.

**הפתרון, ואתה לא מוסר לי סוד:**

```
status_snapshot(p_key text)  --  SECURITY DEFINER, GRANT TO anon
```
בתוכה: השוואה בזמן קבוע של `digest(p_key,'sha256')` מול hash יחיד
בטבלה `private.status_key`. לא תואם → `raise exception 'forbidden'`.

**אתה מייצר את המפתח, שם אותו כ-Worker Secret, ומעביר לי את ה-hash בלבד.**
אני לא רואה את הסוד ולא מחזיק אותו. אותה סיסמה משותפת של §3 לא מתאימה
כאן — **תייצר שני סודות נפרדים**, אחרת דליפת אחד היא דליפת שניהם.

---

## שינוי 2 — דלי הזמן: `starts_at`, ולא `created_at`

**אני דוחה את ההצעה, והנימוק שלך על "אותה קבוצה" נשמר.**

הבעיה ב-`created_at` גדולה ממה שתיארת. היא לא רק שמשמרת שאוישה יום אחרי
נספרת כשלא־אוישה; היא שהחלון כולל משמרות **שטרם התחילו ועדיין פתוחות
בצדק**. אלה לא "לא אוישו" — אלה **לא הוכרעו**. מעסיק שמפרסם שבועיים
מראש יוריד לנו את היחס בלי ששום דבר בביצועים השתנה.

**הכלל:** דלי לפי `starts_at`, **וסופרים רק משמרות ש-`starts_at <= now()`** —
כלומר כאלה שהתוצאה שלהן ידועה. אותה קבוצה במונה ובמכנה, כדרישתך.

**ובנוסף, שני שדות חדשים:** `shifts_cancelled_7d` / `_30d`.
משמרת שבוטלה היא תוצאה שלישית, לא "לא אוישה". לקפל אותה לתוך הכישלון
מסתיר את ההבדל בין *לא מצאנו עובד* לבין *המסעדה חזרה בה* — ואלה שתי
בעיות שונות לחלוטין.

---

## שינוי 3 — פיצול החציון: מיידי מול דו-צדדי

**כן, שני הכיוונים נספרים.** השעון הוא מפרסום עד `confirmed_at`, ולא
משנה מי יזם. אבל **לא בשדה אחד**, ויש לזה נימוק במספרים שמדדתי עכשיו:

**2 מתוך 3 המשמרות הן `instant`, ושתי המועמדויות המאושרות — שתיהן על
משמרות `instant`.**

משמרת `instant` קופצת ישירות ל-`confirmed` ברגע התפיסה, כי המעסיק הסכים
מראש. כלומר **הערך של 160.96 דקות שמדדת אינו מכיל אישור מעסיק כלל** —
הוא "כמה זמן עד שעובד הרים". זה מדד אמיתי, אבל הוא לא מה שהכותרת אומרת.

**פצל:** `median_minutes_instant` ו-`median_minutes_two_sided`, כל אחד עם
ה-n שלו. הכותרת הגדולה בעמוד היא המשוקלל, ושני הפיצולים מתחתיו.

הסיבה המעשית: כששניהם יהיו על נפח אמיתי, הפער ביניהם הוא **הזמן שלוקח
למעסיק לענות** — וזה המספר שאומר לנו אם הצוואר הוא בצד ההיצע או בצד הביקוש.

---

## שינוי 4 — שם השדה: `workers_can_claim`, לא `workers_profile_complete`

יש שלוש הגדרות שונות ל"פרופיל מלא" במערכת: הטופס, `app.html:827`,
ומה שהשרת באמת אוכף. אימתתי ב-`claim_shift`: הוא דורש `birth_date`
(ו-18+), ו-`home_city_id` רק כשלמשמרת יש `max_distance_km`.
**`given_name` ו-`family_name` — אף מסלול שרת לא קורא אותם.**

`family_name` **אינו נדרש**, כפי ששיערת.

**ההגדרה שננעלת:** `birth_date is not null AND home_city_id is not null`.
בלי עיר ה-`shifts_for_me` לא מחזיר כלום, אז עובד בלי עיר לא משתתף בפועל
גם אם טכנית יכול להגיש.

**ושם השדה משתנה** כי "מלא" מעורפל, ו"יכול להגיש" הוא מה שנמדד.
הכלל של `app.html` מחמיר יותר בשם פרטי; ההפרש הוא לכל היותר משתמשים עם
עיר ותאריך לידה ובלי שם — היום אפס, וזה יישאר נכון כל עוד המסך דורש שם.

**המספר עצמו ראוי לתשומת לב של המייסד: 4 מתוך 10.** שישה משתמשים רשומים
אינם יכולים להשתתף בכלל. זה ממצא מוצר, לא הגדרת מדד.

---

## שני דברים שכבר טיפלתי בהם

**`my_shifts()` פתוח ל-`anon` — צדקת, וזו רגרסיה שלי.** הפונקציה לא נולדה
כך: הפלתי ויצרתי אותה מחדש פעמיים בסבב הזה כדי לשנות טיפוס החזרה, וכל
`CREATE` החיל מחדש את default privileges של Supabase שמעניקות `anon`.
ה-`revoke ... from public` שלי מסיר את PUBLIC ולא נוגע במענק מפורש ל-`anon`.

מיגרציה `restore_privilege_floor_on_reader_functions` הוחלה. סרקתי את כל
הפונקציות ב-`public`: אותו דפוס היה גם ב-`shifts_for_me` וב-`shifts_near_city`.
**אומת אחרי התיקון: אפס פונקציות שה-`anon` יכול להריץ.**

**הגיבוי — מאושר כסבב נפרד, והוא קודם לעמוד הסטטוס.** התיקון הקטן בתמונה
שלך: git הותקן היום ויש קומיט מקומי `955f621` (29 קבצים, 3.0MB), כך
שקיימת היסטוריה מקומית. **מחוץ למכונה עדיין אין כלום** — וזו הנקודה שלך,
והיא עומדת. שני החסמים הם פעולות מייסד: אישור דחיפה יוצאת, והפעלת R2.

---

## הערה על ראש המיגרציות
זז מאז שכתבת: `restore_privilege_floor_on_reader_functions`. לא טעות
שלך — תזמון. תקרא אותו מחדש לפני שאתה בונה את `rpc_version`.

---

## מה עכשיו
1. תחזיר לי את ה-hash של מפתח ה-RPC (שינוי 1).
2. אני כותב את `status_snapshot(p_key)` עם 34 השדות: 30 שלך, פחות
   `workers_profile_complete` שמוחלף, ועוד `shifts_cancelled_7d`/`_30d`,
   `median_minutes_instant`, `median_minutes_two_sided` ושני ה-n שלהם.
3. אתה בונה. תנאי הגמר של שלב ב בבריף ללא שינוי.
