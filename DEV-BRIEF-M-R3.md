# DEV-BRIEF — סבב M, סיבוב 3: שתי שורות שנשארו
**מאת: Tech Lead · 20 בספטמבר 2026 · חוסם את QA**
**נוגע למפתח (§2–§4) ולמעצב (§5)**

> **גרסה שנייה של הקובץ הזה.** הראשונה הורתה לשנות את סדר הטעינה
> (`insertBefore` בשורה 994). **ההוראה ההיא בוטלה** — מדדתי את הקוד שלך
> כפי שהוא ב-00:35 וגיליתי שהפתרון שלך כבר עובד בלעדיה. הפירוט ב-§1.

---

## 1. מה קרה כאן, ומה מדדתי

שלושתנו רדפנו אחרי אותו באג בלי לדעת.

**המעצב** בנה רתמה ששחזרה את `loadLeaflet()` מילה במילה, וגילה שהפופאפ
לבן במצב כהה ולא רק ה-tooltip. הוא אמר לי במפורש לא לסמוך על המספרים
שלו ולמדוד בעצמי.

**אני** מדדתי, ואישרתי שהבעיה רחבה עוד יותר: במצב כהה כל עשר הדריסות
שלנו ל-Leaflet היו מתות — פופאפ, tip, תווית, חץ, כפתורי זום, ורקע המיכל
ב-`rgb(221,221,221)`. **וגם גיליתי טעות שלי**: בסיבוב 2 דיווחתי שהפופאפ
תקין, אבל הרתמה שלי טענה את Leaflet דרך `<link>` ב-head — הסדר הרצוי,
לא הסדר האמיתי. מדידה נכונה של תצורה שגויה.

**אתה** מצאת ותיקנת את אותו דבר בעצמך בזמן שכתבתי, דרך סגוליות: תחילית
`.map` לכל כלל `leaflet-*`, ו-`.map.leaflet-container` היכן של-Leaflet
עצמה יש שני מחלקות. **הכיוון שלך נכון ואני מאמץ אותו.** `insertBefore`
מיותר, והערת הקוד שלך ("אל תוריד את התחילית הזו מאף כלל leaflet-*") היא
המשמעת הנכונה. שני מנגנונים מקבילים היו גרועים מאחד.

### המדידה על הקוד שלך כפי שהוא, מצב כהה

| מה | אצלך עכשיו | פסק |
|---|---|---|
| `.leaflet-popup-content-wrapper` | `rgb(17,26,45)` | ✓ |
| `.leaflet-popup-tip` | `rgb(17,26,45)` | ✓ |
| `.leaflet-tooltip` רקע | `rgb(17,26,45)` | ✓ |
| `.leaflet-tooltip` טקסט | `rgb(233,238,251)` | ✓ |
| `.leaflet-container` רקע | `rgb(26,36,56)` | ✓ |
| כפתורי זום | `rgb(17,26,45)` | ✓ |
| attribution טקסט | `rgb(147,164,192)` | ✓ |
| **attribution רקע** | **`rgba(255,255,255,.8)`** | **✗** |
| **חץ התווית** | **`rgb(39,53,79)`** ← `--border` | **✗** |

**שמונה מתוך עשר עברו.** שתיים נשארו, ואף אחת מהן אינה כשל בגישה שלך.

---

## 2. השורה הראשונה שנשארה: רקע ה-attribution

```css
.map .leaflet-control-attribution{...}
```
זה `(0,2,0)`. הכלל של Leaflet הוא `.leaflet-container .leaflet-control-attribution`
— **גם הוא `(0,2,0)`**. תיקו, וגיליון Leaflet נטען אחרי שלנו, אז הוא מנצח.

`.map` ו-`.leaflet-container` הם **אותו אלמנט**, ולכן תחילית עם רווח לא
מוסיפה מחלקה מעבר למה של-Leaflet כבר יש. צריך לחבר בלי רווח:

```css
/* Leaflet: .leaflet-container .leaflet-control-attribution — גם (0,2,0).
   רווח כאן נותן תיקו, וגיליון Leaflet נטען אחרינו ומנצח. חיבור בלי
   רווח הופך את זה ל-(0,3,0). נמדד: עם רווח rgba(255,255,255,.8),
   בלי רווח כהה. */
.map.leaflet-container .leaflet-control-attribution{
  background:color-mix(in srgb,var(--surface) 84%,transparent);
  color:var(--muted);font-size:10.5px}
.map.leaflet-container .leaflet-control-attribution a{color:var(--muted)}
```

זה מחליף את שתי השורות הקיימות (547–549). **הטקסט כבר עובד** — הוא
חד-מחלקתי אצל Leaflet — אבל עדיף ששתי התכונות יישבו באותו כלל ולא יתחלקו
בין שתי רמות סגוליות.

**בדוק את אותו דפוס בשאר הבלוק.** כל מקום שבו ל-Leaflet יש
`.leaflet-container X` ואצלך `.map X` הוא אותו תיקו. ב-`close-button`
כבר עשית את זה נכון.

---

## 3. השורה השנייה: חץ התווית — שלוש המלצות של המעצב, כולן מאושרות

**3.1 צבע החץ.** שורה 523:
```css
.map .leaflet-tooltip-bottom::before{border-bottom-color:var(--border)}
```
←
```css
/* החץ הוא משולש מלא, לא מסגרת: הוא חייב את צבע המילוי של הבועה, אחרת
   הוא נקרא כמשולש כהה מנותק. ארבעת הכיוונים, כי Leaflet בוחרת כיוון
   לפי מקום פנוי ולא לפי מה שביקשנו. */
.map .leaflet-tooltip-top::before{border-top-color:var(--surface)}
.map .leaflet-tooltip-bottom::before{border-bottom-color:var(--surface)}
.map .leaflet-tooltip-left::before{border-left-color:var(--surface)}
.map .leaflet-tooltip-right::before{border-right-color:var(--surface)}
```

**3.2 גבול הבועה** — `--border` ← `--border-strong` בשלושה מקומות:
`.leaflet-tooltip` (521), `.leaflet-popup-content-wrapper` (526),
`.leaflet-popup-tip` (528). מעל אריחים `--border` נבלע; הבועה צריכה קצה
שמפריד אותה מהמפה, לא קצה שמפריד אותה מכרטיס.

**3.3 עוצמת "העיר שלכם"** — היום `--text`, כלומר אותה עוצמה כמו תוכן
ראשי. מרכז העיר הוא נקודת ייחוס, לא יעד, ואסור לו להתחרות בסמני המשמרות.
המעצב מדד AA בשני המצבים (5.4:1 / 6.87:1). **מאושר.**

```css
.map.leaflet-container .leaflet-tooltip.tip-muted{color:var(--muted)}
```
ובקריאת `bindTooltip` של סמן העיר:
```js
.bindTooltip(ui('ui.map_my_city'),
             { permanent:true, direction:'bottom', offset:[0,8], className:'tip-muted' });
```

---

## 4. הכלל שנשאר אחרי הסבב

**התחילית שלך היא החוק.** כל כלל `leaflet-*` חדש מקבל `.map`, ואם
ל-Leaflet יש שם `.leaflet-container X` — מחברים בלי רווח.
**בדיקה של דריסת Leaflet נעשית מול הטוען הדינמי, לא מול `<link>` ב-head.**
זה הכלל שעלה לי טעות בסיבוב 2, והוא נכנס לתיק החפיפה.

`white-space:nowrap` על התווית נכון לתווית קצרה. תווית ארוכה עתידית תגלוש
מהמפה ותצטרך `max-width` ו-wrapping. מתועד, לא לפעולה עכשיו.

**פעולה אחת קטנה שנשארה (D12), מהמעצב:** ארבעת כללי החץ משתמשים ב-`.map `
ואילו `tip-muted` ב-`.map.leaflet-container`. **שתיהן נכונות** — כל אחת
מנצחת את היריב הספציפי שלה. אבל מי שיעתיק את התבנית הלא-נכונה לכלל הבא
יקבל תיקו ויפסיד בסדר, בשקט. **הוסף הערה אחת בקוד מעל הבלוק** שאומרת:
רווח כשל-Leaflet יש `.leaflet-X` בלבד; בלי רווח כשיש לה `.leaflet-container .leaflet-X`.
זו הערה, לא שינוי התנהגות — היא נועדה לאדם הבא, ואין לה מה לחכות לסבב.

---

## 5. למעצב

**הממצא שלך היה חוסם וצדקת בו לחלוטין**, כולל ההבחנה המדויקת שסדר לא
מנצח סלקטיביות ושה-attribution יישאר לבן גם אחרי תיקון הסדר. **זה בדיוק
מה שקרה** — המפתח תיקן דרך סגוליות, וה-attribution שרד כלבן בדיוק כפי
שחזית, מסיבה מעט אחרת (תיקו ולא הפסד).

שלוש ההמלצות שלך מאושרות: `--surface` לחץ, ארבעת הכיוונים,
`--border-strong` לבועה. גם `tip-muted` מ-OPEN 1 מאושר.

**התצפית המתודולוגית שלך שווה יותר מהתיקון**, והיא נכנסת ככלל קבוע ב-§4:
רתמה משחזרת את הטוען הדינמי, לא `<link>` סטטי. הרתמה שלי נבנתה בסדר
הרצוי ולכן נתנה לפופאפ ציון עובר שגוי; שלך נבנתה בסדר האמיתי ותפסה.

**מה שנשאר לך:** אחרי שהמפתח מדביק — הבועה, החץ בארבעת הכיוונים והתווית
המוחלשת, בעיניים, בשני המצבים. טקסט בלבד; הקובץ בידי המפתח.

---

## 6. סדר

1. §2 ו-§3. זה קטן: שני כללי attribution, ארבע שורות חץ, שלוש החלפות
   `--border-strong`, כלל `tip-muted` אחד ו-`className` אחד ב-JS.
2. מדוד בעצמך את תשע השורות מהטבלה ב-§1, כהה ובהיר, והחזר מספרים.
   **אל תסתמך על שלי** — הרתמה שלי אינה `app.html`.
3. המייסד נכנס → 12 סעיפי §6.
4. QA.

**QA לא מתחיל לפני ששתי השורות ב-✗ הופכות ל-✓ במדידה שלך.**
