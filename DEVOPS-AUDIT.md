# DEVOPS-AUDIT — כל פעולה שביצעתי, לבדיקה חוזרת
**מאת: DevOps · 21 בספטמבר 2026, 16:54 · מענה ל-`START-HERE.md` §DevOps**

**כל שורה כאן ניתנת להרצה חוזרת.** הפקודה או השאילתה מופיעה כדי שתוכל
לבדוק אותי ולא להאמין לי. שלוש המשימות מ-`START-HERE` בראש, ואחריהן
הרישום המלא.

---

## א. שלוש המשימות שהטלת עליי ב-START-HERE

### 1. אימות `backup.yml` — הקובץ הנכון על הענף הנכון ✅ · ריצה ❌ טרם קרתה

| נבדק | פקודה | תוצאה |
|---|---|---|
| הקובץ שעל `origin/main` | `git rev-parse origin/main:.github/workflows/backup.yml` | blob `4e0ce250`, **8,796 בתים** |
| זהה לגרסה שכתבתי | `git hash-object .github/workflows/backup.yml` | `4e0ce250` — **אותו blob בדיוק** |
| ענף ברירת המחדל | `git remote show origin` | `HEAD branch: main` |

**מסקנה: הקביעה שלך ב-START-HERE נכונה.** הגרסה שלנו יושבת על ענף
ברירת המחדל, ולכן התזמון אכן ירוץ ממנה.

**אבל ריצה עדיין לא קרתה, ואי אפשר לומר שהגיבוי נבדק.**
ה-cron הוא `0 1 * * *`, כלומר **01:00 UTC = 04:00 בישראל**. השעה עכשיו
13:54 UTC. **הריצה הראשונה היא הלילה.**

### ⚠️ הדבר הדחוף היחיד במסמך הזה

**אם `SUPABASE_DB_URL` ו-`BACKUP_PASSPHRASE` לא הוזנו כסודות בריפו עד
04:00, ריצת הלילה תיכשל ולא יהיה גיבוי.** היא תיכשל נקי ועם הודעה
מפורשת — כך תכננתי אותה — אבל התוצאה זהה: **עוד לילה בלי עותק מחוץ
למכונה.** הסיסמה מוכנה ומחכה; ההזנה היא דקה אחת.

**מה שאני לא יכול לבדוק מכאן:** הריפו פרטי, `gh` לא מותקן, ואין לי
טוקן — `api.github.com/repos/mak-bag/mishmeret` מחזיר **404**. לכן איני
יכול לראות אם הסודות הוזנו, אם יש ריצות, או אם GitHub קיבל את ה-YAML.
**אם תרצה, אפתח את עמוד ה-Actions דרך הדפדפן המחובר של המייסד ואקרא
משם** — בקשה שלי, לא פעולה שאבצע לבד.

### 2. ה-hash — נרשם ✅
`dde8b8290d069cebe52fdfc1f900f07bebff5075886ac162cad4b49b55fadb63`.
זו הייתה אי-הבנה ולא טעות של אף אחד; נסגר.

### 3. 36 השדות — נשלחו זה מכבר ⏳ ממתינים לך
הטבלה המלאה והשמות הנעולים ב-**`DEVOPS-SPEC-A2-HANDOFF.md` §3.2–3.3**,
מ-20/09. אני חוזר על הנקודה החשובה בלבד: **ספרת 34, הספירה היא 36.**
ההפרש הוא `median_n_instant` ו-`median_n_two_sided` — שני ה-n של
החציונים המפוצלים, תנאי שאתה עצמך הצבת בשינוי 3.

---

## ב. תיקון שאני מקדים לדיווח: כמעט דיווחתי ממצא שקרי

הרצתי בדיקה שהחזירה **"`backup.yml` לא קיים על אף ענף מרוחק"** — ממצא
שהיה סותר את START-HERE חזיתית.

**הוא היה שגוי.** git bash על Windows (MSYS) המיר את המחרוזת
`origin/main:.github/...` ל-`origin\main;.github\...` — המרת נתיבים
אוטומטית ששברה את התחביר `ref:path`. `HEAD:...` עבד כי אין בו לוכסן,
וזה מה שיצר את הסתירה הפנימית שתפסה אותי.

חזרתי עם `MSYS_NO_PATHCONV=1` וקיבלתי את התמונה האמיתית שבסעיף א.1.

**רושם את זה כי זה לא רק שלי:** כל בדיקת `git` בתחביר `ref:path`
במכונה הזאת תיתן תשובה שקרית בלי המשתנה הזה, ותשובה שקרית שנראית
סבירה היא בדיוק מה שהתקן שלך נכתב נגדו.

---

## ג. ממצא קטן במצב הריפו

```
git rev-parse --abbrev-ref main@{upstream}   →   origin/round-n-2026-09-21
git remote show origin                       →   main pushes to main
```

**`main` דוחף ל-`origin/main` אבל מושך מ-`origin/round-n-2026-09-21`.**
`git pull` על `main` ימזג ענף אחר בלי להתריע, ו-`git status` מדווח
"ahead 6" מול היעד הלא נכון — מה שמסביר את הבלבול.

**הגיבוי לא נפגע:** שלב `Record run` שלי כותב `git pull --rebase origin
main` במפורש ואינו נשען על ה-upstream. התיקון הוא שורה אחת, **והקובץ
אינו שלי** — לכן אני מדווח ולא נוגע:

```bash
git branch --set-upstream-to=origin/main main
```

---

## ד. רישום מלא — מה הורץ, ומה זה הוכיח

### אימות תשתית (שלב א, 20/09)

| # | פעולה | איך | תוצאה |
|---|---|---|---|
| 1 | פרויקט Supabase | `list_projects` | `ACTIVE_HEALTHY`, PG 17.6, eu-central-1 |
| 2 | `anon` מול טבלאות הליבה | curl ל-REST עם מפתח publishable | `shifts` → `[]`, `businesses` → `[]` |
| 3 | מדיניות RLS | `select … from pg_policies` | רק `cities`, `city_names`, `terms` פתוחות ל-`anon` |
| 4 | Cloudflare | `workers_list`, `kv_namespaces_list` | חשבון נגיש, **0 Workers, 0 KV** |
| 5 | **R2** | `r2_buckets_list` | **`403 — Please enable R2`** |
| 6 | כלי פריסה | `Get-Command node/npm/wrangler` | **שלושתם לא מותקנים** |
| 7 | האגרגטים של העמוד | SQL על הנתונים האמיתיים | 2 עסקים · 10 פרופילים · 3 משמרות · חציון 160.96 דק׳ |

### אימות ההכרעה שלך (21/09)

| # | קביעה שלך | איך בדקתי | תוצאה |
|---|---|---|---|
| 8 | אפס פונקציות פתוחות ל-`anon` | `has_function_privilege('anon', …)` על 20 הפונקציות | **0/20** ✅ |
| 9 | ולא רק במענקים | curl חיצוני כ-`anon` | `my_shifts` → **401**, `shifts_for_me` → **401** ✅ |
| 10 | ראש מיגרציות זז | `list_migrations` | `20260920023031` ✅ |
| 11 | **שינוי 3 — פיצול החציון** | `group by rollup (instant, initiated_by)` | שתי המועמדויות `instant`+`worker`. **אפס אישורי מעסיק** ✅ |
| 12 | המפתח בדפדפן (שינוי 1) | `grep` על `app.html` | `app.html:700` מחזיק `sb_publishable_…` ✅ |

### סבב הגיבוי (21/09)

| # | פעולה | איך | תוצאה |
|---|---|---|---|
| 13 | **DNS למארח המסד** | `nslookup -type=A/AAAA` | **אין A כלל.** AAAA בלבד — IPv6 בלבד |
| 14 | DNS ל-pooler | `nslookup` | שלוש כתובות IPv4 ✅ |
| 15 | גודל המסד לפי סכימה | `pg_total_relation_size` | `public` 1.4MB · `auth` 2.7MB · סה״כ ~4.8MB |
| 16 | **סריקת סודות לפני דחיפה** | `git grep` על כל ההיסטוריה + `grep -rIE` על העץ | אין JWT · אין `service_role` · אין `sb_secret` — **נקי** |
| 17 | תקינות ה-YAML | בדיקה מבנית בלבד | 5 מפתחות ראשיים, 8 שלבים, 5 סודות, 0 טאבים |

### מה שינוי בפועל

| קובץ | שינוי | מצב |
|---|---|---|
| `.github/workflows/backup.yml` | **נכתב מחדש**, 8.8KB | על `origin/main`, **טרם רץ** |
| `.gitignore` | **נוצר** — לא היה קיים | על `origin/main` |
| `DEVOPS-SPEC-A.md` | מפרט שלב א | לעיונך |
| `DEVOPS-SPEC-A2-HANDOFF.md` | ה-hash + 36 השדות | **ממתין לך** |
| `DEVOPS-N4-BACKUP-RUNBOOK.md` | רנבוק N8/N9 | לעיונך |
| `DEVOPS-TO-TECHLEAD-N4.md` | תמצית | לעיונך |

**ארבעה סודות נוצרו** (`openssl rand`, מחוץ לריפו, `icacls` מגביל
למשתמש). **אפס מהם הוזנו לשום מקום.** נמסרו החוצה: hash אחד וטביעת
אצבע אחת. **שום ערך סוד לא נכתב לריפו ולא עבר בשיחה.**

---

## ה. מה לא נגעתי בו

`service_role` · `app.html` · כל אובייקט במסד — סכימה, מיגרציה, RLS,
RPC, grant. `START-HERE.md`, `ROUND-*.md` — לא שלי, לא ערכתי.

---

## ו. מה לא נבדק, ואני לא מתחזה שכן

1. **`backup.yml` לא רץ מעולם.** אין `node`, `pg_dump`, `gh` או Python
   אמיתי במכונה — גם סימולציה מקומית אינה אפשרית. **GitHub הוא ה-parser
   הראשון שיראה את הקובץ, וריצת הלילה היא הבדיקה הראשונה.**
2. **הסרת `--no-privileges` לא אומתה.** תרגיל השחזור נועד להכריע אותה,
   והוא עצמו לא רץ. **עדיין ממתין לאישורך.**
3. **לא ידוע אם סודות הריפו הוזנו.** ראה האזהרה בסעיף א.1.
4. **עלות הגיבוי מחושבת ולא מדודה.** אמדוד בריצה הראשונה.

---

## בלוק חפיפה

**DONE** — אומת שה-blob של `backup.yml` על `origin/main` זהה לגרסה
שכתבתי (`4e0ce250`, 8,796 בתים) ושענף ברירת המחדל הוא `main` — כלומר
הקביעה שלך עומדת. נמצא ותוקן ממצא שקרי שנבע מהמרת נתיבים של MSYS.
נמצא שה-upstream של `main` מצביע לענף הלא נכון. 17 בדיקות מתועדות
לעיל, כל אחת עם הפקודה שמחזירה אותה.

**STATE** — הגיבוי חי על הנייר וטרם רץ. אין עדיין עותק אחד מחוץ למכונה.

**NEXT** — לקרוא את תוצאת ריצת 04:00 ולדווח מספרים: תוצאת תרגיל
השחזור, גודל הארטיפקט, ודקות שנצרכו.

**FOR YOU** — (1) **36 השדות.** (2) אישור הסרת `--no-privileges`.
(3) האם לפתוח את עמוד ה-Actions דרך דפדפן המייסד כדי לאמת סודות וריצות.

**MUST KNOW** — **סודות הריפו עד 04:00, אחרת אין גיבוי גם הלילה.**
זו הפעולה היחידה כאן שיש לה שעון.

**OPEN** — R2 (N9) עדיין סגור · `private.status_key` טרם נוצר ·
`main` מושך מענף שגוי (סעיף ג).
