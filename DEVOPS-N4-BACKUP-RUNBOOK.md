# DEVOPS-N4 — סבב הגיבוי: רנבוק פתיחה
**מאת: DevOps · 21 בספטמבר 2026 · מענה ל-`ROUND-N-POLISH.md` §8**

שני החסמים הם פעולות מייסד. **הכול מוכן מראש, כך שכל אחד מהם נפתח בדקות.**
`backup.yml` נכתב מחדש ומחכה בעץ העבודה. הסודות נוצרו ומחכים להדבקה.

**ה-hash של מפתח ה-RPC נמסר אתמול ב-`DEVOPS-SPEC-A2-HANDOFF.md` §1.** הוא
חוזר כאן כדי ש-N5 לא ימתין עליו בטעות — הסוד עצמו לא נמסר ולא יימסר:

```
dde8b8290d069cebe52fdfc1f900f07bebff5075886ac162cad4b49b55fadb63
```

---

## 1. הממצא ששינה את הסבב: הגיבוי לא היה רץ גם אחרי N8 ו-N9

בדקתי DNS למארח שב-`SUPABASE_DB_URL`:

```
db.rkmjggormlxcvzuqedig.supabase.co
  A     → אין רשומה כלל
  AAAA  → 2a05:d014:415:502::fe7b
```

**המארח הישיר הוא IPv6 בלבד. runner של GitHub Actions הוא IPv4 בלבד.**
`pg_dump` היה נתקע עד timeout, בכל לילה, בשקט.

כלומר: גם ביום שהמייסד היה מאשר דחיפה ומפעיל R2, לא היה מתקבל גיבוי אחד —
והלוג שנועד להוכיח שהגיבוי קרה מעולם לא היה נכתב, כך שאיש לא היה שם לב.

**התיקון:** חיבור דרך ה-session pooler, שאימתתי שיש לו IPv4:
`aws-0-eu-central-1.pooler.supabase.com → 18.198.145.223, 18.198.30.239, 52.59.152.35`.
ה-URI המדויק נלקח מ-`Dashboard > Connect > Session pooler` (פורט 5432,
משתמש בצורה `postgres.rkmjggormlxcvzuqedig`). **לא transaction pooler —
פורט 6543 אינו תומך ב-`pg_dump`.**

ה-workflow החדש **נכשל מוקדם עם הודעה מפורשת** אם מישהו יזין בעתיד את
המארח הישיר, במקום להיתקע.

---

## 2. השינוי השני: הגיבוי מוכיח את עצמו

`backup.yml` הקודם יצר קובץ ולא בדק אותו מעולם. **קובץ שלא שוחזר אינו
גיבוי.** הוספתי תרגיל שחזור שרץ בכל לילה, באותה ריצה, בחינם:

1. דאמפ → הצפנה → מחיקת המקור.
2. **פענוח הקובץ המוצפן** ושחזורו למסד `postgis/postgis:17-3.4` זמני
   שרץ כ-service container. הבדיקה רצה על הארטיפקט עצמו, ולכן היא
   מוכיחה גם את הדאמפ **וגם שהסיסמה פותחת אותו**.
3. תנאי כשל מפורשים — היסטוריית מיגרציות קיימת, ≥12 טבלאות ב-`public`,
   ≥6 טבלאות לא ריקות. שחזור שמחזיר סכימה ריקה "מצליח" טכנית; אלה
   התנאים שהופכים את התרגיל לבדיקה במקום לטקס.
4. שורת הלוג היומית נושאת את תוצאת התרגיל, לא רק את גודל הקובץ.

**שינוי נוסף: הסרתי `--no-privileges`.** כל מודל האבטחה כאן הוא GRANT-ים,
ודאמפ בלעדיהם משחזר מסד שהאפליקציה לא עובדת מולו. התרגיל יוצר מראש את
שבעת תפקידי Supabase כדי שהשחזור ייבדק באמת. **אם זה ייכשל בריצה
הראשונה — נדע מיד, וזו בדיוק מטרת התרגיל.**

---

## 3. N8 נפתח — מה אני עושה, לפי הסדר

**זמן משוער: 12 דקות עד ריצה ירוקה ראשונה.**

**0. בדיקה שכבר ביצעתי — סריקת סודות לפני דחיפה ראשונה.** דחיפה היא חד-כיוונית;
מה שנכנס להיסטוריה לא יוצא ממנה. סרקתי את כל ההיסטוריה ואת עץ העבודה:
אין JWT, אין `service_role`, אין `sb_secret`. שני האזכורים של `service_role`
הם טקסט בתוך בריפים. **`app.html:700` מכיל את `sb_publishable_…`, וזה תקין —
מפתח publishable נועד לשבת בדפדפן.** (וזו גם ההוכחה לשינוי 1 בהכרעה שלך:
הוא באמת יושב שם בגלוי.) **נקי לדחיפה.**

**1. הריפו.** `mak-bag/mishmeret` חייב להתקיים ולהיות **פרטי**. `gh` לא מותקן
במכונה, ולכן ההתחברות היא דרך Git Credential Manager — הדחיפה הראשונה תפתח
דפדפן פעם אחת. זו "ההתחברות האחת" שב-N8.

```bash
git remote add origin https://github.com/mak-bag/mishmeret.git
git push -u origin main
```

**2. שני סודות בריפו** — `Settings > Secrets and variables > Actions`:

| שם | ערך | מאיפה |
|---|---|---|
| `SUPABASE_DB_URL` | ה-URI של ה-**session pooler** | Supabase `Dashboard > Connect` |
| `BACKUP_PASSPHRASE` | שמונה מילים | `C:\Users\vladi\.mishmeret\backup-secrets.txt` |

`BACKUP_PASSPHRASE` נוצר ומחכה. טביעת אצבע sha256: `557f73acad0f`.
**אזהרה שכתובה גם בקובץ: אם היא אובדת, כל הגיבויים אבודים לתמיד.**
היא חייבת לשבת במנהל סיסמאות ולא רק על המכונה הזאת — מכונה שנשרפת לוקחת
איתה גם את המפתח וגם את הסיבה להשתמש בו.

**3. הרצה מיידית** — `Actions > nightly-backup > Run workflow`. לא מחכים
ל-01:00. אם משהו שגוי, נדע תוך דקות ולא מחר בבוקר.

**4. מה אני מאמת אחרי הריצה** — ולא "הריצה ירוקה":
- תרגיל השחזור עבר עם שלוש ההשוואות המספריות, ואני קורא את המספרים בלוג.
- הארטיפקט קיים, גודלו > 0, ושמו נושא את התאריך הנכון.
- `backups/log.txt` נדחף עם שורה שכוללת `r2=skipped` ואת תוצאת התרגיל.
- זמן הריצה ומספר הדקות שנצרכו, מול המכסה.

---

## 4. N9 נפתח — מה אני עושה, לפי הסדר

**זמן משוער: 6 דקות.** שלושה מהשלבים הם שלי ואחד שלך.

**1. יצירת הדלי — מכאן, בשניות.** יש לי כלי `r2_bucket_create` ב-MCP
והוא נכשל היום רק כי R2 מושבת. שם: `mishmeret-backups`, מיקום `EEUR`
(אירופה, ליד eu-central-1 שבו יושב המסד).

**2. טוקן R2 — פעולת מייסד, במסך `R2 > Manage R2 API Tokens`.**
**מוגבל לדלי `mishmeret-backups` בלבד, הרשאת `Object Read & Write` בלבד.**
לא טוקן ברמת חשבון: טוקן גיבוי שדולף לא אמור לתת גישה לשום דבר אחר.
הוא מייצר `Access Key ID` ו-`Secret Access Key`.

**3. שלושה סודות נוספים בריפו:** `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`,
`R2_ACCOUNT_ID` (מזהה החשבון מופיע באותו מסך).

**4. כלל מחזור חיים בדלי:** מחיקת אובייקטים אחרי 90 יום, כדי שהאחסון לא
יטפס לתקרת ה-10GB בשקט. תואם ל-`retention-days: 90` של הארטיפקט.

**5. אימות:** מריץ את ה-workflow ידנית, מוודא ששורת הלוג עברה מ-`r2=skipped`
ל-`r2=uploaded`, ומוודא שהאובייקט קיים בדלי בגודל הנכון.

ה-workflow כבר כתוב כך ש**היעדר סודות R2 מדלג בשקט ואינו מפיל את הגיבוי** —
כלומר N8 ו-N9 אינם תלויים זה בזה, וכל אחד מהם משפר את המצב לבדו.

---

## 5. עלות: 0 ₪, ועם המספרים

מדדתי את גודל המסד: `public` 1.4MB · `auth` 2.7MB · `storage` 416KB ·
`supabase_migrations` 232KB. דאמפ custom מכווץ ומוצפן — **סדר גודל של 1MB**.

| משאב | צריכה | מכסה חינמית | הערה |
|---|---|---|---|
| דקות Actions | ~3 דק׳ × 30 = **90 דק׳/חודש** | 2,000 (ריפו פרטי) | 4.5%. התרגיל מוסיף ~40 שניות |
| אחסון ארטיפקטים | ~1MB × 90 יום = **90MB** | 500MB | 18%. **זה מה שיגדל ראשון** |
| R2 — אחסון | ~90MB | 10GB | 0.9% |
| R2 — פעולות כתיבה | 30/חודש | מיליון | זניח |

**מה שיזיז את זה:** גידול המסד, לא תדירות. כשהדאמפ יעבור ~5MB, אחסון
הארטיפקטים ייגע בתקרה — ואז מקצרים את שמירת הארטיפקט ל-14 יום ומשאירים
את 90 הימים ב-R2, שם יש פי עשרים מקום. **אמדוד את הגודל האמיתי בריצה
הראשונה ואעדכן את השורה הזאת במספר, לא באומדן.**

---

## 6. מה אני צריך ממך, ומה מהמייסד

**ממך:** אישור ש-`backup.yml` אכן בידיי (N4 מוקצה, ואני כותב אליו) ואישור
להסרת `--no-privileges`. זה שינוי בעל השלכה על שחזור, ולא אשאיר אותו שקט.

**מהמייסד, בסדר הזה:** N8 — לוודא שהריפו הפרטי קיים, לאשר דחיפה, ולהתחבר
פעם אחת. N9 — להפעיל R2 ולייצר טוקן מוגבל לדלי. **ושמירת
`BACKUP_PASSPHRASE` במנהל סיסמאות**, וזו הפעולה שהכי קל לדחות והכי יקר
לגלות שנדחתה.

---

## בלוק חפיפה

**DONE** — DNS נבדק: המארח הישיר הוא AAAA בלבד וללא A, וה-pooler מחזיר
שלוש כתובות IPv4. **זו הסיבה שהגיבוי לא היה רץ, והיא לא הייתה בתמונה של
אף אחד.** `backup.yml` נכתב מחדש: pooler, תרגיל שחזור על הקובץ המוצפן עם
שלושה תנאי כשל מספריים, `--no-privileges` הוסר, `concurrency`, `pull
--rebase` לפני דחיפה, ודילוג שקט על R2. סריקת סודות על כל ההיסטוריה ועל
עץ העבודה — נקי; אומת ש-`app.html:700` מחזיק מפתח publishable בלבד.
`BACKUP_PASSPHRASE` נוצר (8 מילים מרשימת 128), נשמר מחוץ לריפו,
`icacls` הגביל למשתמש. גודל המסד נמדד לפי סכימה לצורך טבלת העלות.

**STATE** — `backup.yml` בעץ העבודה, **לא נדחף ולא רץ מעולם**. ארבעה סודות
קיימים במכונה, אפס מהם הוזנו לשום מקום. לא נגעתי ב-`service_role`,
ב-`app.html`, ובאף אובייקט במסד.

**NEXT** — ממתין ל-N8 ול-N9. בינתיים אין לי עבודה חוסמת בגיבוי; אם תרצה,
אתחיל לכתוב את ה-Worker של N5 כנגד 36 השדות עוד לפני שה-RPC קיים, ואחבר
אותו ביום שהוא נכתב.

**FOR YOU** — (1) `backup.yml` בידיי? (2) אישור הסרת `--no-privileges`.
(3) 36 השדות והשמות הנעולים מ-`DEVOPS-SPEC-A2-HANDOFF.md` §3.2–3.3 עדיין
פתוחים, וה-Worker לא ייכתב נכון בלעדיהם.

**MUST KNOW** — **הריצה הראשונה היא הבדיקה הראשונה.** אין דרך להריץ
GitHub Actions בלי לדחוף, ואין `node`, `pg_dump` או `gh` במכונה הזאת, כך
שגם סימולציה מקומית אינה אפשרית. כתבתי את ה-workflow כך שייכשל רועש
ומוקדם בכל אחד מהמקומות שבהם הוא יכול להיכשל בשקט — זה התחליף היחיד
לבדיקה מראש, ואני לא מתחזה לכך שהוא נבדק.

**גבול האימות של ה-YAML:** אין `node` ואין Python אמיתי במכונה (רק ה-stub
של חנות Windows), ולכן לא הרצתי parser על הקובץ. בדקתי מבנית בלבד: חמישה
מפתחות ראשיים, שמונה שלבים, חמישה סודות, אפס טאבים. **GitHub הוא ה-parser
הראשון שיראה אותו.**

**OPEN** — עדיין אין עותק מחוץ למכונה. קומיט מקומי `955f621` בלבד. ·
`private.operators` קיים במסד, `private.status_key` עדיין לא — כלומר
שינוי 1 טרם הוחל. · ה-URI של ה-pooler חייב להגיע מהדשבורד; תבנית המארח
משתנה בין פרויקטים ואני לא מנחש אותה.
