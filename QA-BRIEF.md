# QA-BRIEF — סבב M: מיקום, סיכה ומפה
**מאת: Tech Lead · 19 בספטמבר 2026, לילה · דורס כל QA-BRIEF קודם**

קרא קודם את `QA-HANDOVER.md` שלך. הקובץ הזה מוסיף סבב אחד.
אתה **קריאה בלבד**; בדיקת כתיבה שאתה לא יכול להריץ — נסח אותה ומסור לי או
למפתח, בדיוק כפי שתרצה שתרוץ.

---

## ‼ עדכון 20/09 — תתחיל עכשיו, בחצי השרתי

**אל תחכה לכניסה למערכת.** הסבב חסום על session בדפדפן, אבל **רוב הבריף
הזה לא צריך דפדפן בכלל** — הוא רץ בהתחזות מול המסד.

**מה שפתוח לך עכשיו, מיד:**
- סעיף 1 כולו — מבחן הבריאות המתוקן.
- סעיף 3.1 פריט 2 (עסק שיושב באמת על מרכז העיר).
- סעיף 3.2 כולו — הגבול וההרשאות: 50 ק״מ, מנהל שאינו בעלים, כתיבה ישירה
  ל-`located_at`, עסק סגור.
- סעיף 3.3 כולו — הזזת סיכה מול רדיוס, `max_distance_km`, ו-UNDO.

**מה שממתין ל-session — צומצם 20/09 04:46.** רתמת הלקוח של המפתח
(`app-test.html`, עותק של `app.html` שנבדל בשורת ה-`import` **בלבד** —
אימתתי את זה ב-diff משלי) מכסה עכשיו את רוב 3.4.

**אל תריץ מחדש את מה שהרתמה כבר מוכיחה. תבקר אותה.** קרא את הטענות
ב-`fake-sb.js` ותגיד לי אם טענה **מנוסחת חלש** — למשל בודקת שקריאה
קיימת במקום לבדוק שהיא יחידה, או בודקת שדה קיים במקום לבדוק את ערכו.
זה שווה יותר מריצה חוזרת.

**מה שנשאר באמת ל-session:** 3.1 פריטים 1 ו-3 (מטען הרשת בפועל בלשונית
הרשת), ו-3.5 — כלומר הדבר היחיד שרתמה לא יכולה להוכיח: **ששורה נכתבה
במסד בעקבות לחיצה בדפדפן מחובר.**

**הרתמה כבר תפסה באג אמיתי** שאף אחד מאיתנו לא ראה בקריאת קוד:
`wazeUrl` החזיק ענף שלישי שנפל ל-`?ll=` כשאין דגל ואין כתובת, כלומר
שלח את העובד לנווט **למרכז העיר**. נסגר. אם תמצא עוד נפילה כזו — זו
בדיוק המשפחה שהסבב הזה קיים בשבילה.

**אל תדווח "חסום" על מה שאתה יכול להריץ.** תריץ, תדווח, ותשאיר את החצי
השני מסומן `ממתין ל-session` — בדיוק כפי ששני הקולגות שלך עשו.

### מה כבר אומת ואין טעם שתחזור עליו
אימתתי בעצמי, במדידה ולא לפי דיווח:
- **שכבת ה-CSS של המפה** — 16 ערכים בשני המצבים, מול בלוק הסגנון האמיתי
  שנמשך מ-`app.html` ומול הטוען הדינמי האמיתי. הכול תקין.
  **אם תראה פופאפ/תווית/attribution לבנים במצב כהה — זה באג חדש, דווח.**
- **חוזה ה-RPC** — 16 שמות פונקציה שהלקוח קורא (10 מפורשים, 6 דרך שיגור
  דינמי), כולם קיימים, כל שמות הפרמטרים תקפים. אפס אי-התאמות.
- **מחרוזות** — 92 קודי `ui` שהלקוח מבקש בפועל, אפס חסרים ב-he, אפס ב-en,
  אין `.one` בלי `.other`.

זה משחרר אותך להתמקד במה שבאמת לא נבדק: **התנהגות, לא סטטיקה.**

---

## 0. פתח בזה. שתי בדיקות קודמות התבזבזו על snapshot ישן

```sql
select version, name from supabase_migrations.schema_migrations order by version desc limit 5;
```

**עודכן 20/09 — הראש זז פעמיים מאז.** חייב להופיע בראש
`shifts_carry_their_own_located_at_snapshot`, ומתחתיו
`my_shifts_exposes_biz_located_at`, `propagate_pin_to_cancelled_but_future_shifts`,
`map_round_remaining_ui_terms` ו-`map_pin_truth_and_shift_location_propagation`.
לא שם — אתה בודק גרסה ישנה. עצור.

**ממצא 1 שלך נסגר, וההכרעה הייתה שלך:** הדגל עבר למשמרת.
`shifts.located_at` הוא צילום מצב כמו `lat/lng`; `post_shift` מעתיק אותו
בזמן פרסום; `update_business` מעדכן אותו רק במשמרות שטרם הסתיימו;
`my_shifts` מחזיר `located_at` (בלי התחילית `biz_`) מהמשמרת ולא מהעסק.
אומת ב-rollback בדיוק בתרחיש שתיארת. **הוסף ל-Q5 את השורה:**
```sql
union all select 'ended shift flag <> its own point', count(*)
  from shifts s join cities c on c.id=s.city_id
  where s.ends_at <= now()
    and (s.located_at is not null) <>
        (extensions.ST_Distance(s.location,
           extensions.ST_SetSRID(extensions.ST_MakePoint(c.lng,c.lat),4326)::extensions.geography) > 150)
```
זו לא בדיקת אמת מוחלטת — ההקצאה למפרע נשענה על אותה היוריסטיקה — אבל
היא תתפוס סחיפה עתידית.

---

## 1. ‼ תיקון חובה ב-Q5 שלך — אחרת תדווח שקר

השורה האחרונה ב-Q5 היא:

```sql
union all select 'shift pin <> business pin', count(*) from shifts s join businesses b on b.id=s.business_id
  where s.lat is distinct from b.lat or s.lng is distinct from b.lng;
```

**השורה הזו כבר לא נכונה, ומהסבב הזה היא תחזיר מספר חיובי בצדק.**

`shifts.lat/lng` הם צילום מצב מזמן הפרסום. מעכשיו הזזת סיכה מעדכנת רק את
המשמרות שטרם הסתיימו, ו**משאירה בכוונה** את אלה שהסתיימו על הנקודה שבה הן
בוצעו בפועל — זו היסטוריית ההגעה, והיא לא נכתבת מחדש. אחרי כל הזזת סיכה של
עסק עם משמרות עבר, השורה הישנה תספור אותן כתקלה.

**החלף אותה בשתי השורות האלה:**

```sql
union all select 'LIVE shift pin <> business pin', count(*)
  from shifts s join businesses b on b.id=s.business_id
  where s.ends_at > now() and (s.lat is distinct from b.lat or s.lng is distinct from b.lng)
union all select 'LIVE shift name <> business name', count(*)
  from shifts s join businesses b on b.id=s.business_id
  where s.ends_at > now() and s.biz_name is distinct from b.name
```

**ועוד שלוש שורות חדשות שנולדו עם העמודה `businesses.located_at`:**

```sql
union all select 'located_at set but pin at centroid', count(*)
  from businesses b join cities c on c.id=b.city_id
  where b.located_at is not null
    and extensions.ST_Distance(b.location,
          extensions.ST_SetSRID(extensions.ST_MakePoint(c.lng,c.lat),4326)::extensions.geography) < 1
union all select 'pin outside the 50 km bound', count(*)
  from businesses b
  where b.lat is not null and not private.point_near_city(b.lat, b.lng, b.city_id)
union all select 'ui term missing a language', count(*) from (
  select code from terms where kind='ui' group by code having count(distinct lang) < 2) x
```

השורה `located_at set but pin at centroid` היא **הבדיקה המרכזית של הסבב**.
היא תופסת בדיוק את הכשל שממנו אני הכי חושש: לקוח ששולח את מרכז העיר כאילו
היה סיכה שהמשתמש בחר. אם היא אי פעם תחזיר מספר — המערכת מבטיחה מיקום מדויק
שאינו קיים, ועובד ייסע לכתובת הלא נכונה.

**בסיס שאני מוסר לך, נמדד עכשיו:** כל השורות 0, פרט ל-
`address but no pin` שמחזירה **1** — זה `ביסטרו נוטר` של המייסד, כתובת
אמיתית (אוסישקין 11) וסיכה במרכז נתניה. זה הבאג שהסבב סוגר; הוא יירד ל-0
כשהמייסד יסמן את הסיכה דרך המסך החדש. **אל תדווח עליו שוב כממצא.**

---

## 2. מה השתנה בשרת

### 2.1 `businesses.located_at timestamptz` (חדש)
`null` = ה-`lat/lng` הם מרכז העיר, אף אחד לא סימן. לא-null = סיכה מפורשת.
הוקצה למפרע לפי מרחק > 150 מ׳ ממרכז העיר.

### 2.2 `create_business`
חצי נקודה (`p_lat` בלי `p_lng` או להפך) → `bad_location`. נקודה מלאה נבדקת
מול 50 ק״מ מהעיר ורושמת `located_at`. בלי נקודה → מרכז עיר, `located_at` null.

### 2.3 `update_business`
כותב `located_at`, ומעדכן `lat/lng/biz_name` בכל משמרת של העסק ש**טרם
הסתיימה** (כולל אחת שבוטלה וממתינה ב-UNDO — כדי ששחזור לא יחזיר נקודה ישנה).
משמרת שהסתיימה לא נוגעים בה. מחזיר `shifts_moved`.

### 2.4 `terms`
38 שורות `ui` חדשות (19 קודים × he/en) למפה ולסיכה. סך `ui` כעת 298.

**מה שכבר אומת אצלי ב-rollback, ואתה מוזמן לא להאמין לי ולחזור על זה:**
בעלים הזיז סיכה → המשמרת החיה זזה ו-`location` חושב מחדש (0.0 מ׳);
שתי משמרות שהסתיימו שמרו שם ונקודה; זר קיבל `forbidden`; חצי נקודה קיבלה
`bad_location`; נקודה בבאר שבע מול עסק בנתניה קיבלה `location_far`.

---

## 3. איפה ללחוץ

### 3.1 השקר על המיקום — העיקר בסבב הזה
1. **לקוח ששולח מרכז עיר כסיכה.** פתח עסק דרך הממשק **בלי לגעת במפה**.
   `located_at` חייב להישאר `null`. אם הוא נכתב — הלקוח שולח את ברירת
   המחדל, וכל תג "מיקום מדויק" באפליקציה הוא שקר. **זה כשל חוסם.**
2. **מרכז עיר אמיתי.** עסק שיושב באמת על הקואורדינטה של מרכז העיר וסומן
   בכוונה. `located_at` יהיה מלא והמרחק 0 — השורה
   `located_at set but pin at centroid` תתפוס אותו כתקלה. **תגיד לי אם זה
   קורה בפועל**; ייתכן שהסף צריך להיות "הלקוח שלח נקודה" ולא מרחק.
3. **תג המיקום המשוער בממשק.** עסק בלי `located_at` חייב להופיע לעובד
   כ"מיקום משוער · מרכז העיר" — בכרטיס, בפופאפ על המפה, ובכל מקום שהסיכה
   נראית. עסק אחד בלי התג הזה הוא הבטחה שקרית.

### 3.2 הגבול והרשאות
4. **50 ק״מ**: נסה נקודה מעבר לגבול דרך הממשק ודרך קריאת RPC ישירה. בשתי
   הדרכים `location_far`, ובלי כתיבה חלקית — ודא שהשם והטלפון **גם הם**
   לא נשמרו כשהנקודה נדחתה.
5. **מנהל שאינו בעלים** מנסה `update_business` → `forbidden`. ודא שגם
   הסיכה לא זזה וגם המשמרות לא זזו.
6. **כתיבה ישירה ל-`businesses.lat/lng/located_at`** מהדפדפן. הגרנטים
   אמורים לחסום (SELECT בלבד). ודא שהעמודה החדשה לא פתחה חריץ.
7. **עסק סגור** (`closed_at`) — `update_business` מחזיר `business_closed`,
   ואף משמרת לא זזה.

### 3.3 ההשלכה שאף אחד לא רואה: הזזת סיכה משנה מי רואה את המשמרת
8. סיכה שזזה משנה את `shifts.location`, שממנו נגזר הרדיוס הדו-צדדי.
   בדוק: עובד שמשמרת **הייתה** בטווח שלו, אחרי שהמעסיק הזיז את הסיכה
   40 ק״מ — המשמרת נעלמת מהלוח שלו. זו התנהגות **נכונה** (המסעדה באמת שם),
   אבל אם הוא כבר **שובץ** — הוא נשאר משובץ, ו-`my_shifts` עדיין מחזיר לו
   אותה. ודא ששיבוץ קיים לא נשבר ולא נעלם.
9. **`max_distance_km`**: מעסיק שמזיז סיכה לא אמור לפסול מועמד שכבר אושר.
   `claim_shift` בדק בזמן ההגשה. ודא שאין בדיקה חוזרת שמפילה משהו קיים.
10. **UNDO**: בטל משמרת, הזז את הסיכה, שחזר תוך החלון. המשמרת המשוחזרת
    חייבת לשבת על הסיכה **החדשה**. זה בדיוק מה שסגרתי במיגרציה השלישית.

### 3.4 המפה בלקוח
11. **אותן משמרות בדיוק** ברשימה ובמפה. ספור. משמרת של עסק שהמשתמש חבר בו
    מוסתרת בשתיהן, והשרת עדיין מחזיר `own_business` על ניסיון ישיר.
12. **הגשה מתוך פופאפ** יוצרת בדיוק שורה אחת ב-`applications`, זהה להגשה
    מהרשימה. לא שתיים, לא אחת עם שדה חסר.
13. **אין קואורדינטות של עובד בשום בקשה.** פתח את לשונית הרשת, סנן לפי
    `rest/v1`, ועבור על כל קריאה במסך המפה. `lat`/`lng` של המשתמש לא
    מופיעים בגוף, לא בפרמטרים ולא בכותרות. אין קריאה ל-`geolocation`.
    **זו החלטה ארכיטקטונית, לא העדפה.**
14. **גרירת מחוון הרדיוס במצב מפה** — אין `Map container is already
    initialized` בקונסולה, אין מופעי מפה שמצטברים.
15. **`ui.map_*` ו-`ui.pin_*`** — כל מחרוזת על המסך מגיעה מ-`terms`. אם
    תמצא מחרוזת חדשה כתובה בקוד, זה ממצא. הרץ את שורת
    `ui term missing a language`.

### 3.5 ניווט
16. עסק **עם** `located_at` → קישור הניווט לפי קואורדינטה (`?ll=`).
    עסק **בלי** → לפי כתובת (`?q=`). בלי שניהם → אין קישור כלל.
    זו היפוך של ההתנהגות הקודמת; ודא שההערה בקוד עודכנה ולא נשארה מטעה.

---

## 4. מה לא לדווח שוב

- Leaked Password Protection — לא זמין ב-free tier.
- אזהרות `SECURITY DEFINER` על 17 ה-RPC — זו הארכיטקטורה.
- `result.last_owner` בלי מסלול בממשק.
- `address but no pin = 1` — זה `ביסטרו נוטר`, מתועד למעלה.
- סיסמה מזערית 6 — פתוח אצל המייסד בהגדרות Auth.

---

## 5. סיום

בלוק חפיפה ל-**Tech Lead**. ב-`DONE` — מה אומת ואיך, לא מה נוסה.
כל ממצא: **הצעד המדויק לשחזור, מה ציפית, מה קרה**. אם בדיקה דורשת כתיבה
שאתה לא יכול לבצע — נסח את ה-SQL המדויק ומסור, אל תדלג ואל תסמן אותה כעברה.
