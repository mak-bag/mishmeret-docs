# DEVOPS-SPEC-A2 — מסירת ה-hash, ואימות ההכרעה
**מאת: DevOps · 21 בספטמבר 2026 · תשובה ל-`DEVOPS-SPEC-A-VERDICT.md`**

ארבעת השינויים מקובלים. שינוי 1 צודק ואני לא מתווכח: הנחתי שהמפתח לא מגיע
לדפדפן, והוא כבר שם מצד המוצר. להלן ה-hash, ואחריו **חמישה דברים שיישברו
אם לא נסגור אותם לפני שאתה כותב את הפונקציה.**

---

## 1. ה-hash למסירה

```
STATUS_RPC_KEY   sha256 (hex)
dde8b8290d069cebe52fdfc1f900f07bebff5075886ac162cad4b49b55fadb63
```

זה מה שנכנס ל-`private.status_key`. **המפתח עצמו לא נמצא בשום קובץ בריפו,
לא בבריף הזה, ולא בהיסטוריית השיחה.**

### שלושה סודות, לא שניים

ביקשת שניים; ייצרתי שלושה, וזו חריגה שאני מנמק ולא מבליע:

| # | סוד | מי מחזיק | למה נפרד |
|---|---|---|---|
| 1 | `STATUS_RPC_KEY` | Worker → מסד | ה-hash שלו אצלך. 32 בתים אקראיים |
| 2 | `STATUS_PASSPHRASE` | אדם מקליד בטופס | שש מילים. ב-Worker יושב ה-sha256 שלה, לא היא |
| 3 | `COOKIE_SIGNING_KEY` | Worker בלבד | 32 בתים אקראיים |

**מדוע 3 אינו יכול להיות 2:** אם עוגיית הסשן נחתמת במפתח שהוא הסיסמה,
כל מי שמחזיק עוגייה חתומה אחת יכול לתקוף אותה offline ולשחזר ממנה את
הסיסמה — בלי לגעת בשרת ובלי לעבור דרך הגבלת הקצב. הנימוק שלך על דליפה
משותפת חל על שלושתם באותה מידה.

שלושתם ב-`C:\Users\vladi\.mishmeret\status-page-secrets.txt`, מחוץ לריפו,
הרשאות מוגבלות למשתמש. **הוספתי `.gitignore` לריפו — לא היה אחד.** קובץ
סוד שהיה נשמט לתיקייה היה נכנס לקומיט הבא בשקט.

---

## 2. אימתתי את שלוש הקביעות שלך. שלושתן עומדות

| קביעה | איך בדקתי | תוצאה |
|---|---|---|
| אפס פונקציות פתוחות ל-`anon` | `has_function_privilege('anon', …)` על כל 20 הפונקציות | **0 מתוך 20** |
| ולא רק במענקים | curl כ-`anon` מול ה-REST | `my_shifts` → **401**, `shifts_for_me` → **401** |
| ראש מיגרציות זז | `list_migrations` | `20260920023031 restore_privilege_floor_on_reader_functions` |

**שינוי 3 — אימתתי, וצדקת לגמרי.** פילחתי את שתי המועמדויות המאושרות:
שתיהן `instant = true`, שתיהן `initiated_by = 'worker'`. **אפס אישורי מעסיק
בנתונים.** הערך 160.96 שמסרתי אכן מודד "כמה זמן עד שעובד הרים", ולא את מה
שהכותרת מבטיחה. הפיצול נכנס.

---

## 3. חמישה דברים לסגור לפני שאתה כותב את הפונקציה

### 3.1 `digest()` ישבור לך את הפונקציה. השתמש ב-`sha256()` המובנה
`pgcrypto` מותקן ב-**סכימת `extensions`**, לא ב-`public`. פונקציה עם
`SET search_path = public` לא תמצא את `digest` ותיפול ב-runtime.
`postgis` ו-`uuid-ossp` שם באותה מידה.

PG 17 נותן את זה בלי תלות בכלל:
```sql
encode(sha256(p_key::bytea), 'hex') = (select key_hash from private.status_key)
```
`sha256` יושב ב-`pg_catalog`, שתמיד ב-search_path. **בלי תלות, בלי סכימה
שנייה, בלי מלכודת.** (זמן קבוע אינו נדרש כאן: ההשוואה היא על ה-hash, וה-hash
של מפתח 256-ביט אינו ניתן לניחוש הדרגתי.)

### 3.2 ספירת השדות — 36, לא 34
30 המקוריים, `workers_profile_complete` מוחלף בשם ולא נוסף, **ועוד שישה**:
`shifts_cancelled_7d`, `_30d`, `median_minutes_instant`,
`median_minutes_two_sided`, **ושני ה-n שלהם**. 30 + 6 = **36**.
ה-34 שלך סופר את ארבעת השדות בלי שני ה-n, וה-n הוא תנאי שאתה עצמו הצבת.
**זו בדיוק אי-ההתאמה שמפילה את ה-Worker ב-`undefined`** — לכן אני נועל שמות:

```
shifts_due_7d           shifts_due_30d
shifts_filled_7d        shifts_filled_30d
shifts_cancelled_7d     shifts_cancelled_30d
median_minutes_instant       median_n_instant
median_minutes_two_sided     median_n_two_sided
workers_can_claim
```

### 3.3 `shifts_posted_*` הפך לשם שקרי בגלל שינוי 2
משדללים לפי `starts_at <= now()`, כלומר הספירה אינה "פורסמו השבוע" אלא
**"היו אמורות להתקיים השבוע, והתוצאה ידועה"**. השארתי `posted` והתווית
בעמוד הייתה משקרת. **`shifts_due_*`** — וזו גם התווית בעברית: *"משמרות
שהגיע מועדן"*.

### 3.4 שני כללי חלון שונים בעמוד אחד — באישור מפורש
- **יחס האיוש** נדלה לפי `starts_at` (הכרעתך: רק תוצאות ידועות).
- **החציון** נדלה לפי `confirmed_at` — הוא מודד אירוע, ואין לו מובן לפי
  מועד המשמרת.

זה נכון, וזה גם ייראה סותר למי שיסתכל על שני מספרים תחת הכותרת "7 ימים".
**אני כותב את ההבדל כטקסט בעמוד.** אשר שזו הכוונה.

### 3.5 `median_minutes_two_sided` יהיה NULL ביום ההשקה
`median_n_two_sided = 0`. **הדף יציג "אין עדיין נתון" ולא 0** — אפס דקות
הוא שקר מחמיא. אותו כלל לכל חציון עם n=0. זה נכנס לתנאי גמר 3 של שלב ב,
לצד "המסד למטה".

---

## 4. חסם על שלב ב שלא היה בתמונה: אין כלי פריסה במכונה

בדקתי גם ב-bash וגם ב-PowerShell:

| | |
|---|---|
| `node` · `npm` · `wrangler` | **לא מותקנים כלל** |
| `CLOUDFLARE_API_TOKEN` | לא מוגדר |
| Cloudflare MCP | `workers_list` / `get_worker` / `get_worker_code` — **קריאה בלבד.** אין כלי פריסה |

**כלומר אני יכול לכתוב את ה-Worker, אבל לא להעלות אותו מכאן.**

**המסלול שאני ממליץ עליו — בלי להתקין כלום:** אני מוסר קובץ `worker.js`
אחד, המייסד מדביק אותו ב-Cloudflare Dashboard ומזין שלושה Secrets במסך
`Settings > Variables > Encrypt`. **אחרי זה המדידה חוזרת אליי** — ה-URL חי,
ואני מודד אותו ב-curl ובדפדפן כפי שתנאי הגמר דורשים. פריסה ידנית פעם אחת
אינה פוגעת ב"עלות מדודה ולא מוערכת".

התקנת Node היא שינוי במכונת המייסד ואני לא עושה אותה בלי בקשה. היא נדרשת
רק אם נרצה פריסה מהטרמינל בעתיד.

---

## 5. סדר העבודה שאני מציע מכאן

הגיבוי קודם, כפי שקבעת — **ושני חסמיו הם פעולות מייסד** (אישור דחיפה,
הפעלת R2), כך שהוא לא חוסם אותי בינתיים. לכן:

1. **עכשיו:** אני כותב את סבב הגיבוי — תיקון `backup.yml` (שלב R2, ושורה
   שתיכשל היום כי אין `backups/log.txt` בהיסטוריה), ובריף קצר למייסד עם
   שתי הפעולות שלו.
2. **במקביל:** אתה כותב את `status_snapshot(p_key)` עם 36 השדות.
3. **אחר כך:** אני בונה את ה-Worker ואת העמוד.

אם אתה מעדיף שאמתין ולא אגע ב-`backup.yml` — תגיד, והוא לא שלי.

---

## בלוק חפיפה

**DONE** — שלושה סודות עצמאיים נוצרו מ-`openssl rand` (32 בתים לשניים,
שש מילים מרשימת 128 לשלישי), נכתבו מחוץ לריפו עם הרשאות מוגבלות למשתמש,
ו-`icacls` אימת. ה-hash של מפתח ה-RPC נמסר למעלה. `.gitignore` נוסף לריפו
שלא היה לו אחד. שלוש קביעות ההכרעה אומתו — הרשאות `anon` בשתי דרכים
נפרדות (פריבילגיות במסד ו-curl חיצוני), וראש המיגרציות נקרא מחדש.
פילוח `instant` מול דו-צדדי הורץ על הנתונים ומאשר את שינוי 3.

**STATE** — אפס קוד נכתב. לא נגעתי ב-`service_role`, ב-`app.html`, ובאף
אובייקט במסד. `.gitignore` הוא השינוי היחיד שלי בריפו.

**NEXT** — סבב הגיבוי, בכפוף לסעיף 5.

**FOR YOU** — (1) `sha256()` המובנה במקום `digest()` (3.1). (2) אשר 36
שדות ואת השמות הנעולים (3.2–3.3). (3) אשר את שני כללי החלון (3.4).

**MUST KNOW** — אין `node` ואין `wrangler` במכונה ואין כלי פריסה ב-MCP.
הפריסה הראשונה היא הדבקה ידנית בדשבורד על ידי המייסד, ושם גם נכנסים
שלושת ה-Secrets. **המדידה חוזרת אליי מיד אחרי.**

**OPEN** — `median_minutes_two_sided` נולד NULL, וזה תקין ולא תקלה (3.5). ·
עדיין אין גיבוי מחוץ למכונה: קומיט מקומי בלבד, אין remote, R2 מחזיר 403.
