# DESIGN-NOTE — סבב M, סיבוב 2
**מאת: Tech Lead · 20 בספטמבר 2026 · תשובה ל-`DESIGN-M5-MAP.css`**

קרא את זה לפני שאתה נוגע בעוד שורה. חלק מהקובץ שלך לא ייכנס, ואני רוצה
שתדע בדיוק איזה חלק ולמה — ומה כן נכנס, שהוא החלק שרק אתה יכולת לייצר.

---

## 1. ההכרעה, בלי לרכך אותה

**רוב `DESIGN-M5-MAP.css` לא מודבק.** המפתח כתב CSS מלא למפה ב-00:08, חמש
דקות לפני שמסרת, עם חוזה מחלקות אחר: אצלו `.pin-shift` הוא האלמנט עצמו עם
`.many`, אצלך `.pin-shift > .pin-body` עם `.multi`; אצלו `.pin-pick.on`,
אצלך `.unset/.set/.is-dragging`; אצלו `.pop-row`, אצלך `.map-pop`.

הכרעתי לטובת הבלוק שלו, ולא כי הוא טוב יותר. הוא כבר משולב ונטען בלי
שגיאות, והחלפתו דורשת להחליף גם JS שאיש לא יכול לבדוק בדפדפן עד שהמייסד
ייכנס לחשבון. **שינוי לא בדיק בתוך קוד לא בדיק הוא איך נולדים באגים בלי בעלים.**

**זו תקלה שלי, לא שלך.** ה-DESIGN-BRIEF נתן לך דרישות ולא שמות מחלקות
סופיים, ואמרתי "תאם עם המפתח" במקום לקבוע מי מחזיק את חוזה המחלקות. בסבב
הבא שמות המחלקות נקבעים מראש, על ידי, לפני ששניכם מתחילים.

---

## 2. מה כן נכנס — ארבעה פריטים, וכולם שלך

1. **`--tiles-filter` ו-`--pin-shadow`**, בשלושת בלוקי הטוקנים.
2. **`.leaflet-tile-pane{filter:var(--tiles-filter)}`** — עם הערכים שכיילת,
   בלי שינוי של מספר אחד.
3. **`isolation:isolate`** על `.map`, בנוסף ל-`z-index:0` של המפתח.
4. **מצב `.is-open`** לסמן שנפתח, כולל שני מאזיני `popupopen/popupclose`.

פילטר האריחים הוא הפריט היחיד בסבב הזה שאי אפשר היה להגיע אליו בלי לשבת
מול אריחים אמיתיים בשלוש איטרציות. בלעדיו המפה לבנה בוהקת בממשק כהה, וזה
היה הפער הוויזואלי היחיד שהמפתח השאיר פתוח במודע.

---

## 3. בדקתי את הצירוף שאיש מכם לא בדק

הרכבתי רתמה: **ה-markup של המפתח** עם **הפילטר שלך**, 40 סמנים מעל מרכז
תל אביב, בהיר וכהה.

**אי-הזליגה שדיווחת עליה — מאושרת, ועכשיו גם מול הקוד האמיתי ולא רק מול
האב-טיפוס שלך.** `marker-pane`, `popup-pane`, `overlay-pane` **ו-`tooltip-pane`**
כולם `filter:none`. הסמנים יצאו `rgb(37,99,235)` על לבן, הטבעת `rgb(92,155,255)`.

**מה שכיסיתי מתוך ה-OPEN שלך (סעיף 2 — "לא ראיתי עם נתונים אמיתיים"):**
- **צפיפות 40 סמנים** — עובדת. יש חפיפה חלקית באשכולות צמודים, אבל כל סמן
  נשאר לחיץ. לא נדרש שינוי לפיילוט.
- **שם עסק ארוך בפופאפ** — נשבר לשתי שורות כראוי.
- **עסק בלי כתובת** — `.pop-sub` יורד בלי להשאיר מפריד ריק.

**מה שעדיין פתוח ואינו שלך:** הכל מאחורי הכניסה. זה עובר ל-QA.

---

## 4. פגם שמצאתי, ושנינו־שלושתנו פספסנו

**`.leaflet-tooltip` יוצא לבן.** מדוד: רקע `rgb(255,255,255)`, טקסט
`rgb(34,34,34)`, גבול לבן. במצב כהה זו לוחית לבנה בוהקת על מפה כהה.

הסיבה מדויקת ומלמדת: אתה עיצבת `.pin-city .pin-label` — אלמנט פנימי משלך.
המפתח מימש את אותה תווית דרך `bindTooltip({permanent:true})` של Leaflet.
אף אחד לא עיצב את ה-tooltip, כי אף אחד לא ידע במה השני משתמש. זה בדיוק
אותו כשל שהוליד את התנגשות המחלקות, באותו יום.

**הכרעה: נשאר `bindTooltip`, ואתה מעצב אותו.** כלל אחד מכסה גם כל תווית
עתידית. נתתי למפתח ערכי פתיחה כדי שלא ייחסם:

```css
.leaflet-tooltip{background:var(--surface);color:var(--text);
  border:1px solid var(--border);border-radius:8px;box-shadow:var(--pin-shadow);
  font-size:12px;font-weight:600;padding:4px 9px;white-space:nowrap}
.leaflet-tooltip-bottom::before{border-bottom-color:var(--border)}
```

**מה שאני צריך ממך:** לאשר או לכייל את הערכים האלה בעיניים אחרי ההדבקה,
בשני המצבים, כולל החץ. אל תערוך את `app.html` — המפתח מחזיק את הקובץ בסיבוב
הזה. מסור תיקון כטקסט.

---

## 5. שתי ה-OPEN הנותרות — הכרעתי, לא צריך לחזור אליהן

**טבעת ה-50 ק״מ בזום 14** (מדדת 12,400px ברוחב 688px): **אין שינוי.**
זום 14 הוא הזום שבו מסמנים דלת של מסעדה — זה מה שהמסך קיים בשבילו. הטבעת
היא מעקה שנעשה רלוונטי בדיוק כשמתרחקים, ואז היא מופיעה מעצמה. סגור.

**attribution ב-10.5px וב-`--muted`**: **מאושר וחתום עליי.** קריא, הקישור
עובד, וזה בקירוב גודל ברירת המחדל של Leaflet. אתה והמפתח הגעתם לאותם ערכים
בנפרד — זה סימן טוב, לא צירוף מקרים.

**`:hover` של הסמנים לא נבדק במגע** — נכון, ואין hover בטלפון. מצב `.is-open`
שנכנס נותן את המשוב שחסר במגע. זה מכסה את החשש.

---

## 6. שורה תחתונה

העבודה שלך לא הלכה לפח: הפריט שהציל את מצב הכהה הוא שלך, אי-הזליגה שלך
אומתה מול הקוד האמיתי, ושלוש מה-OPEN שלך נסגרו במדידה. מה שאבד הוא חוזה
המחלקות — ואת זה אני הייתי צריך לקבוע מראש.

שמור את `DESIGN-M5-MAP.css` בתיקייה. אם נחליף אי פעם את שכבת הסמנים, הוא
הבסיס להחלפה הזו, כולל מצבי `is-dragging` והטיפה של `.pin-pick`.
