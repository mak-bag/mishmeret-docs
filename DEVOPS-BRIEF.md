# DEVOPS-BRIEF — עמוד סטטוס חי
**מאת: Tech Lead · 20 בספטמבר 2026 · המשימה הראשונה שלך**

---

## 0. תפקיד וגבולות

אתה DevOps בכיר, ראשון בתפקיד. אתה מחזיק תשתית, פריסה, ניטור ועלות.

**מה לא שלך:** סכימה, מיגרציות, RLS, RPC. אתה **מגדיר מה אתה צריך** ואני
כותב. זה לא היררכיה לשמה — כל חוק עסקי חי במסד, ושינוי שם הוא שינוי מוצר.

**מה כן שלך, בלי לשאול:** בחירת פלטפורמת הרצה, CI, סודות, ניטור, גיבוי,
מדיניות מטמון, מדידת עלות.

---

## 1. המשימה

עמוד סטטוס חי אחד. קהל: מייסד (בטלפון), Tech Lead, וכל מי שבצוות.

**למה הוא קיים:** היום מצב המערכת מפוזר בין קובץ `ROUND-*.md` לבין שאילתות
SQL שרק אני מריץ. המייסד לא רואה את המספרים בלי לשאול.

**הוא מציג מספרים חיים בלבד.** לוח המשימות נשאר בקובץ — שני מקומות
שמתארים את אותו דבר זה הבאג שרדפנו אחריו כל הסבב הקודם.

### המדדים, לפי סדר חשיבות

| # | מדד | הערה |
|---|---|---|
| 1 | **חציון דקות מפרסום משמרת עד אישור עובד** | **מדד הצפון.** גדול, למעלה, לבד |
| 2 | משמרות שאוישו במלואן / שפורסמו — 7 ימים ו-30 יום | |
| 3 | משמרות פתוחות כרגע, מהן דחופות | |
| 4 | עסקים פעילים · עסקים מאומתים · עסקים בלי סיכה | האחרון הוא חוב ידוע |
| 5 | עובדים עם פרופיל מלא / סה״כ נרשמים | פרופיל חסר = לא יכול להגיש |
| 6 | מועמדויות לפי מצב: applied · offered · confirmed · declined | |
| 7 | **מבחן שלמות — מספר שורות שאינן אפס** | ירוק/אדום. הפירוט בלחיצה |
| 8 | ראש המיגרציות + חותמת זמן | תופס "בדקתי snapshot ישן" |

---

## 2. אילוצים

1. **עלות אפס.** free tier בלבד. שירות בתשלום — אישור מייסד עם מספר.
2. **בלי vendor lock-in.** העמוד ימות בלי להשאיר שובל.
3. **בלי npm ב-front-end.** אותו כלל כמו `app.html`. build לצד שרת — מותר.
4. **הדף לא מחזיק נתונים בקוד.** קריאה חיה בכל טעינה. מספר שנכתב ידנית
   בקוד הוא מספר ששקרי תוך יומיים.
5. **אין PII בעמוד.** לא שמות, לא טלפונים, לא מזהי משתמש. **אגרגטים בלבד.**
6. **העמוד לא פומבי.** נתוני נפח הם מידע עסקי. הגנה — בחירתך (Cloudflare
   Access, טוקן משותף, Basic). **תנמק, ואל תסתמך על סודיות ה-URL.**

---

## 3. הגישה לנתונים — קרא לפני שאתה מתכנן

**אל תתחבר למסד עם `service_role`.** מפתח כזה עוקף RLS לגמרי; בעמוד
שמונגש לדפדפן זו דלת אחורית לכל הנתונים.

**מצב RLS היום, אמת שבדקתי:** `cities` ו-`city_names` פתוחות ל-`anon`.
`businesses`, `shifts`, `applications`, `profiles` — מדיניות הקריאה שלהן
היא `to authenticated`, כלומר **`anon` לא רואה מהן כלום**. לכן שאילתה
ישירה מהעמוד לא תחזיר דבר, וזה תקין.

**הפתרון הנכון:** RPC יחיד, `SECURITY DEFINER`, שמחזיר **אך ורק אגרגטים**
ופתוח ל-`anon`. אני כותב אותו. **אתה מספק לי את המפרט:** שם, שדות
ההחזרה המדויקים, וטיפוס לכל שדה. תגיש אותו כטבלה, לא כפסקה.

**המפתח הפומבי (`anon`) נועד לשבת בדפדפן** — RLS מגן, לא הסתרת המפתח.
אל "תאבטח" בהסתרתו.

---

## 4. תשתית

לפי הרישום שלנו: חשבון Cloudflare מחובר (Workers/D1/KV/R2 זמינים דרך
MCP; **אין כלי DNS ואין Pages** — פעולות דומיין הן ידניות של המייסד).
Supabase `mishmeret`, ref `rkmjggormlxcvzuqedig`, free tier.
GitHub: ריפו פרטי `mak-bag/mishmeret`. git הותקן במכונה ב-20/09;
**עדיין אין remote מוגדר** ודחיפה ראשונה ממתינה למייסד.

**אמת את כל זה בעצמך לפני שאתה מתכנן.** רישום ישן הוא הדרך הקלה ביותר
לתכנן על תשתית שאינה קיימת.

---

## 5. תקן העבודה כאן

1. **מה שלא נכתב לקובץ — לא קרה.** בריף, ממצא, החלטה.
2. **מחזיק אחד לקובץ בכל רגע**; אני קובע מי. `app.html` אינו שלך.
3. **"הקוד נראה נכון" אינו אימות.** מריצים, מודדים, מדווחים מספר.
4. **בדיקת RLS בלי `set local role authenticated` רצה כ-postgres,**
   עוקפת הכול, ו"עוברת" בלי לבדוק כלום. זו הטעות המסוכנת בפרויקט.
5. **בלוק חפיפה בסוף כל יחידת עבודה:** DONE (מה אומת ואיך, לא מה נוסה),
   STATE, NEXT, FOR YOU, MUST KNOW, OPEN. דיווח מנופח נתפס ומוחזר.

**קוד:** שמות מלאים, בלי קיצורים. הערה מסבירה **למה**, לא מה. כל ערך
שיכול להיכשל — נבדק. בלי `catch` ריק. בלי `any`. בלי תלות שאינה נדרשת.

---

## 6. סדר מסירה

**שלב א — מפרט, לא קוד.** עמוד אחד:
- טבלת שדות ה-RPC שאתה צריך ממני.
- פלטפורמת ההרצה + נימוק.
- שיטת ההגנה + נימוק.
- עלות חודשית צפויה, כולל מה קורה כשהתנועה תוכפל פי עשר.
- מה יישבר כשמישהו יוסיף טבלה ולא יעדכן את ה-RPC.

**אני מאשר, כותב את ה-RPC, ואז אתה בונה.**

**שלב ב — העמוד.** גמור =
1. נטען מטלפון בפחות משתי שניות ברשת סלולרית.
2. מצב כהה ובהיר; המערכת קובעת. עברית, RTL.
3. המסד למטה — הדף מציג "לא זמין" ולא קורס ולא מציג אפס.
4. אפס PII, אומת בלשונית הרשת.
5. אפס שגיאות קונסולה.
6. העלות מדודה, לא מוערכת.

---

## 7. מה מחוץ לגבול

- **לוח המשימות.** נשאר ב-`ROUND-*.md`.
- **התראות וניטור אקטיבי.** סבב נפרד, אחרי שהעמוד עומד.
- **גרפים היסטוריים.** אין לנו סדרת זמן ולא נתחיל לצבור אחת בשביל עמוד.
  מספרים נוכחיים בלבד.
- **`app.html`.** לא שלך, ולא בסבב הזה.

---

## 8. לקרוא לפני שאתה כותב שורה

`HANDOVER-TECHLEAD.md` · `ROUND-M-MAP.md` (טבלאות ההכרעות D1–D18) ·
`ROADMAP-AND-HIRING.md` §3 — שער הכניסה לחנות. **המדד בשער הוא המדד
שהעמוד שלך מציג. זו הסיבה שהוא קיים.**
