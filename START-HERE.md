# START HERE — מה לעשות עכשיו
**עודכן: 21 בספטמבר 2026, ערב · Tech Lead**

**זה דף השיגור, לא מקור האמת.** מצב מלא: `ROUND-N-POLISH.md`.
הכרעות: `ROUND-N-DECISIONS.md`. אם יש סתירה — הם גוברים על הדף הזה.

---

## חדשות שנוגעות לכולם

**הקוד בגיטהאב.** `main` = `7480da3`, 49 קבצים, כולל `assets/` ושני
קובצי המקור. ההיסטוריה הישנה נשמרה במיזוג ולא נדרסה. זו הפעם הראשונה
שהפרויקט קיים מחוץ למכונה אחת.

**מה זה משנה לך:** יש עכשיו `git` במכונה. `app.html` שנשבר ניתן לשחזור.
אבל **הנעילה לא בוטלה** — עדיין מחזיק אחד לקובץ בכל רגע, ואני קובע מי.

---

## Developer — אתה משוחרר, לך

1. **שבע המחרוזות נזרעו.** `terms` = 312 שורות `ui`, אף קוד בלי שפה.
2. **שינוי אחד מהבקשה שלך:** `ui.bucket_weekend` → `ui.bucket_thisweek`
   ("השבוע"). צדקת שהגבול `d<=6` מייצר תווית שקרית ביום ראשון.
3. **מה שנוסף לך בגלל זה:** בדליים "השבוע" ו"בהמשך" השורה מציגה רק
   שעה. **הוסף יום בשבוע לשורות בשני הדליים האלה בלבד**, דרך
   `toLocaleDateString('he-IL',{weekday:'short'})` — לא מחרוזת חדשה.
4. **`.has-act` על כל שורה בדלי שיש בו פעולה**, לא רק על שורות עם
   כפתור. ההוראה הקודמת שלי הייתה שגויה; המעצב מדד קפיצה של 164px.
   `.c-act` — רק היכן שאפשר להגיש.
5. חמש המחלקות שביקשת אישור עליהן נכנסו לחוזה. יפה שעצרת לשאול.

**סיים בבלוק חפיפה** — מה אומת ואיך, לא מה נוסה.

---

## Designer — שתי ההכרעות שלך אושרו

`152px` ו-`.has-act` על כל שורות הדלי. שתיהן תיקנו הוראה שלי, ושתיהן
נתמכו במדידה על הקובץ האמיתי. זה בדיוק מה שאני רוצה שיקרה כשאני טועה.

**מה שנשאר לך, אחד:** המפתח מוסיף יום בשבוע לשורות ב"השבוע" ו"בהמשך".
**איפה הוא יושב בתוך רצועת 58px — שלך.** אל תיתן לגובה השורה לזוז.

ואחרי ההדבקה: **מצב בהיר**. לא אישרתי אותו במו עיניי במוקאפ.

---

## QA — הממצא שלך היה נכון, התיקון שהצעת היה מסוכן

`is_adult` אכן הייתה פתוחה, והבעיה רחבה ממה שדיווחת — **כל** פונקציה
ב-`private` נשאה execute ל-PUBLIC.

**אבל "ה-revoke בטוח" לא היה נכון.** אחת-עשרה מדיניות RLS קוראות
לפונקציות האלה, ומדיניות מוערכת בתור התפקיד השואל. הסרה גורפת הייתה
מחזירה permission denied על חמש טבלאות. תוקן לפי פונקציה ולא לפי
סכימה, ואומת: הטבלאות נקראות, `is_adult` מסורבת.

**דיוק חומרה:** ה-API חושף רק `public`. זו הייתה הרשאה שגויה, לא
דליפה פעילה מהאינטרנט. ממצא מנופח שוחק את הבא אחריו.

**עכשיו:**
1. מסלול ההצלחה של `verify_phone` — **הרצתי בשבילך**, עם rollback.
   אחרי: 0 מאומתים, 0 מפעילים.
2. **N-Q1 — כן, בכוונה**, ומעולם לא היה מסלול עובד: המדיניות היא
   `to authenticated`, אז `anon` קיבל אפס שורות גם עם ה-EXECUTE.
   עדכן את §4 בתיעוד שלך.
3. **N-F2** — אם יש משפט לא מדויק בהערה, **צטט אותו**. סיווג בלי ציטוט
   הוא מה שסגרנו בסבב הקודם.
4. **כשהמפתח מוסר:** סכום השורות בכל הדליים מול `boardShifts()`, ו-
   `.c-act` רק בדלי היום. **קיבוץ שמאבד שורה הוא הכשל הצפוי כאן.**

---

## DevOps — הגיבוי נפתח חלקית

**N8 בוצע: הקוד על `main`.** `backup.yml` יושב שם בגרסת 8.8KB שלנו,
לא בגרסת 13 השורות הישנה. **התזמון רץ מענף ברירת המחדל — כלומר הוא
חי מעכשיו.**

**מה שעדיין חסום: R2 (N9).** פעולת מייסד.

**מה שבידיך עכשיו:**
1. לאמת שהריצה הראשונה של `backup.yml` באמת מתרחשת ומה היא מחזירה.
   **זו ההזדמנות הראשונה לבדוק את זה באמת** — עד היום היא לא רצה מעולם.
2. ה-hash נרשם (`dde8b829…`). הטעות הייתה שלי.
3. **שלח לי את 36 השדות** — זה מה ש-N5 ממתין לו, לא ה-hash.

---

## Tech Lead — מה אני עושה, כדי שתדעו על מה לא לחכות לי

1. **36 השדות של `status_snapshot`** ל-DevOps. זה מה שחוסם את N5.
2. **סכימת הזמינות המוצהרת** — יום, שעות, תפקידים, רדיוס. זו התשתית
   להתראות, ובלעדיה התראה היא ספאם.
3. **אכיפת `phone_verified`** — מוחזקת עד שיהיה מפעיל ולפחות חשבון
   אחד מאומת. לא נדלקת בהפתעה.
4. **נעילת הסיכה** — מוחזקת עד שעסק אחד נקבע דרך האפליקציה האמיתית.

---

## תפקידים שעוד לא אוישו

**מפעיל פיילוט** — הגיוס הבא, ולפני כל מתכנת. אפס משמרות אוישו דרך
המערכת אי פעם; זו לא בעיית קוד. `ROADMAP-AND-HIRING.md`.
**מהנדס מובייל** — הפרומפט מוכן ב-`MOBILE-BRIEF.md`, ומחכה לזמינות
מוצהרת ולנפח.

---

## Founder — מה שנשאר

| | פעולה | מה זה פותח |
|---|---|---|
| ~~N8~~ | ~~דחיפה לגיטהאב~~ | **בוצע** |
| N9 | הפעלת R2 בדשבורד Cloudflare | שלב 2 של הגיבוי |
| N10 | סימון הסיכה של "ביסטרו נוטר" | מדליק את נעילת הסיכה |
| N11 | להוסיף אותך כמפעיל ראשון | בלי זה מנגנון האימות תיאורטי |

---

## שלושה כללים. הם לא נימוס

1. **מה שלא נכתב לקובץ — לא קרה.** שני ממצאי QA אבדו כך ונסגרו.
2. **מחזיק אחד לקובץ**, ואני קובע מי. שלושה עורכים במקביל עלו לנו חצי סבב.
3. **"עבר" נאמר רק על מה שהורץ.** מה שלא הורץ — אומרים שלא הורץ ולמה.
   דיווח מנופח נתפס פה בכל סבב עד כה.
