# DEV-BRIEF — סבב M: מסך המפה
**מאת: Tech Lead · 19 בספטמבר 2026, לילה · דורס כל DEV-BRIEF קודם**

קרא קודם את `HANDOVER-DEV.md`. הקובץ הזה מוסיף עליו סבב עבודה אחד, והוא עצמאי:
כל מה שצריך כדי לבנות נמצא כאן, כולל מה שכבר נסגר בשרת.

---

## 0. לפני שאתה נוגע בקובץ

```powershell
Get-Item "app.html" | Select-Object Length, LastWriteTime
```
נכון לכתיבת הבריף: **99,611 בתים, 19/09/2026 23:48**. אם המספר שונה — מישהו ערך
במקביל. עצור ותגיד.

`app.html` קיים **רק במחשב של המייסד**. זה לא באחריותך, אבל אל תתחיל סבב בלי
להזכיר לו לדחוף לגיטהאב.

**אל תערוך את הקובץ ב-PowerShell string replacement.** כלי Edit בלבד. זה כבר שבר
שישה template literals פעם אחת.

---

## 1. מה כבר עשיתי בשרת בשבילך (מיגרציות `20260919...`)

**אל תיישם מחדש בלקוח כלום מזה. זה נאכף.**

### 1.1 עמודה חדשה: `businesses.located_at timestamptz`
זו התשובה לשאלה "האם הנקודה אמיתית או שהיא סתם מרכז העיר".

- `null` → ה-`lat/lng` של העסק **הם מרכז העיר**, כי אף אחד לא סימן סיכה.
- לא-null → מישהו סימן סיכה בפועל.

הוקצתה למפרע: עסק שהנקודה שלו רחוקה מ-150 מ׳ ממרכז העיר סומן כמסומן.
היום: `ביסטרו בנמל` מסומן, `ביסטרו נוטר` **לא** — יש לו כתובת אמיתית
(אוסישקין 11) וסיכה במרכז נתניה. זה בדיוק הבאג שהמסך שלך סוגר.

הקריאה מותרת: `businesses` פתוחה ב-SELECT לכל מחובר, ברמת טבלה, אז העמודה
החדשה נקראת בלי שינוי הרשאות.

### 1.2 `create_business` — סיכה מפורשת מול נפילה למרכז
- שולחים `p_lat` **וגם** `p_lng`, או אף אחד מהם. חצי נקודה מחזירה `bad_location`.
- נשלחה נקודה → נבדקת מול 50 ק״מ מהעיר, ונרשם `located_at = now()`.
- לא נשלחה נקודה → מרכז העיר, `located_at` נשאר `null`.
- נקודה רחוקה מדי → `location_far`. **אל תבדוק את זה בלקוח.** תן לשרת לסרב
  ותציג `resultText('location_far')` — התרגום כבר קיים.

### 1.3 `update_business` — הזזת סיכה מזיזה גם את המשמרות
זה היה באג אמיתי, לא חוסר פיצ׳ר: `shifts.lat/lng` הם **צילום מצב** שנלקח
בזמן פרסום המשמרת, ו-`shifts.location` נגזר מהם. עד היום, מעסיק שמתקן את
הסיכה שלו תיקן רק משמרות עתידיות — המשמרת שכבר פורסמה נשארה בנקודה הישנה,
גם לניווט וגם לחישוב הרדיוס.

מהיום `update_business`:
- כותב `located_at = now()` כשנשלחה נקודה,
- מעדכן `lat/lng/biz_name` בכל משמרת של העסק ש**טרם הסתיימה ולא בוטלה**,
- **לא נוגע** במשמרות שהסתיימו — הן היסטוריית ההגעה, והיא לא נכתבת מחדש,
- מחזיר `{"result":"saved","shifts_moved":<n>}`.

אומת ב-rollback: בעלים הזיז סיכה → המשמרת החיה זזה איתו ו-`location` חושב
מחדש (0.0 מ׳ סטייה); שתי משמרות שהסתיימו שמרו על השם והנקודה הישנים;
זר שניסה להזיז קיבל `forbidden`; חצי נקודה קיבלה `bad_location`; נקודה
בבאר שבע מול עסק בנתניה קיבלה `location_far`.

### 1.4 מחרוזות `terms` — כבר זרועות, עברית ואנגלית
אל תכתוב מחרוזת חדשה בקוד. אלה הקודים שנוצרו בשבילך:

| קוד | עברית |
|---|---|
| `ui.map_view` / `ui.list_view` | מפה / רשימה |
| `ui.map_loading` | טוען מפה… |
| `ui.map_failed` | לא הצלחנו לטעון את המפה. אפשר לעבור לרשימה. |
| `ui.map_empty` | אין משמרות להציג על המפה בטווח שבחרתם. |
| `ui.map_radius_note` | הרדיוס נמדד ממרכז העיר שבחרתם, לא מכתובת הבית |
| `ui.map_my_city` | העיר שלכם |
| `ui.map_venue_shifts.one` / `.other` | משמרת אחת בעסק הזה / {n} משמרות בעסק הזה |
| `ui.approx_location` | מיקום משוער · מרכז העיר |
| `ui.pin_title` | מיקום מדויק על המפה |
| `ui.pin_hint` | גררו את הסיכה לכניסה לעסק, או לחצו על המפה. |
| `ui.pin_why` | הסיכה קובעת לאן מנווטים ומאיזו נקודה נמדד הרדיוס. כתובת בלבד לא מספיקה. |
| `ui.pin_unset` | עדיין לא סומן מיקום. הסיכה יושבת במרכז העיר. |
| `ui.pin_set` | המיקום סומן |
| `ui.pin_city_first` | בוחרים עיר, ואז מסמנים על המפה |
| `ui.pin_reset` | החזרה למרכז העיר |
| `ui.pin_moved_shifts.one` / `.other` | המיקום עודכן גם במשמרת אחת / ב־{n} משמרות שטרם התקיימו |

`ui.map_venue_shifts` ו-`ui.pin_moved_shifts` נושאות מספר → דרך `plural()`.
חסרה לך מחרוזת? **תבקש ממני, אל תכתוב אותה בקוד.**

---

## 2. תיקון מקדים שחייב לקרות לפני שתי המשימות

**`loadReference` לא מביא קואורדינטות של ערים, ואתה צריך אותן בשני המסכים.**

`app.html:648` — הוסף `lat, lng` ל-select, ו-`app.html:651` — שמור אותם באובייקט:

```js
sb.from('cities').select('id, slug, lat, lng, city_names(lang,name)').order('id')
...
S.cities = (cities||[]).map(c => ({
  id: c.id,
  lat: c.lat, lng: c.lng,
  name: (c.city_names||[]).find(n=>n.lang==='he')?.name || c.slug
})).sort(...)
```

`cities` פתוחה לקריאה גם ל-anon. אין כאן סיכון.

**וכן `loadBiz` (`app.html:738`)** — הוסף `lat, lng, located_at` ל-select של העסקים,
אחרת טופס העריכה לא יודע איפה להעמיד את הסיכה.

---

## 3. משימה A — סימון סיכה בכרטיס העסק

**איפה:** `newBizScreen(edit)` (`app.html:994`), `onBizForm` (`app.html:1585`).

### 3.1 המיקום בטופס
במצב **פתיחה**: אחרי שדה העיר, לפני סוג העסק.
במצב **עריכה**: אחרי שדה הכתובת. (בעריכה אין שדה עיר — היא נעולה.)

```html
<label>${esc(ui('ui.pin_title'))}</label>
<div class="note">${esc(ui('ui.pin_why'))}</div>
<div id="bizMap" class="map map-pick"></div>
<div class="hint" id="pinState"></div>
<input type="hidden" id="b_lat" name="lat" value="">
<input type="hidden" id="b_lng" name="lng" value="">
```

### 3.2 התנהגות
1. **פתיחה, לפני בחירת עיר**: המפה לא נטענת. במקומה פאנל עם `ui.pin_city_first`.
   ברגע ש-`cityField` פותר שם→מזהה, המפה נטענת וממורכזת על מרכז העיר, zoom 14.
   שינוי עיר → מרכוז מחדש ואיפוס הסיכה.
2. **סיכה התחלתית**: במרכז העיר, אבל **מסומנת כלא-נקבעה** (`ui.pin_unset`,
   ויזואלית עמומה — ראה DESIGN-BRIEF). השדות הנסתרים **ריקים**.
3. **המשתמש גרר או לחץ על המפה** → הסיכה עוברת, מקבלת מראה מלא, `ui.pin_set`,
   והשדות הנסתרים מתמלאים ב-6 ספרות אחרי הנקודה.
4. **כפתור `ui.pin_reset`** — **רק במצב פתיחה.** מחזיר את הסיכה למרכז, מרוקן את
   השדות, חוזר ל-`ui.pin_unset`. בעריכה אין כפתור כזה: אי אפשר לבטל
   `located_at` בשרת, וכפתור שנראה כאילו הוא מבטל הוא שקר.
5. **עריכה**: יש `located_at` → סיכה מלאה ב-`lat/lng` של העסק, `ui.pin_set`,
   השדות הנסתרים מלאים מראש. אין `located_at` → סיכה עמומה במרכז, שדות ריקים.
6. **עיגול 50 ק״מ** סביב מרכז העיר, מקווקו, בלי מילוי. **הוא מציג את הגבול,
   הוא לא אוכף אותו.** אל תחסום גרירה מחוצה לו — תן לשרת להחזיר `location_far`.

### 3.3 השליחה
ב-`onBizForm`, בשני הענפים:

```js
const lat = f.get('lat'), lng = f.get('lng');
const pin = (lat && lng) ? { p_lat:+lat, p_lng:+lng } : {};
```
ואז `...pin` בתוך אובייקט הפרמטרים.

**שלח שניהם או אף אחד. לעולם לא אחד.** שדה ריק אחד = `bad_location` מהשרת.

**אל תשלח את מרכז העיר כסיכה.** אם המשתמש לא נגע — אל תשלח כלום. הסיכה
במרכז העיר היא ברירת מחדל של השרת; אם תשלח אותה, `located_at` ייכתב והמערכת
תטען שהמיקום מדויק כשהוא לא. זו הנקודה היחידה בסבב הזה שאם תפספס אותה,
כל השאר נהיה שקר מדיד.

### 3.4 אחרי שמירה מוצלחת בעריכה
`data.shifts_moved` מגיע מהשרת. אם הוא > 0, הוסף אחרי הטוסט הרגיל:
`toast(plural('ui.pin_moved_shifts', data.shifts_moved))`.
זה לא קישוט — מעסיק שמזיז סיכה צריך לדעת שהוא הזיז גם משמרת שכבר פורסמה.

---

## 4. משימה B — מפת המשמרות לעובד

**איפה:** `boardScreen()` (`app.html:963`), `render()` (`app.html:~1340`), `wire()` (`app.html:1374`).

### 4.1 מקור הנתונים — אין RPC חדש ולא צריך
`shifts_for_me` מחזיר `SETOF shifts`, כלומר **כולל `lat` ו-`lng`**. הן כבר
ב-`S.shifts`. המפה מציירת בדיוק את מה שהרשימה מציגה, לא שורה אחת יותר.

**חובה: חלץ את הסינון הקיים לפונקציה אחת** —
```js
function boardShifts(){
  return S.shifts.filter(s =>
    !S.mine.some(m => m.shift_id === s.id) &&
    !S.bizList.some(b => b.id === s.business_id));
}
```
ושתי התצוגות קוראות לה. אם המפה תסנן בנפרד, יהיו שני מקורות אמת ומשמרת של
העסק של המשתמש עצמו תופיע על המפה אחרי שהוסתרה מהרשימה.

### 4.2 המתג
בתוך ה-`.bar` שמעל הרשימה, לצד מחוון הרדיוס: שני כפתורים, `ui.list_view` /
`ui.map_view`, הפעיל צבוע מלא (**`--accent-solid`**, לא `--accent` — ראה מלכודת 5).
מצב ב-`S.view`, נשמר ב-`localStorage` תחת `mishmeret-board-view`.

### 4.3 טעינת Leaflet — עצלה
Leaflet 1.9.4 מ-CDN, **נטען רק בפתיחה הראשונה של המפה**, לא בטעינת האפליקציה.
CSS ו-JS מוזרקים דינמית, עם `Promise` יחיד שנשמר כדי שלא ייטען פעמיים.
בזמן הטעינה: `ui.map_loading`. נכשל: `ui.map_failed` + חזרה אוטומטית לרשימה
ושמירת `S.view='list'`. האפליקציה לא נשברת בלי אינטרנט לספק ה-CDN.

### 4.4 סימון על המפה
- **קיבוץ לפי עסק, לא לפי משמרת.** קבץ לפי `` `${lat.toFixed(6)},${lng.toFixed(6)}` ``.
  ל-`ביסטרו בנמל` יש שתי משמרות באותה נקודה — בלי קיבוץ הן יושבות זו על זו
  ואחת מהן בלתי-לחיצה.
- **סמן** = `L.divIcon` עם HTML שלנו (ראה DESIGN-BRIEF), לא תמונת ברירת המחדל
  של Leaflet: משמרת אחת → `₪<rate>`; יותר → מספר המשמרות. אין אימוג׳י.
- **פופאפ**: שם העסק, כתובת, תג "עסק מאומת"/"לא מאומת" — בדיוק כמו בכרטיס —
  ואם לעסק `located_at is null` גם תג `ui.approx_location`. מתחת:
  `plural('ui.map_venue_shifts', n)` ושורה קומפקטית לכל משמרת (תפקיד, שעות,
  תעריף) עם **אותו** `<button class="btn" data-claim="${s.id}">`.
  האצלת הלחיצות היא `el('view').onclick` (`app.html:1420`), ומיכל המפה יושב
  בתוך `#view` — אז הכפתור בפופאפ עובד בלי חיווט נוסף. **אל תוסיף מאזין משלך.**
- **מרכז העיר של העובד**: סמן דהוי נפרד + `ui.map_my_city`, ועיגול רדיוס
  מקווקו לפי `S.radius`. מתחת למפה, קבוע: `ui.map_radius_note`.
  זה חשוב ולא קוסמטי — הרדיוס נמדד ממרכז העיר, לא מהבית של העובד, והמפה
  היא המקום היחיד שבו אי-ההבנה הזו נהיית גלויה.
- **אין קואורדינטות לעובד. בשום מקום.** לא geolocation, לא "מצא אותי", לא
  שליחה לשרת. העובד הוא עיר ורדיוס, נקודה.
- מפה ריקה → `ui.map_empty` בתוך המפה או מתחתיה (העיגול עדיין מוצג).
- **גבולות**: `map.fitBounds` על הסמנים + מרכז העיר, עם `padding` ו-`maxZoom:15`.
  סמן יחיד → `setView(zoom 14)`.

### 4.5 המלכודת שתשבור לך את המסך אם תתעלם
`render()` דורס את `#view.innerHTML` **אחרי כל פעולה**. מופע Leaflet ששרד את
הדריסה הוא דליפת זיכרון, והניסיון הבא ליצור מפה על אותו מזהה זורק
`Map container is already initialized`.

```js
let boardMap = null, pickMap = null;
function killMaps(){
  if(boardMap){ boardMap.remove(); boardMap = null; }
  if(pickMap){  pickMap.remove();  pickMap  = null; }
}
```
`killMaps()` נקראת **בתחילת `render()`, לפני** הצבת ה-innerHTML. יצירת המפה
נעשית ב-`wire()`, אחרי שה-DOM קיים — לא בתוך מחרוזת ה-HTML.

שים לב ש-`render()` מופעל גם על גרירת מחוון הרדיוס. ודא שגרירה לא הורסת
את המפה מתחת לאצבע — אם היא כן, זה מורגש מיד.

### 4.6 פרמטרים של המפה
`scrollWheelZoom:false` (אחרת גלילת הדף נחטפת בדסקטופ), `zoomControl` בפינה
התחתונה, גובה `min(62vh, 560px)` ולפחות 320px, פינות מעוגלות כמו `.card`.
אריחים: `https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`, `maxZoom:19`,
**עם ה-attribution של OSM — זה תנאי שימוש, לא קרדיט נחמד.**

---

## 5. תיקון נלווה — יעד הניווט

`wazeUrl` (`app.html:864`) מעדיף היום את הכתובת החופשית על הקואורדינטה,
והערת הקוד שמעליה אומרת ש-lat/lng הם "מרכז העיר". **מהיום זה נכון רק
כש-`located_at` הוא null.**

הכלל החדש:
- לעסק יש `located_at` → **נווט לקואורדינטה** (`?ll=`). סיכה שהמעסיק סימן
  בעצמו אמינה יותר מכתובת שאף אחד לא אימת.
- אין `located_at` → נווט לכתובת (`?q=`), כמו היום.
- אין גם וגם → אין קישור ניווט.

כדי שזה יעבוד, `loadBoard` (`app.html:713`) צריך גם `located_at`:
```js
.select('id, address, city, verified_at, lat, lng, located_at')
```
ושמור אותו ב-`S.bizAddr[b.id]`. **עדכן גם את הערת הקוד מעל `wazeUrl`** —
היא מתעדת החלטה שהשתנתה, ותטעה את מי שיקרא אותה אחריך.

---

## 6. הגדרת "גמור"

לא מדווחים גמור לפי קוד שנראה נכון. הרשימה הזו כולה, בדפדפן חי:

1. שרת מקומי (`serve.ps1`), לא `file://`.
2. **פתיחת עסק בדיקה** בשם `QA מסעדת מפה` דרך הממשק, עם סיכה שנגררה.
   ואז במסד: `select name, lat, lng, located_at from businesses where name like 'QA %'`
   — `located_at` **חייב** להיות לא-null והנקודה **חייבת** להיות שונה ממרכז העיר.
3. **פתיחת עסק שני** בלי לגעת בסיכה → `located_at` **חייב** להיות null.
   (אחרי הבדיקה תסגור אותו; מוסכמת הניקוי ב-`HANDOVER-DEV.md` §6.)
4. **גרירת סיכה לעסק קיים עם משמרת עתידית** → הטוסט מדווח על המשמרת שזזה,
   ובמסד `shifts.lat` של אותה משמרת שווה לסיכה החדשה.
5. **סיכה רחוקה** (גרור לעיר אחרת) → הטופס מציג "הנקודה שסומנה רחוקה מדי
   מהעיר שנבחרה", והעסק לא נשמר.
6. **לוח העובד** עם חשבון `0509998877`: מעבר רשימה↔מפה, אותן משמרות בדיוק
   בשתי התצוגות. ספור אותן.
7. **לחיצה על "הגשת מועמדות" מתוך פופאפ** יוצרת שורה ב-`applications`. תבדוק
   במסד, לא לפי הטוסט.
8. **שתי משמרות באותו עסק** → סמן אחד עם "2", שתי שורות בפופאפ.
9. **גרירת מחוון הרדיוס במצב מפה** → העיגול והסמנים מתעדכנים, אין שגיאה
   בקונסולה, אין `Map container is already initialized`.
10. **מצב כהה** — בעיניים, בשתי הווריאנטות (ראה §מלכודות ב-HANDOVER-DEV).
    אריחי OSM בהירים; הפתרון בידי המעצב — **אל תמציא פילטר משלך, קח מ-DESIGN-BRIEF.**
11. **קונסולה נקייה.** אפס שגיאות, אפס אזהרות Leaflet.
12. **ניקוי**: מחק כל ישות `QA ` שיצרת, לפי סדר המחיקה. הרץ שוב את הבדיקה
    שאין יתומים.

---

## 7. מה מחוץ לגבול הסבב הזה

- **חיפוש "באזור הזה"** (גרירת מפה → משמרות באזור החדש). דורש RPC חדש ופותח
  שאלת פרטיות. **אל תבנה.** אם המפה מרגישה חסרה בלעדיו — תגיד לי, זה הסבב הבא.
- **חובת סיכה לפני פרסום משמרת.** זו הכוונה, אבל היא נדלקת רק אחרי שהמסך
  שלך עולה ועובר QA — אחרת נועלים מעסיק בלי מסלול. אני מפעיל, לא אתה.
- **geocoding של כתובת.** אין. לא בתקציב ולא בסבב הזה.
- **זמינות מוצהרת של עובד.** סכימה עוד לא נמסרה. אל תתכנן אותה בעצמך.

---

## 8. סיום

סיים בבלוק החפיפה (פורמט ב-`HANDOVER-DEV.md` §9), ל-**Tech Lead**.
ב-`DONE` כתוב מה **אומת** ואיך, לא מה נוסה. אם סעיף מ-§6 לא רץ — תגיד איזה
ולמה. סבב עם סעיף שלא נבדק ולא דווח הוא סבב שאני צריך לבדוק מחדש מאפס.
