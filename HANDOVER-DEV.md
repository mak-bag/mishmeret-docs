# תיק חפיפה — Developer · משמרת
**נכון ל‑19 בספטמבר 2026, סוף יום. הקובץ עצמאי: אין צורך בשום היסטוריה קודמת.**

אתה המתכנת הבא. הקובץ הזה נותן לך את המצב האמיתי, לא את המצב המתוכנן.
כל מה שכתוב כאן אומת מול המסד או מול הדפדפן באותו יום. היכן שמשהו
לא אומת — זה נאמר במפורש.

---

## 0. איך הצוות עובד

| תפקיד | מי | סמכות |
|---|---|---|
| **Founder** | ולדי | קובע מוצר ועדיפויות. לא מתכנת — מסבירים לו קצר ובעברית, בלי ז׳רגון. |
| **Tech Advisor / Tech Lead** | סשן נפרד | **מחליט** ארכיטקטורה, סכימה, מיגרציות, RLS. כותב את ה‑RPC. |
| **Developer** | **אתה** | בונה את `app.html` בלבד. לא כותב מיגרציות ולא משנה סכימה. |
| **QA** | סשן נפרד, **קריאה בלבד** | מבקר. **לא עובד מולך ישירות** — ממצאיו עוברים דרך ה‑Advisor. |

**כללי הזהב של ההיררכיה:**
- צריך שינוי סכימה? **אל תאלתר.** תגיד ל‑Advisor מה חסר ולמה, והוא יחליט.
- QA שולח לפעמים בדיקות כתיבה שהוא לא יכול להריץ בעצמו. תריץ בדיוק כפי שנכתבו, תדווח תוצאה, ותנקה אחריך.
- כל יחידת עבודה נגמרת בבלוק חפיפה (פורמט בסוף הקובץ).
- אם הגיע אליך brief שמפנה ל"בלוק למטה" שלא צורף — **תגיד את זה במפורש** ותעבוד לפי מה שיש. זה קרה שלוש פעמים ביום אחד.

---

## 1. המוצר בשלוש שורות

שוק שמחבר מסעדות בישראל לעובדי משמרות: ברמן, עובד שטיפה, מלצר, טבח, איש פלור.
הערך הוא סגירת נפילה ברגע האחרון — מסעדה שנשארה בלי שוטף ב‑18:00 מוצאת מישהו תוך דקות.
**המדד היחיד שחשוב: דקות מרגע פרסום המשמרת ועד עובד מאושר.**

**מה אנחנו לא:** חברת לידים שמחברת בין שני צדדים, **לא חברת כוח אדם** — קטגוריה
מורשית בישראל. המערכת **לעולם לא משבצת אוטומטית**: אדם לוחץ "אני לוקח" ואדם
לוחץ "מאושר". אל תבנה שיבוץ אוטומטי, דירוגי מיומנות, תעודות או תנאי העסקה.
חמש קטגוריות תפקיד, זהו.

---

## 2. תשתית וכלי עבודה

- **Supabase**: פרויקט `mishmeret`, ref `rkmjggormlxcvzuqedig`, Postgres 17, free tier. חיבור MCP זמין.
- **המפתח הפומבי נועד לשבת בדפדפן.** RLS הוא מה שמגן, לא הסתרת המפתח.
- **GitHub**: ריפו פרטי `mak-bag/mishmeret`. **אין git במכונה** ואין הרשאות דחיפה — העלאה דרך ממשק הווב של GitHub, וזו פעולה של המייסד. `main` מחזיק כרגע snapshot ישן (`0736654`); הגרסה הנוכחית **לא שם**.
- **הרצה לבדיקה**: חובה HTTP, לא `file://` (מודולי ES לא ייטענו). אין node ואין python אמיתי במכונה. יש `serve.ps1` בתיקיית ה‑scratchpad — שרת `HttpListener` ב‑PowerShell, עובד בלי הרשאות מנהל:
  ```
  powershell -NoProfile -ExecutionPolicy Bypass -File <scratchpad>\serve.ps1 -Root "<תיקיית הפרויקט>" -Port 8080
  ```
- **בדיקה אמיתית = דפדפן + מסד.** לא מדווחים הצלחה כי הקוד נראה נכון. פותחים את הדף, מבצעים את הפעולה, ואז בודקים במסד שהשורה נכתבה.
- **בדיקות צד־שרת בהתחזות** (כך בודקים RLS באמת):
  ```sql
  select set_config('request.jwt.claims','{"sub":"<uuid>","role":"authenticated"}', true);
  set local role authenticated;
  ```
  **בלי `set local role authenticated` אתה רץ כ‑postgres, עוקף RLS לגמרי, וכל בדיקה "עוברת" בלי לבדוק כלום.** זו הטעות הכי מסוכנת בפרויקט הזה.
  שים לב: `DO` block שנכשל מגלגל אחורה גם את מה שנוצר בתוכו, ואחרי `set local role` צריך `reset role` לפני פעולות שדורשות postgres.

---

## 3. מה קיים היום ב‑`app.html`

קובץ יחיד, ~92KB, ~1,290 שורות, בלי build ובלי npm. מודול ES אחד עם `createClient` מ‑jsdelivr.

**מסכי עובד**
- הרשמה (3 שדות: טלפון, שם, עיר) וכניסה — טלפון + סיסמה.
- **השלמת פרטים** — שם פרטי, שם משפחה, עיר, תאריך לידה. חוסם: בלי זה `claim_shift` מסרב. תאריך לידה נכתב פעם אחת ואז השדה נעול.
- **לוח המשמרות** — מ‑`shifts_for_me`, מחוון רדיוס, כתובת מדויקת, טלפון, ניווט, תג 18+, תג אימות עסק.
- **"המשמרות שלי"** — מ‑`my_shifts()`. כולל משמרות שבוטלו, עם סיבת הביטול.

**מסכי מעסיק**
- פתיחת עסק ועריכתו (שם, סוג, כתובת, טלפון; עיר ננעלת אחרי הפתיחה).
- פרסום משמרת ועריכתה — אותו טופס, כולל תיבת 18+ שננעלת על ברמן.
- מסך משמרת: מועמדים עם היסטוריית הגעה, טלפון, "לדבר לפני אישור", אישור, דחייה, ביטול, הוספה לצוות קבוע.
- ביטול משמרת עם סיבה + **UNDO** (חלון שתי דקות בשרת).
- סגירת עסק (בעלים בלבד), בורר עסקים כשיש יותר מאחד.

**חוצה מסכים**
- **מתג מצב** תחתון: "אני מחפש עבודה" / "אני מגייס". הצד הפעיל צבוע מלא — לא לשוניות עדינות.
- מצב כהה/בהיר עם כפתור בכותרת, נשמר ב‑`localStorage`.
- כניסה למסך הפרטים דרך **השם בכותרת** — אין נתיב אחר אחרי שהפרופיל הושלם.

---

## 4. ה‑API שאתה בונה מולו

**טבלאות**: `profiles`, `profile_contacts`, `businesses`, `business_members`, `shifts`,
`shift_translations`, `applications`, `reviews`, `team_members`, `cities`, `city_names`, `terms`.

**מצבי מועמדות**: `applied` → `offered` → `confirmed`, ובנוסף `declined`, `cancelled`, `withdrawn`.
`shifts.filled` עולה רק ב‑`confirmed`, **והמסד מתחזק אותו — לעולם לא הלקוח**.

**RPC (כל הכתיבה עוברת דרכם):**
```
claim_shift(p_shift) -> text
accept_offer(p_shift) -> text
approve_application(p_shift, p_worker) -> text
offer_shift(p_shift, p_worker) -> text
release_application(p_shift, p_worker, p_status) -> text
complete_worker_profile(p_given, p_family, p_city_id, p_birth_date, p_radius_km) -> jsonb
my_profile() -> jsonb
my_shifts() -> TABLE(...)           -- כולל address, biz_phone, lat/lng, cancel_reason, adults_only
shifts_for_me(p_radius_km) -> SETOF shifts
shifts_near_city(p_city_id, p_radius_km) -> SETOF shifts
shift_applicants(p_shift) -> TABLE(... phone, is_adult, attended_*, shifts_done, is_regular)
create_business(p_name, p_city_id, p_address, p_phone, p_kind, p_lat, p_lng) -> jsonb
update_business(p_business, p_name, p_address, p_phone, p_kind, p_lat, p_lng) -> jsonb
close_business(p_business) -> jsonb
post_shift(p_business, p_role_code, p_starts, p_ends, p_rate, p_slots, p_instant,
           p_urgent, p_transport, p_descr, p_max_distance_km, p_exp_code, p_adults_only) -> jsonb
edit_shift(p_shift, ...אותם פרמטרים בלי p_business) -> jsonb
cancel_shift(p_shift, p_reason) -> jsonb
uncancel_shift(p_shift) -> jsonb
dispute_review(p_review, p_note) -> text
```

**מה הלקוח עדיין כותב ישירות (מכוון, מדיניות אוכפת):**
- `profiles.update({search_radius_km})` — עמודות מוגבלות ברמת grant.
- `team_members` insert/delete — דורש מועמדות `confirmed` קודמת באותו עסק.

**מה נשלל מהלקוח לתמיד**: UPDATE/DELETE על `applications` ו‑`shifts`, INSERT ל‑`businesses`,
כתיבה ל‑`profile_contacts`, DELETE על `businesses` ועל `reviews`, וקריאת `birth_date` מהטבלה.
אם אתה מוצא קוד שעושה את זה — **תמחק אותו, אל תתקן**.

---

## 5. כללים שמעצבים את הממשק — לא להתווכח איתם

1. **הלוגיקה במסד.** הלקוח קורא ל‑RPC ומצייר תוצאה. הוא לא מממש כללים מחדש ולא בודק מראש מה שהשרת אוכף.
2. **העובד לא מקליד את פרטיו שוב.** `claim_shift` מקבל מזהה משמרת בלבד.
3. **לעובד אין קואורדינטות בשום מקום** — לא בממשק ולא בבקשות. הוא בוחר עיר ורדיוס; השרת פותר את השאר. רק לעסק יש כתובת מדויקת.
4. **כל מחרוזת חדשה שהמשתמש רואה — ב‑`terms`, לא בקוד.** `kind='ui'`, קוד בפורמט `ui.*` או `result.*`, **תמיד עברית ואנגלית**. יש היום 260 שורות ui. עברית היא הבסיס; שפת המשתמש דורסת רק היכן שיש תרגום.
5. **מחרוזת שנושאת מספר מפוצלת ל‑`.one` ו‑`.other`** והלקוח בוחר (`plural()`). `{n}` חייב להישמר בכל השפות של אותו קוד. זה לא מכסה רוסית וערבית — אם נפתחת שפה כזו, זו החלטה של ה‑Advisor.
6. **קוד תוצאה מהשרת לעולם לא מוצג כמו שהוא.** `resultText(code)` מתרגם; קוד בלי תרגום נופל ל‑`ui.unknown_result`. אף פעם לא אנגלית ולא שגיאת Postgres גולמית.
7. **התחברות בטלפון בלבד.** אימייל לא מופיע באף מסך. הטלפון ממופה פנימית ל‑`972XXXXXXXXX@mak-bag.com`.
8. **`is_adult` והתגים הם הצהרה עצמית** — חייבים להיות מסומנים ככאלה בכל מקום שהם מופיעים.
9. **אין אימוג׳י בממשק.** יש סט אייקוני CSS (`.icon.icon-<name>` עם `mask-image`) שצובע `currentColor`. התו היחיד שנשאר הוא `✓`.
10. **אל תערוך את `app.html` בהחלפת מחרוזות מ‑PowerShell.** זה כבר קטע שישה template literals והשאיר JS שבור. כלי Edit בלבד.
11. **מצב כהה נבדק בעיניים, לא בהיגיון.** יש מלכודות ניגודיות מתועדות — כל משטח מלא עם טקסט לבן חייב `--accent-solid`, לא `--accent`.

---

## 6. מצב הנתונים כרגע (אמיתי, לא לדרוס)

| | |
|---|---|
| עסקים | 2 |
| משמרות | 3 |
| מועמדויות | 1 |
| צוות קבוע / ביקורות | 0 / 0 |
| משתמשים | 8 |
| ערים | 46 |
| שורות `terms` | 320 (מהן 260 `ui`) |

**נתונים אמיתיים של המייסד — אסור למחוק:** משתמש "ולדי בגרנובסקי" (`972527738638`),
עסק **"ביסטרו נוטר"** בנתניה (אוסישקין 11) ומשמרת ברמן ל‑25/09.

**חשבונות בדיקה פעילים** (אותה סיסמה, נוצרו דרך הממשק):
- `0501112244` — מעסיק, "מנהל בדיקה".
- `0509998877` — עובד, "עובד בדיקה", פרופיל מלא, יליד 1995, חיפה, רדיוס 20.
- `0501112233` — **לא מאושר, לא יכול להיכנס** (נוצר לפני שכיבו "Confirm email"). מיותר.

**מוסכמת ניקוי:** כל ישות בדיקה מתחילה ב‑`QA ` בשם. סדר מחיקה:
`applications` → `team_members` → `shifts` → `businesses` (העסק מוחק את `business_members` בקסקייד;
מחיקה ישירה של שורת הבעלים חסומה בטריגר).

---

## 7. מה פתוח — לפי סדר

### 7.1 חוסם אמיתי אחד: הקוד לא בגיבוי
`app.html` קיים **רק במחשב של המייסד**. הגיבוי הלילי מגבה את הריפו, לא את הדיסק.
פעולה של המייסד: `mak-bag/mishmeret` → Add file → Upload files → `app.html` + קבצי ה‑md + תיקיית `assets/`.
**תבקש את זה בתחילת הסשן, לא בסופו.**

### 7.2 הבקשה החמה של המייסד: דיוק מיקום
הוא ביקש במפורש שהלקוח יראה כתובת מדויקת. מה שקיים היום ומה שחסר:
- הכתובת היא **טקסט חופשי שהמעסיק מקליד**. אף אחד לא מאמת אותה ואין גיאוקודינג.
- `lat/lng` של עסק שנפתח דרך האפליקציה הם **מרכז העיר** — `create_business` נופלת לזה כשלא נשלחת נקודה, והלקוח לא שולח.
- **תוקן היום:** קישור הניווט נבנה מהכתובת (`?q=<address>, <city>`) ונופל לקואורדינטות רק כשאין כתובת.
- **מה שחסר: מסך סימון סיכה על מפה.** הצד השרתי מוכן — `p_lat`/`p_lng` ב‑`create_business` וב‑`update_business`, עם `private.point_near_city` שחוסם 50 ק״מ מהעיר. Leaflet + אריחי OpenStreetMap, בלי מפתח ובלי עלות. שדה העיר חייב לבוא **לפני** המפה, כי השרת דוחה סיכה רחוקה (`location_far`).
- **שאלה פתוחה שחייבת תשובה מה‑Advisor לפני שבונים:** האם הסיכה המדויקת מחליפה את מרכז העיר גם בחישוב הרדיוס, או שהרדיוס נשאר ברמת עיר? זה קובע אם `shifts.location` נגזר מהסיכה.
- **אזהרה:** אל תדחוף כתובת לתוך `lat/lng` ואל תנסה להמיר. אותן קואורדינטות מזינות את `shifts.location` ואת כל חישובי הרדיוס — שינוי שלהן משנה למי המשמרת בכלל מוצגת.

### 7.3 הפיצ׳ר הבא בתור: זמינות מוצהרת של עובד
יום בשבוע + שעות + תפקידים + רדיוס. זה מה שהופך את המוצר מלוח מודעות למנוע התאמה,
ותנאי מוקדם להתראות. **הסכימה עדיין לא נמסרה — אל תתכנן את הטבלה בעצמך.** תבקש מה‑Advisor.

### 7.4 פערים ידועים שנשארו פתוחים
- **`my_shifts()` מחזיר `address` רק ב‑confirmed**, אבל הלקוח כבר מצרף כתובת מ‑`businesses` בכל מצב. ההסתרה שם הפכה קוסמטית. צריך הכרעה: להסיר את התנאי ב‑RPC, או להחזיר הסתרה בלקוח.
- **תג "עסק מאומת" לא מופיע ב"המשמרות שלי"** — `my_shifts()` לא מחזיר `business_id` ולא `verified_at`, ואי אפשר לצרף. ביקשתי אחד מהשניים.
- **`verified_at` לא ניתן לקביעה מהממשק.** אין מסך אדמין; הוא נקבע ידנית במסד. יצטרך RPC.
- **אין מסך חברי עסק** — ולכן `result.last_owner` ו‑`members_insert` חסרי מסלול בממשק, ומנהל שאינו בעלים לא יכול להתווסף דרך האפליקציה.
- **הוספה לצוות היא עדיין INSERT ישיר** ולא RPC — החלטה מודעת של ה‑Advisor, המדיניות נושאת את הכלל.
- **אורך סיסמה מזערי 6.** בקוד `minlength="6"`. ההחלטה להעלות ל‑8 תלויה במייסד בהגדרות Auth; כשישתנה שם — לעדכן גם בקוד.
- **Leaked Password Protection לא זמין ב‑free tier.** יופיע לנצח ב‑advisor האבטחה. רעש מקובל, לא לדווח שוב.
- **`app.html` נערך במקביל גם על ידי סשן העיצוב.** קרה יותר מפעם אחת שהקובץ השתנה מתחת לידיים באמצע עבודה. **תבדוק גודל וחותמת זמן לפני שאתה מתחיל, ואל תעבוד עליו במקביל לסשן אחר.**

---

## 8. פרומפט ל‑Advisor — להעברה ל‑QA

> **העבר ל‑QA. Developer סיים סבב; זה מה שהשתנה ומה שצריך בדיקה.**
>
> **התחל בזה:**
> ```sql
> select version, name from supabase_migrations.schema_migrations order by version desc limit 8;
> ```
> שתי בדיקות קודמות התבזבזו על snapshot ישן.
>
> **מה שהשתנה בלקוח בסבב הזה (אין מיגרציה חדשה):**
> 1. **כתובת העסק מוצגת לעובד כבר בלוח**, לפני הגשה. `loadBoard` מצרף `address` ו‑`verified_at` מטבלת `businesses` לכל עסק שמופיע בלוח, ומשלים כתובת גם לשורות `my_shifts` שטרם אושרו.
> 2. **קישור הניווט נבנה מהכתובת** — `?q=<address>, <city>` — ונופל ל‑`?ll=<lat>,<lng>` רק כשאין כתובת.
> 3. **שדה עיר עם חיפוש** (`datalist`) החליף את התפריט הנגלל בשלושה מסכים: הרשמה, פרטים אישיים, פתיחת עסק. מה שנשלח לשרת הוא תמיד המזהה המספרי משדה נסתר.
> 4. **מתג מצב** החליף את שורת הלשוניות. משמרת של עסק שהמשתמש חבר בו כבר לא מוצגת בלוח שלו כלל.
>
> **איפה ללחוץ:**
> - **פרטיות כתובת.** ההחלטה שכתובת עסק היא מידע פומבי היא של ה‑Advisor (F5), והמייסד ביקש במפורש שהיא תוצג. תאשר שזה אכן מה שהוחלט, ושלא נחשף דרך אותו join שדה נוסף שלא התכוונו לו.
> - **שדה העיר.** נסה לגרום לו לשלוח `city_id` שלא תואם למה שמוצג על המסך: הקלדה חלקית, הדבקה, שינוי ה‑DOM, בחירה ואז מחיקה. הלקוח מאמת שם→מזהה, אבל השרת הוא הקובע — ודא ש‑`create_business` ו‑`complete_worker_profile` מחזירים `city_not_found` בכל מסלול כזה.
> - **הסתרת עסק עצמי בלוח** היא הסתרה בלקוח בלבד; השרת מחזיר `own_business`. ודא שהחסימה בשרת עדיין עומדת בעצמה, גם עם מזהה משמרת ישיר.
> - **`my_shifts()`** — ודא שהוא לא מדליף כלום על משמרת שאין לעובד מועמדות אליה, ושהכתובת שהוא מחזיר עדיין מוגבלת ל‑`confirmed` **בשרת**, גם אם הלקוח מציג יותר.
>
> **מה לא לדווח שוב:** Leaked Password Protection (לא זמין ב‑free tier), אזהרות `SECURITY DEFINER` על ה‑RPC (זו הארכיטקטורה), `result.last_owner` בלי מסלול בממשק.
>
> **שאלה פתוחה שתלויה בך, Advisor:** האם סיכה מדויקת על מפה תחליף את מרכז העיר גם בחישוב הרדיוס, או שהרדיוס נשאר ברמת עיר. התשובה קובעת אם `shifts.location` צריך להיגזר מהסיכה, ובלעדיה אי אפשר לבנות את מסך המפה.

---

## 9. פורמט החפיפה — לסיים בו כל יחידת עבודה

```
--- HANDOFF TO: <Tech Lead | QA> ---
DONE:      מה הושלם, ואיך אומת. לא מה נוסה.
STATE:     מצב המסד, הריפו והאפליקציה שהמקבל ימצא, כולל נתוני בדיקה שהשארת.
NEXT:      מה צריך לקרות אחר כך, לפי סדר.
FOR YOU:   הדבר הספציפי שהקולגה הזה חייב לעשות.
MUST KNOW: אילוצים או מוקשים שיעלו לו שעה אם לא יידע.
OPEN:      מה לא נסגר, כולל שאלות למייסד.
--- END HANDOFF ---
```
