# QA — ממצאי סבב M שנסגרו כלא־התקבלו
**נכתב: 21/09/2026 · מאת: QA · נמסר ל-Tech Lead לפתיחה מחדש**

הממצאים דווחו פעם אחת, בגוף החפיפה של סבב M, עם ראיה. **לא נכתב עבורם
קובץ** — זה הפער, וזה מה שהקובץ הזה סוגר. התוכן להלן הוא התוכן המקורי,
לא ניסוח מחדש.

---

## ממצא 2 — `located_at` הוא "מתי נשלחה נקודה לאחרונה", לא "מתי סומנה הסיכה"
**חומרה: נמוכה · סוג: נכונות סמנטית של עמודה · מצב: לא תוקן**

### מה נבדק
גוף `public.update_business`, שנקרא ב-`pg_get_functiondef`, מול אתחול
השדות הנסתרים ב-`app.html`.

### הראיה
`update_business`:
```sql
located_at = case when p_lat is not null then now() else located_at end
```
התנאי היחיד הוא **שנשלחה נקודה**, לא שהיא **שונה** מהקיימת. לשם השוואה,
באותה פונקציה ההפצה למשמרות כן בודקת שינוי בפועל:
```sql
v_moved := p_lat is not null
           and (p_lat is distinct from v_b.lat or p_lng is distinct from v_b.lng);
```

`app.html:1485-1486` — בעריכה השדות הנסתרים מתמלאים מראש מהסיכה הקיימת:
```html
<input type="hidden" id="b_lat" name="lat" value="${marked ? esc(num(b.lat).toFixed(6)) : ''}">
```
כאשר `app.html:1476`: `const marked = edit && b.located_at && hasPoint(b);`

### מה נצפה
כל שמירה של טופס עסק שכבר יש לו סיכה שולחת חזרה את אותן קואורדינטות,
ו-`located_at` נחתם מחדש ב-`now()` — גם כשהמעסיק שינה רק את הטלפון
ולא נגע במפה.

### מה היה אמור לקרות
`located_at` אמור לסמן את הרגע שבו המיקום נקבע. אחרי שמירה שלישית של
הטופס הוא מסמן את השמירה השלישית.

### למה זה לא קוסמטי
1. `my_shifts` כבר מחזיר `biz_located_at` ללקוח. ברגע שמישהו יציג אותו
   כ"מיקום עודכן ב-…", התאריך יהיה שקרי.
2. אין דרך לדעת מהנתון כמה זמן הסיכה יציבה — מידע שיידרש אם אי פעם
   נרצה סף אמון על מיקום.

### התיקון המוצע
```sql
located_at = case
               when p_lat is not null
                and (p_lat is distinct from v_b.lat or p_lng is distinct from v_b.lng)
                 then now()
               when p_lat is not null and v_b.located_at is null
                 then now()
               else v_b.located_at
             end
```
כלומר: חתום רק כשהנקודה זזה, או כשזו הפעם הראשונה שנשלחה נקודה בכלל.

### לא מוכח, ומסור לך
אין לי הרצת כתיבה. הבדיקה: כמפעיל/בעלים, `update_business` עם אותן
קואורדינטות בדיוק, ואז השוואת `located_at` לפני ואחרי. צפי כיום: משתנה.
צפי אחרי התיקון: זהה.

---

## ממצא 3 — נקודה עיוורת בשורת `located_at set but pin at centroid`
**חומרה: נמוכה · סוג: כיסוי של מבחן, לא באג בקוד · מצב: פתוח**

### מה נבדק
השורה שהוספת ל-Q5, מול מקורות ברירת המחדל האפשריים בלקוח.

### הראיה
השורה משווה את סיכת העסק **למרכז העיר שלו בלבד**:
```sql
extensions.ST_Distance(b.location,
  extensions.ST_SetSRID(extensions.ST_MakePoint(c.lng,c.lat),4326)::extensions.geography) < 1
```
`c` הוא `cities` דרך `b.city_id`.

ב-`app.html:1354` קיימת ברירת מחדל גיאוגרפית **אחרת**:
```js
else                    boardMap.setView([31.5, 34.9], 7);
```
זו נקודת מרכז ישראל, ומשמשת את מפת הלוח. היא **אינה** מחוברת היום
לשדות `b_lat`/`b_lng` של טופס העסק — אימתתי את זה: `mark()` נקרא רק
מ-`dragend` ומ-`click` (`app.html:1430-1431`), ובאתחול רץ `clear(centre)`
שמרוקן את השדות (`app.html:1442`).

### מה נצפה
השורה מחזירה 0, ו-0 הזה נכון.

### מה היה אמור לקרות
0 אמור להיקרא כ"לא נשלחה **שום** ברירת מחדל כסיכה". בפועל הוא נקרא
"לא נשלח **מרכז העיר** כסיכה". אלה לא אותו דבר.

### התרחיש שהשורה תפספס
לקוח — היום או אחרי רפקטור — שממלא את שדות הסיכה מנקודת המבט הנוכחית
של המפה, מ-`[31.5, 34.9]`, או ממיקום GPS של המכשיר. בכל אחד מהם
`located_at` נכתב, הסיכה שגויה, והשורה נשארת 0. עובד ייסע לנקודה
שהמערכת מצהירה עליה כמדויקת.

### מה אני **לא** מציע
אל תחליף את הסף ל"הלקוח שלח נקודה" — זה הופך את השורה לאפס תמידי.
זו הייתה התשובה שלי לשאלתך ב-3.1/2 והיא עומדת.

### מה כן
1. **קבל את המגבלה במפורש** — 0 בשורה הזו הוא היעדר כשל אחד, לא הוכחת
   תקינות. שווה הערה ליד השורה כדי שה-QA הבא לא יסיק יותר מדי.
2. **השומר האמיתי הוא בלקוח**, והוא נבדק: השדות ריקים עד מגע. הטענה
   הזו שייכת לרתמת הלקוח — טענה שטוענת ש-`b_lat.value === ''` אחרי
   `wirePinMap()` על עסק בלי `located_at`. שם היא תישבר אם מישהו ישנה
   את האתחול.

---

## סטטוס
שני הממצאים אינם חוסמים ולא היו חוסמים. הם נמסרים לפתיחה מחדש כפי
שביקשת. אם ההכרעה היא "מקובל כפי שהוא" — תגיד, ואוסיף אותם לרשימת
ההחלטות שאין לדווח עליהן שוב.
