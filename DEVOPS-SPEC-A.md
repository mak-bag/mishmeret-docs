# DEVOPS-SPEC-A — מפרט עמוד הסטטוס, שלב א
**מאת: DevOps · 20 בספטמבר 2026 · לאישור Tech Lead · אין קוד בסבב הזה**

---

## 0. מה אימתתי בעצמי, ובמה הרישום לא מדויק

| נבדק | ממצא | מול הרישום |
|---|---|---|
| Supabase `rkmjggormlxcvzuqedig` | `ACTIVE_HEALTHY`, PG 17.6, eu-central-1 | תואם |
| `anon` קורא `shifts` / `businesses` | `[]` — מדדתי ב-curl מול ה-REST, לא הנחתי | תואם. **ה-RPC הוא באמת הדרך היחידה** |
| ראש מיגרציות | `20260919223413 shifts_carry_their_own_located_at_snapshot` (38 מיגרציות) | תואם |
| Cloudflare Workers | החשבון נגיש, **0 Workers**, 0 KV | תואם |
| **Cloudflare R2** | **`403 — Please enable R2 through the Cloudflare Dashboard`** | **לא מופעל.** שלב 2 של `backup.yml` חסום עד שהמייסד מפעיל אותו בדשבורד |
| git remote | **אין.** ענף `main` בלבד | תואם — ו-`backup.yml` לא ירוץ אף פעם עד שתהיה דחיפה |
| `my_shifts()` פתוח ל-`anon` כ-`SECURITY DEFINER` | קראתי אותו כ-`anon`: מחזיר `[]`, כי `auth.uid()` הוא NULL. **לא דולף היום** | ה-linter מסמן; לא שלי לתקן — ראה OPEN |

**מספרי אמת נכון לעכשיו** (הרצתי את האגרגטים בפועל): 2 עסקים פעילים · 0 מאומתים ·
1 בלי סיכה · 10 פרופילים, 4 מלאים · 3 משמרות, 0 פתוחות · 2 מועמדויות מאושרות ·
**חציון פרסום→אישור: 160.96 דקות על n=2**. `ROADMAP-AND-HIRING.md` §1 כתוב על נתון
ישן יותר (8 משתמשים, מועמדות אחת). זו בדיוק הסיבה שהעמוד קיים.

---

## 1. טבלת שדות ה-RPC

`public.status_snapshot()` · `STABLE` · `SECURITY DEFINER` · `SET search_path = public` ·
`GRANT EXECUTE TO anon` + `REVOKE FROM public` · **מחזיר שורה אחת תמיד, לעולם לא אפס
שורות** — אפס שורות ואפס ערכים חייבים להיראות שונה בעמוד.

| # | שדה | טיפוס | הגדרה שאני מבקש לנעול |
|---|---|---|---|
| 1 | `generated_at` | `timestamptz` | `now()` בשרת. הדף מציג "נכון ל־" |
| 2 | `median_minutes_to_confirm_7d` | `numeric` | חציון `(a.confirmed_at - s.created_at)` בדקות |
| 3 | `median_minutes_to_confirm_30d` | `numeric` | אותו דבר, 30 יום |
| 4 | `median_minutes_to_confirm_all` | `numeric` | אותו דבר, מאז ומעולם |
| 5–7 | `confirmed_count_7d` / `_30d` / `_all` | `integer` | **ה-n של כל חציון. חציון בלי n הוא שקר** — 161 דקות על n=2 אינו מדד |
| 8–9 | `shifts_posted_7d` / `shifts_filled_7d` | `integer` | דלי לפי `created_at`; `filled >= slots` |
| 10–11 | `shifts_posted_30d` / `shifts_filled_30d` | `integer` | כנ״ל |
| 12 | `shifts_open_now` | `integer` | `cancelled_at is null and ends_at > now() and filled < slots` |
| 13 | `shifts_open_urgent_now` | `integer` | כנ״ל `and urgent` |
| 14–16 | `businesses_active` / `_verified` / `_without_pin` | `integer` | `closed_at is null` · `+ verified_at is not null` · `+ located_at is null` |
| 17–18 | `workers_total` / `workers_profile_complete` | `integer` | שלם = **בדיוק הכלל של `app.html:827`** |
| 19–22 | `applications_applied` / `_offered` / `_confirmed` / `_declined` | `integer` | ספירה לפי `status` |
| 23–24 | `integrity_tables_nonzero` / `integrity_tables_checked` | `integer` | ירוק כששווים |
| 25 | `integrity_detail` | `jsonb` | `[{"table":"shifts","rows":3}, …]` — **שם טבלה וספירה בלבד**. הפירוט בלחיצה |
| 26–27 | `tables_known` / `tables_public` | `integer` | ראה §5. זה השדה שמציל את מדד 7 |
| 28–29 | `migration_head` / `migration_head_at` | `text` / `timestamptz` | מ-`supabase_migrations.schema_migrations` |
| 30 | `rpc_version` | `text` | מחרוזת שאתה מקדם ידנית. מבדילה בין סכימה ישנה ל-Worker ישן |

**שלוש הכרעות מוצר שאני צריך ממך במפורש, לא הנחה שלי:**

1. **דלי הזמן של מדד 2.** אני מציע לספור לפי `created_at` — מונה ומכנה מתארים את
   אותה קבוצת משמרות. המחיר: משמרת שפורסמה ביום 7 ואוישה ביום 8 נספרת כשלא־אוישה,
   והיחס יכול רק לעלות בדיעבד. החלופה — דלי לפי מועד האיוש — נותנת מספר מחמיא יותר
   ומשווה תפוחים לתפוזים.
2. **`family_name` אינו בכלל השלמות של האפליקציה** (`app.html:827` בודק
   `given_name && home_city_id && birth_date` בלבד). מדדתי 4 מתוך 10 בשני הכללים —
   היום זה לא משנה. אם הכוונה המוצרית היא ששם משפחה נדרש, **האפליקציה וה-RPC
   משתנים יחד**, אחרת העמוד יסתור את המסך.
3. **"אישור עובד" = `applications.confirmed_at`** בשני כיווני הזרימה (claim→approve
   ו-offer→accept). אם יש כיוון שלא אמור להיספר בשער החנות — תגיד עכשיו.

---

## 2. פלטפורמת הרצה: Worker יחיד ב-Cloudflare, על `workers.dev`

Worker אחד מגיש HTML סטטי **וקורא ל-RPC בצד השרת**.

**למה:**
- **זה מה שקיים.** אין כלי Pages ואין DNS ב-MCP. Workers אימתתי שנגיש. `workers.dev`
  לא דורש מהמייסד שום פעולת דומיין.
- **המפתח לא נכנס לדפדפן בכלל.** ה-`anon` בנוי לשבת בדפדפן ואני לא מסתיר אותו —
  אבל אם ה-Worker כבר עומד באמצע, אין סיבה לשלוח אותו. פחות משטח, ואותו מאמץ.
- **בלי npm, בלי build.** קובץ אחד, `fetch` מובנה.
- **המטמון יושב בשרת** (§4) — וזה מה שמנתק את עלות המסד ממספר הצפיות.
- **GitHub Pages נפסל:** הריפו פרטי, ו-Pages על ריפו פרטי דורש תשלום. גם אז זה
  סטטי בלבד — אין איפה לאכוף הגנה ואין איפה להחביא את הקריאה.
- **יציאה:** ~150 שורות JS תקני ו-`fetch`. אין SDK, אין KV, אין D1. מוחקים את
  ה-Worker ולא נשאר שובל.

---

## 3. הגנה: סיסמה משותפת → עוגיית HMAC חתומה. **לא** Cloudflare Access

**Cloudflare Access הייתה הבחירה הנכונה — והיא לא זמינה.** מדיניות Access חלה על
hostname בתוך zone שבחשבון; `*.workers.dev` אינו zone שלנו. בלי דומיין בחשבון
ובלי כלי DNS, Access דורשת פעולה ידנית של המייסד לפני שיש מה להגן. **זה מסלול
השדרוג הנכון ברגע שיש דומיין — ואז הסיסמה יורדת.**

**מה כן:** טופס קטן, סיסמה אחת ב-Worker Secret, השוואה בזמן קבוע, ואז עוגייה
`HttpOnly; Secure; SameSite=Strict` חתומה ב-HMAC-SHA256 עם TTL של 12 שעות
(WebCrypto, מובנה). חמש טעויות מאותו IP → חסימה לדקה.

**מה נפסל ולמה:**
- **Basic Auth** — הדפדפן שולח את הסיסמה בכל בקשה, אין ניתוק, היא נדבקת למנהל
  הסיסמאות של המכשיר, ואי אפשר לבטל אדם אחד. על טלפון זה גם דיאלוג אנגלי.
- **טוקן ב-URL** — הבריף אוסר להישען על סודיות ה-URL, ובצדק: URL נכנס להיסטוריה,
  ל-Referer, ולצילום מסך בוואטסאפ.
- **התחברות עם Supabase Auth** — קושרת את זמינות עמוד הסטטוס לזמינות ה-auth של
  המוצר, ופותחת נתוני נפח עסקי לכל אחד מעשרת המשתמשים הרשומים.

---

## 4. עלות: **0 ₪ לחודש**, וגם אחרי פי עשר

בסיס מדוד: קריאת REST ל-Supabase = 947 בתים כותרות + גוף, 140ms מכאן.
תשובת ה-RPC היא אובייקט JSON אחד, ~700 בתים → **~1.7KB לקריאת מסד**.

| | היום | פי עשר |
|---|---|---|
| טעינות עמוד ליום | ~100 (5 אנשים × 20) | ~1,000 |
| בקשות Worker / יום | 100 | 1,000 — **1% מ-100,000 החינמיות** |
| **קריאות מסד / יום** | **1,440** | **1,440 — ללא שינוי** |
| egress מ-Supabase / חודש | ~73MB מתוך 5GB (1.5%) | ~73MB. זהה |
| CPU ל-invocation | fetch + תבנית מחרוזת, הרבה מתחת ל-10ms | זהה |

**הסיבה שפי עשר לא עולה כלום:** מטמון של 60 שניות ב-Worker Cache API — לפי זמן,
לא לפי משתמש. תקרת קריאות המסד היא 1,440 ליום ולא משנה כמה אנשים מסתכלים.
**מה כן יזיז עלות: גידול נתונים** (500MB תקרת DB) והדאמפ הלילי — לא צפיות.

מספרים אלה חושבו ממדידת בקשה אמיתית. **המדידה המלאה על העמוד עצמו היא תנאי
גמר של שלב ב**, כפי שהבריף דורש.

**סיכון free-tier לנקוב בשמו:** פרויקט Supabase חינמי נרדם אחרי 7 ימי שקט.
הדאמפ הלילי אמור למנוע זאת — **אבל הוא לא רץ אף פעם, כי אין remote.** עד לדחיפה
הראשונה, קריאות העמוד הן ה-keep-alive היחיד בפועל.

---

## 5. מה יישבר כשמישהו יוסיף טבלה ולא יעדכן את ה-RPC

**העמוד ימשיך להציג ירוק וישתוק.** מדד 7 סופר רק טבלאות שה-RPC יודע עליהן, ולכן
טבלה חדשה היא לא "אפס שורות" אלא "לא נשאלה" — וזו בדיוק התקלה שמדד שלמות אמור
לתפוס. גרוע יותר: `זמינות מוצהרת` היא הטבלה הבאה שאתה כותב (§4 ב-`ROADMAP`), כך
שהמקרה הזה יקרה בשבוע הקרוב, לא בתיאוריה.

**לכן שדות 26–27.** `tables_public` נספר מ-`pg_tables` בזמן ריצה, `tables_known`
הוא מה שה-RPC מונה בפועל. כשהם נפרדים הדף עולה לכתום:
*"ה-RPC אינו מכיר N טבלאות"* — **העמוד מזהה את התיישנותו בלי שאף אחד יתחזק אותו**,
וזה התנאי היחיד שבלעדיו מדד 7 מטעה במקום להתריע. מדדתי היום: 12 טבלאות ב-`public`.

שני מצבי כשל נוספים שהעמוד חייב להבדיל ביניהם, ולכן הם במפרט: **שינוי שם עמודה**
שובר את ה-RPC ומחזיר שגיאת HTTP → "לא זמין", לא אפס (תנאי גמר 3). **Worker ישן מול
סכימה חדשה** נתפס דרך `rpc_version` מול `migration_head`.

---

## בלוק חפיפה

**DONE** — אימות תשתית בכלים, לא ברישום: Supabase חי וראש מיגרציות נקרא;
`anon` נחסם בפועל מול `shifts`/`businesses` ב-curl; Workers נגיש וריק;
**R2 מחזיר 403 ולא מופעל**; **אין git remote**; `my_shifts()` נקרא כ-`anon`
ומחזיר `[]`. כל האגרגטים של המפרט הורצו על הנתונים האמיתיים — כולל חציון
הצפון: 160.96 דקות על n=2.

**STATE** — שלב א כתוב. אפס קוד, אפס פריסה, לא נגעתי ב-`service_role`,
לא נגעתי ב-`app.html`.

**NEXT** — עם אישורך: Worker, טופס הסיסמה, העמוד, ואז מדידת עלות אמיתית.

**FOR YOU** — (1) אשר את 30 השדות. (2) הכרע בשלוש שאלות המוצר ב-§1.
(3) כתוב את `status_snapshot()` עם ה-grant כפי שנוסח. (4) החלט לגבי הגרנט
של `my_shifts` ל-`anon`.

**MUST KNOW** — **`backup.yml` לא רץ אף פעם**: אין remote, ו-R2 לא מופעל, כך
ששלב 2 שלו חסום גם אחרי הדחיפה. אין היום גיבוי אחד. זה חמור יותר מעמוד הסטטוס,
ואני מבקש רשות לטפל בו בסבב נפרד.

**OPEN** — `my_shifts()` הוא `SECURITY DEFINER` פתוח ל-`anon`. **לא דולף היום**
כי `auth.uid()` הוא NULL ומדדתי `[]`. הוא הופך לדלת ביום שמישהו יוסיף התנהגות
ברירת־מחדל ל-uid ריק. הסרת הגרנט היא שלך. · שער החנות דורש חציון מתחת לשעתיים;
המדידה היום היא 161 דקות — מעל השער, אבל על n=2 זה רעש ולא ממצא.
