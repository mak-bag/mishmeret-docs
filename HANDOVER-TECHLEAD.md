# תיק חפיפה — Tech Lead / Advisor · משמרת

**נכתב:** 19/09/2026, סוף יום · **ראש מיגרציות:** `20260919204409 derive_full_name_and_unify_my_shifts_disclosure` (33 מיגרציות)

אתה ה-Tech Lead הבא. התיק הזה עצמאי — אין צורך בהיסטוריה.
**כל נתון כאן אומת מול המסד החי ברגע הכתיבה.** היכן שמשהו לא אומת, זה נאמר במפורש.
התיק יתיישן. סעיף 11 מסביר איך לא ליפול בזה.

**שני תיקים משלימים בתיקייה, נכתבו על ידי הצוות — קרא אותם:**
`HANDOVER-DEV.md` (המפתח) · `QA-HANDOVER.md` (ה-QA, כולל מערך שאילתות Q0–Q10 שכדאי לאמץ).

---

## 1. איך הצוות עובד

| תפקיד | מי | סמכות |
|---|---|---|
| **מייסד** | ולדי | קובע מוצר ועדיפויות. **לא מתכנת.** עברית, קצר, בלי ז׳רגון. |
| **Tech Lead / Advisor** | **אתה** | מחליט ארכיטקטורה, סכימה, RLS. כותב מיגרציות ו-RPC. **לא בונה UI — מפרט אותו.** |
| **Developer** | סשן נפרד | בונה `app.html` בלבד. לא כותב מיגרציות. |
| **QA** | סשן נפרד, **קריאה בלבד** | מבקר ומוכיח. ממצאיו עוברים דרכך, לא ישירות למפתח. |

**זרימת העבודה בפועל:** המייסד מעביר בין הסשנים בהעתק-הדבק. הוא לא מריץ כלום.
המפתח ו-QA מחזירים לך **בלוק חפיפה** בפורמט של סעיף 12. אתה מכריע, כותב מיגרציה, ומחזיר brief.

**שלושה כללים שנלמדו בדרך הקשה:**
1. **מסור brief כקובץ `.md`, לא כבלוק בצ׳אט.** שלוש פעמים ביום אחד ה-brief לא הגיע למפתח כי נגזר בהעתקה. `DEV-BRIEF.md` ו-`QA-BRIEF.md` קיימים בתיקייה לשם כך.
2. **אל תשלח מיגרציה שמסתמכת על שדה שהמפתח עוד לא בנה.** שער ה-18+ נשלח לפני שהיה שדה תאריך לידה, ולשעה קלה אף אחד לא יכול היה לתפוס משמרת. **שדה קודם, שער אחריו.**
3. **QA בודק מול תמונת מצב.** פעמיים הוא דיווח חוסמים שכבר תוקנו, כי שלחת מיגרציות בקצב מהיר יותר מקצב מסירת ה-briefs. תמיד תגיד לו להתחיל מ-`list_migrations`.

---

## 2. המוצר והקו האדום

שוק שמחבר מסעדות בישראל לעובדי משמרות: ברמן, עובד שטיפה, מלצר, טבח, איש פלור.
הערך: סגירת נפילה ברגע האחרון. **המדד היחיד: דקות מפרסום משמרת עד אישור עובד.**

**חברת לידים, לא חברת כוח אדם** — קטגוריה מורשית בישראל. מכאן:
- **אין שיבוץ אוטומטי לעולם.** אדם לוחץ "אני לוקח", אדם לוחץ "אישרתי".
- אין דרגות מיומנות, תעודות או תנאי העסקה. חמש קטגוריות, נקודה.
- **חסימה רוחבית של עובד היא הקו שמוביל לחברת כוח אדם.** דחייה תקפה למשמרת אחת בלבד.

**אל תבנה כרגע:** תרגום בפועל · תשלומים · מדינות נוספות · אפליקציה נייטיב · SMS בתשלום.

---

## 3. תשתית

| | |
|---|---|
| Supabase | `mishmeret`, ref `rkmjggormlxcvzuqedig`, eu-central-1, Postgres 17, **free tier** |
| GitHub | ריפו פרטי `mak-bag/mishmeret`. **אין git במכונה** — העלאה דרך ממשק הווב |
| דומיין | `mak-bag.com` (GoDaddy), שרתי שמות לא מכוונים. השם לא תואם למותג — נדחה עד שהפיילוט יוכיח את עצמו |
| אפליקציה | `app.html` — קובץ יחיד, ~90KB, בלי build ובלי npm |

**אפס הוצאה. הכול על free tier.** אם משהו יעלה כסף — תגיד זאת ותגיד מתי זה מוצדק.

**ל-Chrome יש גישה דרך MCP והמייסד מחובר שם ל-GitHub.** כך נדחף ה-snapshot היחיד שקיים (`0736654`). הערה: כתיבה לשדות הגדרות אבטחה ב-Supabase נחסמת על ידי מסנן בטיחות — הגדרות Auth הן פעולה של המייסד.

---

## 4. מצב אמיתי כרגע

| | |
|---|---|
| משתמשים / פרופילים | 8 / 8 |
| עסקים | 2 (0 מאומתים) |
| משמרות | 3 |
| מועמדויות | 1 |
| ביקורות / צוות קבוע | 0 / 0 — **מעולם לא נבדקו על נתונים אמיתיים** |
| ערים | 46 (he+en) |
| `terms` | 320 שורות, 130 קודי `ui` ייחודיים, **אפס קוד חסר שפה** |
| פרופילים בלי תאריך לידה | 5 מתוך 8 — **הם לא יכולים לתפוס משמרת** עד שישלימו |

**נתונים אמיתיים של המייסד — אסור למחוק:** משתמש "ולדי בגרנובסקי", עסק **"ביסטרו נוטר"** בנתניה (אוסישקין 11), משמרת ברמן ל-25/09.

**מוסכמת ניקוי:** ישות בדיקה מתחילה ב-`QA ` בשם. סדר מחיקה: `applications` → `team_members` → `shifts` → `businesses`. **עסק לא ניתן למחיקה דרך הלקוח** — רק `close_business`. כ-postgres מחיקה אפשרית, אבל היא מפילה בקסקייד גם ביקורות.

---

## 5. ה-API — כל הכתיבה עוברת דרכו

```
-- עובד
claim_shift(p_shift) -> text
accept_offer(p_shift) -> text
release_application(p_shift, p_worker, p_status) -> text     -- עובד: 'withdrawn' בלבד
my_shifts() -> TABLE(...)
my_profile() -> jsonb
complete_worker_profile(p_given, p_family, p_city_id, p_birth_date, p_radius_km) -> jsonb
dispute_review(p_review, p_note) -> text

-- מעסיק
create_business(p_name, p_city_id, p_address, p_phone, p_kind, p_lat, p_lng) -> jsonb
update_business(p_business, p_name, p_address, p_phone, p_kind, p_lat, p_lng) -> jsonb   -- בעלים בלבד
close_business(p_business) -> jsonb                                                       -- בעלים בלבד
post_shift(..., p_adults_only) -> jsonb
edit_shift(..., p_adults_only) -> jsonb
cancel_shift(p_shift, p_reason) -> jsonb
uncancel_shift(p_shift) -> jsonb                  -- חלון 120 שניות
approve_application(p_shift, p_worker) -> text
offer_shift(p_shift, p_worker) -> text
shift_applicants(p_shift) -> TABLE(...)

-- לוח
shifts_for_me(p_radius_km) -> SETOF shifts        -- SECURITY INVOKER
shifts_near_city(p_city_id, p_radius_km) -> SETOF shifts  -- SECURITY INVOKER
```

**מה הלקוח עדיין כותב ישירות (מכוון):** `profiles.update` על עמודות מוגבלות · `team_members` insert/delete.
**מה נשלל ממנו לתמיד:** UPDATE/DELETE על `applications` ו-`shifts` · INSERT ל-`shifts` ול-`businesses` · כל כתיבה ל-`profile_contacts` · DELETE על `businesses` ו-`reviews` · כתיבה ל-`cities`/`city_names`/`terms`/`shift_translations` · קריאת `birth_date` · כתיבת `full_name`.

**מכונת מצבים:** `applied → offered → confirmed`, ובנוסף `declined` `cancelled` `withdrawn`.
`shifts.filled` עולה רק ב-`confirmed`, מתוחזק בטריגר `sync_shift_filled`, ו-`CHECK (filled <= slots)` היא רצפת ההגנה.

---

## 6. ההחלטות הנעולות — אל תהפוך בלי לשאול

- **המעסיק הוא עסק, לא אדם.** אחרת הנכס הולך עם המנהל שעוזב.
- **תפקיד הוא יכולת, לא סוג חשבון.** נדון מחדש היום ואושר מחדש: ההתחברות היא טלפון, ולכן אדם אחד הוא חשבון אחד. פיצול היה מחייב מי שהוא גם וגם להחזיק שני קווי טלפון.
- **תקן נצרך רק ב-`confirmed`.** מי שלא יזם — מאשר. `instant` קופץ ישר ל-`confirmed`.
- **לעובד אין קואורדינטות בשום מקום.** עיר + רדיוס; השרת פותר מהצנטרואיד. רק לעסק יש נקודה מדויקת.
- **הרדיוס דו-צדדי**, ומגבלת המעסיק **חוסמת** בשרת (`too_far`), לא רק מסננת תצוגה.
- **ביקורות דו-סטריות ועיוורות.** לא ניתן לשחזר בדיעבד בלי להשחית היסטוריה.
- **נוכחות גוברת על כוכבים.** נתון ההגעה החוצה-מעסיקים הוא הנכס.
- **כל מחרוזת גלויה היא קוד ב-`terms`**, he+en. מחרוזת עם מספר מפוצלת ל-`.one`/`.other`.
- **זמנים `timestamptz` + אזור IANA.**
- **התחברות טלפון+סיסמה**, ממופה ל-`972XXXXXXXXX@mak-bag.com` על דומיין השורש. דורש "Confirm email" כבוי — **כבוי כרגע**.
- **פיילוט 18+ בלבד** לתפיסת משמרת. קטינים נרשמים וגולשים. ראו סעיף 8.
- **עסק נסגר, לא נמחק.** מחיקה מפילה ביקורות בקסקייד משני כיוונים.
- **כתובת עסק וטלפון עסק הם מידע פומבי.** רק העובד מוגן.

---

## 7. מה נסגר ב-19/09 — 22 מיגרציות ביום אחד

כל אחד מהם **הוכח מול המסד החי** לפני התיקון, ואומת אחריו בבלוק `begin/rollback`.

| החור | איך נסגר |
|---|---|
| עובד משבץ את עצמו ב-UPDATE ישיר על `applications` | מדיניות הוסרה, UPDATE/DELETE נשללו |
| קציר טלפונים כבר ב-`applied` | `contacts_read` צומצמה ל-`offered`/`confirmed` |
| זיוף מיקום ב-INSERT ישיר ל-`shifts` | INSERT נשלל; `post_shift` היא הדרך היחידה |
| אותו זיוף דרך UPDATE על `businesses` | UPDATE/DELETE נשללו; `update_business` בלבד |
| מנהל מוחק עסק ומשמיד ביקורות בקסקייד | `close_business` + `closed_at` |
| מנהל מדיח בעלים | `is_business_owner` + טריגר `protect_last_owner` |
| מרוץ נעילה ב-`close_business` | נועלת משמרות לפני מועמדויות |
| `closed_at` נבדק רק בלוח | נבדק בכל מסלולי הכתיבה |
| `birth_date` נקרא ע"י מעסיק | הרשאת **עמודה** נשללה |
| עובד שנדחה תופס שוב | `declined_before` |
| מגבלת מרחק הייתה תצוגתית | `too_far` בשרת |
| `reviews_update` בלי WITH CHECK | טריגר `reviews_identity_guard` |
| `anon` עם INSERT/UPDATE/DELETE/TRUNCATE על 12 טבלאות | רצפת הרשאות: SELECT בלבד |
| **עסקה עצמית — בעלים מזמין את עצמו וכותב לעצמו ביקורות משני הצדדים** | `own_business` ב-`claim_shift`/`offer_shift`, ו-`reviews_insert`/`team_write` דוחות חבר עסק |
| קציר טלפונים נפתח מחדש דרך `offered` | `offer_shift` מוגבלת למי שהגיש או לצוות קבוע |
| `uncancel_shift` הורידה שיבוץ ל-`applied` | `prev_status` משחזר `confirmed` עד `slots` |
| `birth_date` ניתן לשכתוב אינסופי | write-once (`birth_date_locked`) |
| טלפון שבור בהרשמה (`+97250324233`) | `CHECK` על `^\+9725[0-9]{8}$`. **החשבון נמחק לבקשת המייסד** |
| `rls_auto_enable()` נגיש ל-`anon` | EXECUTE נשלל; הטריגר עדיין פועל (אומת עם טבלת בדיקה) |
| הרשאות כתיבה בלי מדיניות תואמת | נשללו. **כרגע: 0 פערים** |

**המלכודת הכי יקרה ביום הזה, תזכור אותה:**
`revoke select (col)` **לא עושה כלום** כל עוד קיימת הרשאת SELECT ברמת טבלה. צריך `revoke select on table` ואז `grant select (cols)` עמודה-עמודה. אותו דבר ל-UPDATE. זה עלה שני סבבים.

---

## 8. נוער — המצב המשפטי

**אני לא עורך דין. צריך עורך דין לפני השקה.** זה כבר ברשימת החשיפות של המייסד.

חוק עבודת הנוער מטיל כמעט את כל החובות על **המעסיק**: אישור רפואי, תקרה של 8 שעות ביום ו-40 בשבוע, ואיסור עבודת לילה — מתחת ל-16 בין 20:00 ל-08:00, ובני 16–18 בין 22:00 ל-06:00. הגשת אלכוהול דורשת 18.

**למה זה נוגע לנו במיוחד:** המוצר כולו הוא משמרות ערב ולילה במסעדות — בדיוק החלון שהחוק סוגר לקטינים. מערכת שמציפה בן 17 למשמרת ב-22:30 מסייעת להפרה, וככל שההתאמה אוטומטית יותר כך קשה יותר לטעון שרק תיווכנו.

**מה הוחלט:** 18+ לתפיסת משמרת בפיילוט. קטין נרשם, גולש ורואה שכר — פשוט לא תופס.
**כשייפתח, זה מערכת ולא דגל:** הסכמת הורה מאומתת · אישור רפואי בתיק · אכיפת שעות לילה בשרת לפי טווח גיל · תקרת שעות שבועית חוצת-מעסיקים (רק אנחנו רואים אותה — יתרון אמיתי על נייר).
**`is_adult` הוא הצהרה עצמית.** write-once מפחית, לא פותר. ההגנה האמיתית היא שהמעסיק מתקשר לפני אישור — אדם רואה אדם. אל תציג "מאומת" למעסיק; הניסוח חייב להישאר "הצהרה של העובד".

---

## 9. מה פתוח — לפי סדר

### 9.1 חוסם: הקוד לא מגובה
`app.html` (90KB) קיים רק במחשב המייסד. `main` מחזיק snapshot ישן (`0736654`) בלי `assets/`.
**זו פעולה של המייסד.** תבקש בתחילת הסשן, לא בסופו.

### 9.2 הפיצ׳ר הבא: זמינות מוצהרת של עובד
יום בשבוע + שעות + תפקידים + רדיוס. זה מה שהופך את המוצר מלוח מודעות למנוע התאמה, ותנאי מוקדם להתראות.
**הסכימה עדיין לא נכתבה — היא שלך. המפתח הונחה לא לתכנן אותה בעצמו.**

### 9.3 מסך מפה — הצד השרתי מוכן, החלטה אחת חסרה
`p_lat`/`p_lng` קיימים ב-`create_business` וב-`update_business`, עם `point_near_city` שחוסם 50 ק״מ.
**המפתח שאל ואני עונה כאן:** הסיכה **כן** מחליפה את מרכז העיר לצד המשמרת. `shifts.location` נגזר מ-`shifts.lat/lng` שמגיעים מהעסק, והם מזינים את כל חישובי הרדיוס. צד העובד נשאר ברמת עיר — זו החלטה נעולה. אין שינוי סכימה; צריך רק שהלקוח ישלח את הנקודה.
**אזהרה למי שיבנה:** אסור לדחוף כתובת טקסטואלית לתוך `lat/lng`. שדה העיר חייב לבוא לפני המפה, כי השרת דוחה סיכה רחוקה (`location_far`). Leaflet + אריחי OpenStreetMap, בלי מפתח ובלי עלות.

### 9.4 באג ידוע שלא נסגר: הסיכה של עסק אמיתי היא מרכז העיר
בדיקת התקינות מחזירה **1** ל-"עסק עם כתובת שהסיכה שלו במרכז העיר" — זה "ביסטרו נוטר".
הניווט **תוקן בלקוח** (בנוי מהכתובת, נופל לקואורדינטות רק בהיעדרה), אבל **הרדיוס עדיין נמדד ממרכז נתניה**. זה ייפתר כשמסך המפה ייבנה ו-`update_business` יקבל סיכה אמיתית.

### 9.5 אימות עסקים — ההחלטה שמחכה למייסד
`businesses.verified_at` קיים, **שום דבר לא חסום עליו**, ואין מסך אדמין (נקבע ידנית ב-Table Editor; `service_role` עוקף RLS).
**ההמלצה שלי, שכבר נמסרה למייסד:** החמרה נדרשת לפני המעסיק הראשון שהוא לא גייס אישית. עד אז הוא מכיר כל מעסיק בשם והדגל מספיק. זה מקדים את סעיף אימות הוואטסאפ ברשימת העבודה.

### 9.6 פערים קטנים שנשארו פתוחים במכוון
- **הוספה לצוות היא INSERT ישיר** ולא RPC. המדיניות נושאת את הכלל, כולל דרישת מועמדות `confirmed` קודמת. החלטה מודעת.
- **אין מסך חברי עסק**, ולכן `result.last_owner` ו-`members_insert` חסרי מסלול בממשק.
- **50 ק״מ ממרכז עיר רופף במרכז הארץ** — עסק יכול להציג "תל אביב" ולהצמיד סיכה 45 ק״מ משם. `city_id` נעול אחרי היצירה, אז אין החמרה עם הזמן. החלטה, לא באג.
- **`profiles_read USING (true)`** מאפשר מניית כל המשתמשים. התקבל לעת עתה — אין מסך חיפוש עובדים, אז אין דרך להשיג uuid של זר. **חייב להיבדק מחדש לפני Web Push**, כי אז הצעות לא-מוזמנות הופכות לספאם.
- **`anon` מקבל SELECT על הכול** אבל אין לו שום מדיניות תואמת, ולכן RLS מחזיר לו אפס שורות. הגנה שעובדת נכון אך מסיבה עקיפה — ביום שמישהו יכתוב מדיניות `to public`, anon יורש קריאה.
- **Leaked Password Protection לא זמין ב-free tier.** יופיע לנצח ב-advisor. **רעש מקובל — אל תרדוף אחריו ואל תבקש מהמייסד להפעיל אותו.**

### 9.7 ממתין להכרעת המייסד
- **אורך סיסמה מזערי 6 → 8.** Auth → Sign In / Providers → Email. נחסמתי ממילוי השדה על ידי מסנן בטיחות; זו פעולה שלו. בקוד יש `minlength="6"` שיתעדכן אחריו.
- **מתי אימות עסק הופך לחובה** (9.5).

---

## 10. שאלות אסטרטגיות פתוחות

נזילות ראשונה באזור אחד · מתי ואיך מתחילים לגבות · מתי השם והדומיין הופכים לבעיה אמיתית · חשיפה משפטית לפני השקה (חוק הגנת הפרטיות ורישום מאגרי מידע, לשון הרע בדירוגים שליליים, הקו מול רישוי כוח אדם, **עבודת נוער — סעיף 8**) · המסלול הבינלאומי כשדיני עבודה הם לאומיים.

---

## 11. איך לא ליפול — חמש מלכודות מאומתות

1. **בלי `set local role authenticated` אתה רץ כ-postgres ועוקף RLS לגמרי.** כל בדיקה "עוברת" בלי לבדוק כלום. זו הטעות הקלה והמסוכנת ביותר כאן.
2. **`revoke` ברמת עמודה חסר ערך כשקיימת הרשאה ברמת טבלה.** ראו סעיף 7.
3. **`SECURITY DEFINER` עוקף מדיניות.** תיקון `contacts_read` לבדו לא סוגר חור ש-`shift_applicants` פותחת. בכל ממצא פרטיות בדוק את **שני** המסלולים.
4. **אל תסמוך על טקסט המיגרציה.** מקור האמת הוא `pg_proc`, `pg_policies`, `pg_constraint`, `information_schema`.
5. **המסד והקובץ משתנים תוך כדי.** התחל כל סבב ב-`list_migrations` וב-`Get-Item app.html`.

**הרץ את מערך הבריאות של QA (Q5 ב-`QA-HANDOVER.md`) בפתיחה ובסגירה של כל סבב.**
נכון לעכשיו: **13 מתוך 14 מחזירים 0**. היחיד שאינו אפס הוא 9.4.

**אחרי כל שינוי סכימה:** `get_advisors` על security ועל performance. מצב נוכחי — 18 אזהרות `SECURITY DEFINER` (זו הארכיטקטורה, מקובל), Leaked Password Protection (לא זמין ב-free), ו-7 אינדקסים לא בשימוש על דאטה ריקה. **אין ממצא פתוח אמיתי.**

---

## 12. פורמט החפיפה — לסיים בו כל יחידת עבודה

```
--- HANDOFF TO: <Developer | QA> ---
DONE:      מה הושלם, ואיך אומת. לא מה נוסה.
STATE:     מצב המסד, הריפו והאפליקציה שהמקבל ימצא, כולל נתוני בדיקה שהושארו.
NEXT:      מה צריך לקרות אחר כך, לפי סדר.
FOR YOU:   הדבר הספציפי שהקולגה הזה חייב לעשות.
MUST KNOW: אילוצים או מוקשים שיעלו לו שעה אם לא יידע.
OPEN:      מה לא נסגר, כולל שאלות למייסד.
--- END HANDOFF ---
```

---

## 13. איך לדבר עם המייסד

- עברית, קצר, בלי ז׳רגון. פסקה עם מסקנה עדיפה על עמוד ניתוח.
- **תן המלצה עם נימוק וסיכון — לא רשימת אפשרויות.**
- **עצור אותו** כשכיוון סותר נזילות מקומית, רגולציה, או פשטות לקהל לא טכני. הוא מעדיף התנגדות מנומקת על הסכמה מנומסת.
- כשהוא חוזר על בקשה אחרי שהתנגדת — זו החלטתו. בצע במלואה ואמור שאתה מבצע.
- **לעולם אל תזין סיסמאות, מפתחות או טוקנים, ואל תבקש ממנו להדביק אותם בצ׳אט.**
- אל תשנה הגדרות אבטחה בחשבונות שלו בלי לשאול.
- דווח מה שקרה באמת. בדיקה שנכשלה — אמור זאת עם הפלט. דילגת — אמור שדילגת.
