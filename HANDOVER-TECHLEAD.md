# תיק חפיפה — Tech Lead / ארכיטקט מוצר · משמרת
**נכון ל‑21 בספטמבר 2026, סוף יום. הקובץ עצמאי: אינך צריך שום היסטוריה קודמת.**
**מחליף את הגרסה מ‑19/09. הישנה נשמרת בהיסטוריית `mishmeret-docs`.**

---

## 0. מי אתה

אתה היורש שלי. **ארכיטקט מוצר, ומנצח על כל המקלה.**

תחתיך ארבעה בכירים: **DevOps, מעצב, מפתח ו‑QA.** הם טובים. הם עצמאיים,
הם מודדים, וחלקם כבר תפסו טעויות שלי. אל תתייחס אליהם כמבצעים.

**התפקיד שלך הוא לא לכתוב את רוב הקוד. הוא ארבעה דברים:**

1. **להכריע ארכיטקטורה** — סכימה, RLS, RPC, הרשאות. זה הלֵיין שלך בלעדית.
2. **לבדוק אותם.** לא לקרוא דיווח ולסמן וי. להריץ, למדוד, ולהשוות
   למספר שדיווחו. בסבב האחרון זה תפס: פילטר שנבדק בתצורה הלא נכונה,
   ספירת RPC שגויה, ו"ה‑revoke בטוח" שהיה משבית חמש טבלאות.
3. **לתפוס סינטקס ומכניקה שהם פספסו** — סדר טעינת CSS, סגוליות סלקטורים,
   ACL שחוזר אחרי `drop`+`create`, מדיניות שמוערכת בתור התפקיד השואל.
   שם נמצאים הבאגים ששורדים ביקורת קוד.
4. **לשמור על המטרה.** מוצר שיחזיק שנים, יעמוד בתנועה, לא ידלוף, לא
   ישקר למשתמש — **ובעיקר ימלא את הייעוד שלו.** פיצ׳ר יפה שלא מקרב
   משמרת לאיוש נדחה.

**ומעל הכול: אל תיתן למוצר להבטיח מה שאינו יודע.** כל הסבב האחרון סבב
סביב וריאציה אחת של השקר הזה — סיכה במרכז העיר שנראית כמו כתובת מדויקת,
קישור ניווט שמוביל לכיכר, דגל "מיקום מדויק" על משמרת שהתקיימה במקום אחר.
**זה דפוס, לא באג. תחפש אותו בכל פיצ׳ר חדש.**

---

## 1. המוצר בשלוש שורות

שוק שמחבר מסעדות בישראל לעובדי משמרות: ברמן, מלצר, טבח, עובד שטיפה,
איש פלור. הערך הוא **סגירת נפילה ברגע האחרון** — מסעדה שנשארה בלי שוטף
ב‑18:00 מוצאת מישהו תוך דקות.

**המדד היחיד: דקות מפרסום המשמרת עד עובד מאושר.**

**מה אנחנו לא:** חברת לידים, **לא חברת כוח אדם** — קטגוריה מורשית
בישראל. המערכת לעולם לא משבצת אוטומטית: אדם לוחץ "אני לוקח", אדם לוחץ
"מאושר". **זו לא העדפה מוצרית אלא קו רגולטורי**, והוא נאכף בתוך פונקציות
המסד. אל תבנה שיבוץ אוטומטי, גם לא "חכם", גם לא "רק המלצה שמאשרת את עצמה".

---

## 2. מצב אמיתי, נמדד עכשיו

| | |
|---|---|
| ראש מיגרציות | `20260921125932 board_bucket_terms` · 42 מיגרציות |
| משתמשים | 10 — **מהם 4 יכולים להגיש בכלל** |
| טלפונים מאומתים | **0** |
| מפעילים (`private.operators`) | **0** |
| עסקים פעילים / מאומתים / בלי סיכה | 2 / 0 / 1 |
| משמרות סה״כ / פתוחות כרגע | 3 / **0** |
| מועמדויות / מאושרות | 2 / 2 — **שתיהן על משמרות `instant`** |
| ביקורות | 0 |
| מחרוזות `ui` | 312, אף קוד בלי שפה |
| פונקציות ב‑`public` | 22 · **אפס נגישות ל‑`anon`** |
| מבחן שלמות | **15 מתוך 16 שורות מחזירות 0** |

השורה היחידה שאינה אפס: `address but no pin = 1` — העסק של המייסד,
"ביסטרו נוטר", כתובת אמיתית וסיכה במרכז נתניה. **ידוע, מתועד, ממתין
לפעולה שלו. אל תדווח עליו כממצא.**

**המספר שצריך להטריד אותך יותר מכולם: אפס משמרות אוישו דרך המערכת אי
פעם.** שתי המועמדויות המאושרות הן `instant` מנתוני זרע. הסיכון בפרויקט
הזה אינו הקוד — הוא ששני הצדדים לא יגיעו. ראה `ROADMAP-AND-HIRING.md`.

---

## 3. איפה הכול יושב

**שני ריפואים, תיקיית עבודה אחת** (`C:\Users\vladi\OneDrive\שולחן העבודה\משמרת מקור`):

| | |
|---|---|
| **`mak-bag/mishmeret-app`** | PROD. 13 קבצים: `app.html`, `assets/`, שני קובצי המותג, `backup.yml`, `.gitignore`. קומיט שורש `01acdeb`, היסטוריה נקייה |
| **`mak-bag/mishmeret-docs`** | כל התיעוד, 41+ קבצים, יושב ב‑`docs/` |

`mak-bag/mishmeret` הישן **נמחק** — הוא ערבב את השניים.

**מלכודת בתיקייה:** הענף המקומי `main` הוא שריד של הריפו שנמחק. הענף
שתואם את ה‑PROD החי הוא **`prod`**. `.git` נעול ברמת מערכת ההפעלה
(OneDrive) ולא הצלחתי לאתחל — סגירה ופתיחה של החלון בדרך כלל משחררת.

**Supabase** `mishmeret`, ref `rkmjggormlxcvzuqedig`, Postgres 17, free tier.
**Cloudflare** מחובר; **R2 לא מופעל** (403) — פעולת מייסד.
**אין node, אין python.** שרת סטטי ב‑PowerShell — הסקריפט מוטמע
ב‑`DESIGNER-HANDOVER-PROMPT.md`.

---

## 4. החלטות נעולות — אל תהפוך אחת בלי לקרוא את הנימוק

הרשימה המלאה: `ROUND-M-MAP.md` (D1–D18) ו‑`ROUND-N-DECISIONS.md`.
אלה שאם תשבור אותן תשבור את המוצר:

- **כל חוק עסקי חי במסד.** הלקוח קורא RPC ומצייר תוצאה. שני מימושים
  לחוק אחד = אחד מהם שגוי ואף אחד לא יודע איזה. זה מה שמאפשר למוצר
  לחיות בלי צוות backend.
- **אישור דו‑צדדי אנושי.** הקו בין חברת לידים לחברת כוח אדם.
- **18+ לתפיסת משמרת.** חוק עבודת הנוער אוסר 22:00–06:00 על גילי 16–18 —
  בדיוק החלון שלנו. נאכף בשרת. **צריך עורך דין לפני שינוי.**
- **לעובד אין קואורדינטות. בשום מקום.** עיר ורדיוס בלבד. חל גם על מובייל:
  אל תבקש הרשאת מיקום.
- **גיל לא נחשף.** `birth_date` ב‑`profile_contacts`, מעסיק רואה `is_adult`
  בלבד, ו‑`private.is_adult` נשללה גם מ‑`authenticated`.
- **מרכז העיר אף פעם לא נשלח כסיכה.** לא נגעו במפה → לא שולחים נקודה.
- **`shifts` הוא רשומת היסטוריה.** `lat/lng/located_at/biz_name` הם
  צילום מצב; משמרת שהסתיימה לא נכתבת מחדש לעולם.
- **כל מחרוזת שהמשתמש רואה ב‑`terms`**, עברית ואנגלית.
- **רצפת הרשאות:** `anon` — SELECT בלבד, אפס פונקציות.

---

## 5. מה פתוח, לפי סדר

### 5.1 חוסם פונקציונלי — **הדבר הראשון שתעשה**
**המעצב מצא ש‑`shiftRow` פולט שורה בלי `data-shift`.** התוצאה: בדליים
"מחר", "השבוע" ו"בהמשך" **אין כפתור וגם אין שורה לחיצה — אי אפשר לפתוח
משמרת ואי אפשר להגיש.** שלושה רבעים מהלוח מתים.

זו רגרסיה שנכנסה עם חלופה ג. המעצב לא תיקן בכוונה: `cursor:pointer`
על משהו שלא קורה בלחיצה הוא שקר קטן. **תן למפתח לסגור, ואז המעצב מוסיף
את מצב ההצבעה בשתי שורות.**

### 5.2 התפר — מה שאיש לא הוכיח
**אף סעיף לא נבדק מקצה לקצה.** רתמת הלקוח מוכיחה מה הלקוח שולח;
התחזות במסד מוכיחה מה השרת עושה; **דפדפן מחובר מול PostgREST אמיתי —
לא נבדק מעולם.** המייסד סירב להיכנס בשבילנו, ואיש מאיתנו אינו רשאי
להקליד סיסמה. מסלול שרתי שמנפיק סשן נחסם על ידי מסווג ההרשאות.
**אל תתחזה שזה נבדק. אל תנסה לעקוף את המסווג.**

### 5.3 שני שערים שאני מחזיק סגורים בכוונה
- **נעילת סיכה לפני פרסום משמרת.** נדלקת רק אחרי שעסק אחד קיבל
  `located_at` דרך האפליקציה האמיתית. הרתמה מוכיחה שהלקוח שולח נכון;
  היא לא מוכיחה שהמסלול האמיתי כותב. אם תדליק מוקדם — **המייסד לא
  יוכל לפרסם משמרת.**
- **אכיפת `phone_verified`.** 0 מתוך 9 מאומתים ו‑0 מפעילים. להדליק
  עכשיו = לחסום כל תפיסה וכל פרסום, כולל של המייסד.

### 5.4 בתור
1. **סכימת זמינות מוצהרת** — יום, שעות, תפקידים, רדיוס. **שלך לכתוב.**
   זו התשתית להתראות; בלעדיה התראה היא ספאם, ומשתמש מכבה התראות פעם
   אחת ולתמיד.
2. **36 שדות `status_snapshot`** ל‑DevOps — חוסם את עמוד הסטטוס.
   ה‑hash כבר בידיי: `dde8b829…fadb63`. **הוא לא מסר את הסוד ואל תבקש.**
3. **גיבוי מחוץ למכונה** — `backup.yml` על ענף ברירת המחדל ולכן התזמון
   חי, אבל **מעולם לא רץ בהצלחה** ו‑R2 חסום.
4. **אימות עסק** — `verified_at` קיים, אפס מאומתים. לפני המעסיק הראשון
   שהמייסד לא פגש.

### 5.5 אצל המייסד
הפעלת R2 · סימון הסיכה של ביסטרו נוטר · **הוספתו כמפעיל ראשון** —
בלעדיה מנגנון האימות תיאורטי לגמרי.

---

## 6. מלכודות שעלו לנו זמן. כולן אומתו

1. **בדיקת RLS בלי `set local role authenticated` רצה כ‑postgres,**
   עוקפת הכול, ו"עוברת" בלי לבדוק דבר. המסוכנת ביותר.
2. **`drop function` + `create` מחזיר ACL ריק**, ו‑Supabase מעניקה
   מחדש ל‑`anon` דרך default privileges. `revoke ... from public`
   **לא** מסיר מענק מפורש ל‑`anon`. קרה לי פעמיים. QA הציע שורת Q5
   שתספור פונקציות בלי ACL מפורש — **תוסיף אותה.**
3. **מדיניות RLS מוערכת בתור התפקיד השואל.** 11 מדיניות קוראות
   ל‑`private.*`; revoke גורף מ‑`authenticated` משבית חמש טבלאות.
   **רצפת הרשאות נקבעת לפי פונקציה, לא לפי סכימה.**
4. **גיליון CSS שמוזרק דינמית נוחת אחרי שלך ומנצח בסדר המקור.**
   רתמת בדיקה חייבת לשחזר את הטוען האמיתי; רתמה עם `<link>` סטטי
   נתנה לי ציון עובר שגוי.
5. **סגוליות: `.map X` מול `.leaflet-container X` זה תיקו**, וסדר לא
   מנצח סלקטיביות. חיבור בלי רווח = `(0,3,0)`.
6. **`git ref:path` משקר ב‑git bash על Windows** — MSYS ממיר נתיבים
   ושובר את התחביר. `MSYS_NO_PATHCONV=1`. DevOps כמעט דיווח ממצא שקרי.
7. **`git diff --cached --name-only` משווה מול HEAD** ולכן מטעה
   כשבונים עץ חדש. תבדוק את אובייקט העץ: `git ls-tree -r`.
8. **אל תערוך `app.html` בהחלפת מחרוזות מ‑shell.** זה כבר קטע שישה
   template literals. כלי Edit בלבד.
9. **מחזיק אחד לקובץ.** שלושה עורכים במקביל ייצרו שתי מערכות CSS
   מתחרות ועלו חצי סבב. **חוזה המחלקות נקבע על ידך מראש, בכתב.**
10. **מה שלא נכתב לקובץ — לא קרה.** שלושה בריפים ושני ממצאי QA אבדו
    בצ׳אט. ממצא שמגיע כסיווג בלי תוכן — סוגרים אותו כלא־התקבל.

---

## 7. איך לאמת כל דבר כאן

```sql
-- מצב סכימה
select version, name from supabase_migrations.schema_migrations
 order by version desc limit 5;

-- התחזות. בלי שתי השורות האלה אתה postgres וכל בדיקה חסרת ערך.
begin;
select set_config('request.jwt.claims',
       '{"sub":"<uuid>","role":"authenticated"}', true);
set local role authenticated;
  -- ...
rollback;   -- חובה. אל תשאיר כתיבות בנתוני המייסד.
```

**מבחן השלמות של QA — הרץ בפתיחה ובסגירה של כל סבב.** 19 שורות
ב‑`QA-HANDOVER.md`, כולל שתי שורות WRITE‑PROOF שמוכיחות ש‑QA לא כתב.

**בדפדפן:** שרת מקומי, לא `file://`. מודולי ES לא ייטענו.

---

## 8. השעה הראשונה שלך

1. **קרא `START-HERE.md`** — מצב הצוות ומה כל אחד מחזיק עכשיו.
2. **הרץ את מבחן השלמות.** אם משהו חוץ מ‑`address but no pin` אינו
   אפס — עצור והבן לפני שאתה נוגע במשהו.
3. **סגור את 5.1.** שלושה רבעים מהלוח לא לחיצים ברגע זה.
4. **קרא את ארבעת התיקים האחרונים** — `DEV-HANDOFF-N1-R2.md`,
   `N2-REVIEW-TECHLEAD.md`, `DEVOPS-AUDIT.md`, `QA-ACTIONS-LOG-N.md`.
   כולם מסתיימים בשאלות שממתינות להכרעה שלך.
5. **ואז הזמינות המוצהרת.** היא שלך, והיא חוסמת את הפיצ׳ר הבא.

---

## 9. שלוש מילים על איך לנהל אותם

**הם מדווחים טוב כי הם יודעים שבודקים.** שמור על זה. בכל סבב עד כה
מצאתי פער אחד לפחות בין דיווח למציאות — ובכל פעם שחשדתי במספר של עובד
**הטעות הייתה שלי**. שתי הפעמים האלה שוות יותר מכל שאר הביקורות:
הן לימדו אותי למדוד לפני שאני מתקן.

**כשעובד תופס טעות שלך — תגיד את זה בקול.** המעצב תיקן דיווח לרעת
עצמו, DevOps פתח בתיקון עצמי לפני הדיווח, והמפתח מצא באג שלוש ביקורות
קוד פספסו. זה קורה רק כשזה בטוח.

**וכשאתה טועה — תכתוב את זה בקובץ.** שלוש טעויות שלי מתועדות בשמי
ב‑`ROUND-M-MAP.md` ו‑`ROUND-N-DECISIONS.md`. זה מה שמחזיק את התקן.

---

## 10. פורמט החפיפה — לסיים בו כל יחידת עבודה

```
--- HANDOFF TO: <role> ---
DONE:      מה אומת ואיך. לא מה נוסה.
STATE:     מצב המסד, הריפו והאפליקציה שהמקבל ימצא.
NEXT:      מה צריך לקרות, לפי סדר.
FOR YOU:   הדבר הספציפי שהקולגה הזה חייב לעשות.
MUST KNOW: מוקשים שיעלו לו שעה.
OPEN:      מה לא נסגר, כולל שאלות למייסד.
--- END HANDOFF ---
```

**"עבר" נאמר רק על מה שהורץ.** מה שלא הורץ — אומרים שלא הורץ, ולמה.
דיווח מנופח נתפס כאן בכל סבב עד היום.
